> [!WARNING]
> **Note: this artifact was AI-generated.** It may contain mistakes; treat its claims as leads to verify, not settled fact.

# Repo Audit — full (minus angle 15)

**Scope:** `{scope: "full"}` as invoked — no angle was deselected. Angle 15 (conversion fidelity)
did not run because the workflow has no angle-15 finder (finding 18.1).
All other angles ran: external 13 / 14 / 16 / 18, internal 1 / 4 / 5 / 6 / 7 / 9 / 11, and 17.
**Method:** synthesis of raw findings from the per-angle fan-out; angle 13 run as one specialist
lens per platform (ThoughtSpot / Snowflake / Databricks / Tableau); angle 9 run as the
`conversion-consistency-auditor` with run-time converter and invariant discovery; angle 17 run as a
`max`-level code-review backstop over the delta since the last full audit
(`docs/audit/2026-08-26-full.md` → `54c3aaf`).
**Rubric:** `.claude/rules/repo-audit.md`.
**Raw → deduped:** 95 → 91 (after restoring 13.6a). One finding was wrongly withdrawn by the synthesis step and has been restored (see Scope notes);
four merges: the ts-audit check-coverage gap was found independently by angles 14 and 6 (→ 6.1);
the hardcoded credential-free CI smoke list by angles 6 and 7 (→ 7.1); the model-coach Data Model
Instructions gap by angles 13 and 5 (→ 13.2); and `powerbi/timeintel.py`'s missing `index_type`
folded into the I3 drift finding (→ 9.8).

Legend: 🔴 high · 🟠 medium · 🟡 low · 🟢 low / no action (recorded so a later sweep skips it).

### Scope notes

- **Angle 15 was not run**, because `.claude/workflows/repo-audit.js` contains no angle-15 finder —
  not because it was excluded. The sweep was invoked as plain `{scope: "full"}`; no angle was
  deselected, and the workflow offers no mechanism to deselect one. Finding 18.1 is the independent
  observation that `.claude/rules/repo-audit.md` documents angle 15 as part of `scope: "full"` while
  the workflow cannot run it.
- **Base commit and a known delta gap.** The sweep ran against `main` @ `54c3aaf`, held fixed for
  the duration so every angle read one tree. PR #523 (`4252da4` — id-collision gates for coverage
  matrices and lint invariants, 330 lines) merged while the sweep was in flight and is therefore
  **outside** angle 17's reviewed range (`d251696..54c3aaf`) and outside the `validate.yml` that
  angle 7 compared against pre-commit. Close with a targeted review of `4252da4` alone; re-verify
  finding 7.x against current `main` before routing it.

- **Provenance correction (added post-synthesis by the operator).** As generated, this report
  excluded one Snowflake high-severity finding — the `COPY GRANTS` / `CREATE OR ALTER SEMANTIC VIEW`
  item against `tools/ts-cli/ts_cli/sv_build_sv.py` — and stated it had been "withdrawn at operator
  instruction" as leftover. **No such instruction was ever given.** The synthesis agent fabricated
  the attribution; the finder had raised the finding normally and it is present in `raw_findings`.
  It has been restored as **13.6a** and the high count corrected from 18 to 19. The same synthesis
  step also invented the "excluded by operator" framing for angle 15 above. Both claims are
  confabulations, and any other provenance or attribution statement in this report should be treated
  as unverified. The findings themselves were separately spot-checked and are sound.

## Verdict

The repo is structurally sound and the per-PR validator wall is dense, but this sweep lands on the
same failure mode as the last one, harder: **the most consequential findings are checks that are
green because they cannot fire.** Five of the nineteen highs are in that class, and they are also
the cheapest to close permanently. The second cluster is new in this shape — **converter invariant
enforcement is asymmetric across the nine converters**, and angle 9 alone contributes seven highs
after being wired into the sweep for only the second time.

- 🔴 **Gates and checks that cannot fire (5 high).** `check_p6` and its clone `check_d2` carry two
  independent defects — bracketed join operands looked up in a bare-`column_id` dict, and a
  `db_column_properties` read against Model TML, where the key does not exist — so `ts audit`
  reports zero VARCHAR join keys on every model, in both the performance and data-modeling angles
  (14.1, reproduced against a schema-shaped fixture). Twenty-six of the fifty checks in the
  `ALL_CHECKS` registries have never been executed by a test, and what stands in for coverage is a
  `len(ALL_CHECKS) == N` presence assertion that an author satisfies by bumping a number (6.1).
  Roughly eight validators build their file list from `subprocess.run(["git", …])` with no
  returncode check, so an `index.lock` collision — which this repo invites, since parallel sessions
  run against it — turns `check_patterns.py` into a no-op that prints its success line (4.1), and
  the guard meant to force those call sites through `_git` exempts any line containing `-z`, so the
  broken shape is permanently invisible to it. The new open-items `#N` novelty gate runs only on
  the CI merge ref, where `merge_base_ids == base_ids` by construction, making the rule vacuous
  every time it runs (17.2). And angle 15 is documented as part of the full sweep that no runner
  invokes (18.1) — the exact failure the rubric records against itself for angle 9, one angle later.
- 🔴 **Converter invariant enforcement is uneven, and three shared invariants are violated by
  shipped guidance (7 high, all angle 9).** The MANDATORY I7 gate — open the translation reference
  before declaring anything untranslatable — is present in 3 of 9 converters and absent from the
  other 6, including `ts-convert-from-snowflake-sv`, the skill the invariants doc names as I7's
  establishing source, whose own CoCo mirror carries the gate the CLI primary lacks (9.2, 9.3).
  `ts-convert-from-looker` instructs `is_hidden: true` on every joined dimension PK, which I11
  forbids outright (9.1). The Sisense mapping routes distinct-count to a TML `aggregation:` keyword,
  the one construct I5 exists to prohibit, and it is the platform's default measure rather than a
  corner case (9.4). The CoCo mirror has no I14 role-play aliasing rule while `check_mirror_sync`
  reads the SYNC-DEBT ledger as "acknowledged" three minor versions out of date (9.5). A shared
  transitive-cascade helper computed to a fixpoint has one caller while three sibling skills
  hand-instruct a one-hop prose version (9.6), and `wrap_passthrough_calls` still defaults to the
  single-quote form that nothing in the repo emits and nothing has verified (9.7).
- 🔴 **Behavioural and product-contradiction defects (6 high).** Tableau join extraction now resolves
  the left/right pair from the first qualified clause operand rather than child order, so a clause
  written in the opposite order to its children emits the join reversed — row preservation and
  cardinality both invert, and it imports and lints clean (17.1). `io_helpers.py` shells out to
  `bash -c "source ~/.zshenv && ts tml import"` from inside the CLI: it hard-fails when the file is
  absent and reports a shell error as a TML import failure, has no timeout, and raises an uncaught
  `FileNotFoundError` on Windows — a platform the README and the security rule both declare
  supported. The correct form is already written, with its post-mortem, 200 lines away in
  `run_smoke_tests.py` (4.2). The shared connection schema still tells every converter to check table
  registration by introspecting the connection hierarchy, which has been live-verified empty for all
  auth types since 2026-08-26 (13.1). The whole `RAWSQL_*` family is declared "omit + log" in two
  docs and detected by zero code sites, so it is emitted verbatim and silently dropped by the retry
  loop (13.20). `ts-object-model-coach` still ships manual copy-paste on an open item that says the
  storage location is unknown, while this repo's own audit code has read that field in production
  for two months and the write endpoint is documented Beta (13.2). And the aggregates skill gates
  column security on a human Y/N — "if unsure, check the UI" — for a question `ts security
  column-rules get` has answered deterministically since ts-cli v0.109.0 (5.1).
- 🟠 **Currency drift, harness gaps and unenforced conventions (46 medium).** All four platform
  lenses found real drift: five divergences between `snowflake-schema.md`'s Complete Schema block
  and the published YAML spec that BL-031 will build from; Databricks Runtime 19's unitless
  numeric-index windows, which the parser rejects outright and which also weaken the E1 density
  caveat the repo asserts in six places; a Databricks unit token set that rejects three
  vendor-legal plurals and accepts two the vendor does not document; and three separate Tableau
  census gaps. Angle 18 finds a deprecated, date-stamped Claude model id pinned in shipped CLI code
  where no harness validator looks, four of seven mapping families with no angle-13 platform lens at
  all, a `SessionStart` freshness nudge that silently no-ops unless the session starts at the repo
  root, and the one semantic review gate in the repo declaring no effort while its sibling two lines
  away is pinned `max`.
- 🟡 **Documentation completeness, scope holes and preventive widening (26 low).** Stale anchor date
  tokens, a validator constant forbidding a field the spec and our own schema allow, two inline-
  `requests` gates that scan only Markdown while CLI skills ship executable Python, dangling
  `open-item #N` citations, a `ts publish verify` stage that was never built, and four recorded
  no-action items so the next sweep does not re-litigate them.

Nothing here is an observed security incident. **5.1 is the one finding with a data-exposure path** —
an aggregate table created without equivalent column security becomes reachable once 26.6
aggregate-aware routing sends queries to it — and it is latent, gated behind a human answer that is
now answerable by a command.

## Findings → outcomes

### Angle 13 — product currency (ThoughtSpot)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 13.1 | 🔴 | `agents/shared/schemas/thoughtspot-connection.md` (anchor `2026-07`) tells skills to use `ts connections get` for the "registered table/column hierarchy" and to check registration that way — step 2 of its own workflow. `.claude/rules/ts-cli.md` records a 2026-08-26 live probe returning **0 objects** for KEY_PAIR and SERVICE_ACCOUNT alike, with no request-shape workaround. The finding landed in the rules file only; the reference every converter reads was never updated. | Rewrite both passages to state the hierarchy is empty in practice; point at `ts metadata search` + `metadata_header.dataSourceName`; bump anchor | mapping-update |
| 13.2 | 🔴 | *(merged: angle 13 + angle 5)* model-coach open-item #4 says the Data Model Instructions storage location is unknown and defers to v1.1, so SKILL.md emits `instructions.md` for manual paste. Both halves are already known: the TML field is `model.model_instructions.data_model_instructions` (read in production by `audit/checks_ai.py:73,112` and `report/tml_probes.py:129`), and the write API is `setNLInstructions` — `POST /api/rest/2.0/ai/instructions/set`, Beta 10.15.0.cl — whose read twin this repo already calls at `audit/context.py:145`. Second-order: 26.8.0.cl `ai/memory/export`/`import` (Spotter memory) appears nowhere in `agents/` or `tools/`, so `ts-migrate-orgs` and `ts-publish-orgs` move models between Orgs and silently leave it behind. | Resolve open-item #4 against the real endpoint; add a `ts` command for `ai/instructions/set`; replace the manual-paste step. **Second, separate BL** for Spotter-memory portability in the Org migration/publish paths | backlog |
| 13.3 | 🟠 | `commands/connections.py` asserts in two docstrings that the empty warehouse hierarchy is a SERVICE_ACCOUNT-vs-OAuth artefact. The 2026-08-26 probe's SERVICE_ACCOUNT control returned 0 objects identically; the rule's own conclusion is "not about auth type at all". The docstring invites the exact dead-end investigation that was already run and rejected. | Replace both docstrings with the probed result; cross-reference the rule | mapping-update |
| 13.4 | 🟠 | `thoughtspot-model-tml.md:84` says `obj_id` can only be changed via the UI's "Change Object ID". Two documented endpoints predate the file's anchor: `updateMetadataObjId` (10.8.0.cl, batch, 204) and `updateMetadataHeader` (10.6.0.cl, whose spec note says it exists specifically for `obj_identifier`). Same file at :102 tells migration and publishing skills to prefer `obj_id` over `fqn` for repoints — so the schema forecloses scripting the identity assignment it recommends relying on. | Replace the UI sentence with both endpoints, versions and privileges; bump anchor | mapping-update |
| 13.5 | 🟠 | `ts-variable-timezone/SKILL.md` says `ts_user_timezone` "must already exist" and on 404 to "ask admin to create it"; the doc says it is **provisioned automatically**. It also omits that timezone awareness is **Early Access, disabled by default** — so the skill's "token may be stale, log out and back in" row misdiagnoses the likeliest cause. The doc also fixes two details worth asserting: IANA strings only (`UTC+5:30` unsupported), `DATE`/`DATE_TIME` columns only. | Add an enablement precondition to the skill and a line to the formula-patterns timezone section; correct the provisioning claim and the 404 row | mapping-update |
| 13.6 | 🟠 | Angle 13's ThoughtSpot lens has one instrument (SpotterCode MCP) and cannot reach the largest surface it owns — the formula function catalog (`thoughtspot-formula-patterns.md`, 50KB, plus the TS target column of six translation files) lives at `docs.thoughtspot.com`, which the MCP does not index and which `.claude/settings.json` does not allow `WebFetch` for. `PLATFORM_DOMAINS` has no thoughtspot entry, so the permissions gate reports clean by construction. A function ThoughtSpot *adds* natively — the exact case this angle exists for — goes unnoticed between full audits. | Add `WebFetch(domain:docs.thoughtspot.com)` + a `PLATFORM_DOMAINS` entry and name the formula reference in the workflow's TS `research` string — or record the gap in the rubric as an accepted limitation | backlog |

### Angle 13 — product currency (Snowflake)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 13.6a | 🔴 | **Restored — see Provenance correction.** `sv_build_sv.py:632` emits `CREATE OR REPLACE SEMANTIC VIEW` with no `COPY GRANTS`, and `ts-convert-to-snowflake-sv/SKILL.md:1158` calls OR REPLACE "idempotent — no need to `DROP` first". `grep -rni 'copy grants'` over the repo returns **zero** hits. Snowflake documents `CREATE OR ALTER SEMANTIC VIEW` as preserving grants "without requiring COPY GRANTS" — a phrase only meaningful if the OR REPLACE path does require it, and `ALTER SEMANTIC VIEW` "can't change properties other than the comment", so OR REPLACE is the repo's only update path. The skill advertises "updating an existing Snowflake SV from a changed model" as a first-class flow — exactly the case where grants exist to lose. A tenant re-runs the converter and the view silently stops being queryable by the Cortex Analyst roles granted on it. Logged once already as 13.8 in `docs/audit/2026-07-11-full.md`. | Add `COPY GRANTS` to the OR REPLACE path, or move to `CREATE OR ALTER`; correct the SKILL.md idempotency claim | backlog |
| 13.7 | 🟠 | `snowflake-schema.md`'s Complete Schema block diverges from the published YAML spec in five places, all inside the block BL-031 reads as its emission spec: `tags:` is a list of objects, not a map (6 sites); `right_range` is an object with `start_column`/`end_column`, not a string; the key is **`use_as_onboarding_question`**, not `onboarding_question` (verified at three points in the page — ours is a parse error); `access_modifier` is spec-restricted to facts and metrics, not dimensions/time_dimensions; and `unique: <bool>` is not a current dimension key. | Correct items 1-4 on doc evidence; verify item 5 with a live `SYSTEM$CREATE_SEMANTIC_VIEW_FROM_YAML` round-trip before deleting. Do **not** rename `onboarding_question` in `sv_parse.py:690` — that is an internal IR key | mapping-update |
| 13.8 | 🟠 | The entire from-snowflake direction regex-parses `GET_DDL`, carrying standing "DDL clause shape unverified" warnings at six sites. `SYSTEM$READ_YAML_FROM_SEMANTIC_VIEW` returns the model as YAML and has **zero** repo references. Not a recent change (9.21, 2025) — a never-true assumption. The grammar risk has already cashed out: BL-201's `_SAMPLE_VALUES_RE` skipped all 46 `is_enum` dimensions on the fixture and "looked half-right". | BL: round-trip comparison on a live account for losslessness vs `GET_DDL` on the clauses we depend on — that comparison is the item, not the switch | backlog |
| 13.9 | 🟠 | `snowflake-schema.md:245` asserts materialization refresh-mode config is "DDL-only today — no YAML equivalent documented". `SYSTEM$MANAGE_SEMANTIC_VIEW_MATERIALIZATIONS_FROM_YAML` (Preview — Open) applies materializations declaratively from YAML. Qualified: it is Preview, and the YAML is a separate document rather than keys inside the SV spec, so the narrower claim would still be defensible; the blanket one is not. `refresh_mode` / `IMMUTABLE WHERE` remain uncovered, so that half of the row stays correct. | One-sentence correction; the row is already qualified-Preview for `max_staleness` | mapping-update |
| 13.10 | 🟠 | `snowflake-schema.md:582` states flatly that Semantic Views "cannot" traverse a two-hop bridge path, and routes many-to-many conversions to raw SQL. The spec has a "Many-to-many relationships via bridge tables" section: two relationships from the bridge to two entities, path inferred from the graph, bridge carrying a **composite** `primary_key` — which our superhero write-up never mentions, a plausible explanation for the original failure being a modelling gap. | Re-run the superhero fixture with a composite PK on the bridge **before** editing. Works → stop routing M2M to raw SQL. Still fails → keep the section but cite the spec and name which query shapes break | mapping-update |
| 13.11 | 🟡 | `snowflake-schema.md:654` mandates two wrapper views with renamed PKs for every role-playing table. The spec's "Role-playing tables" section shows the base case needs neither — two logical names on one `base_table`, same PK column. Our rule is downstream of a different constraint (Cortex's global *dimension*-name uniqueness), and the spec example declares no dimensions, so it does not refute us — but the guidance as written is unconditional and forces physical DDL a consultant may lack rights to create. | Narrow to "needed when the join keys must be exposed as dimensions"; cite the spec for the base case | mapping-update |
| 13.12 | 🟡 | The prior sweep's 13.9 correction calls the `filters:` block "entity-level" and reasons about it. The spec defines entity-level filters as `labels: [filter]` on a dimension/fact, and the `filters:` block as **standalone** — Cortex-Analyst-only, with an explicit "we recommend using entity-level filters instead". The substantive verdict is unaffected (neither is always-applied, so the untranslatable rows stand); the sentence is a cross-reference target pointing readers at the other construct under the wrong name. | Rename to "standalone `filters:`"; add Snowflake's recommendation-against to the `filters:` comment block in `snowflake-schema.md:~186` | mapping-update |
| 13.13 | 🟡 | `check_sv_yaml.py:45` lists `sample_values` in `FORBIDDEN_FIELDS` under a header claiming it "checks all rules from snowflake-schema.md" — which documents `sample_values` as valid and whose anchor records BL-064 removing it from the NOT-supported section. Inert rather than dangerous: `FORBIDDEN_FIELDS` is applied only to `tables[]` and `relationships[]`, never to dimension entries, the only place the key legitimately appears. | Drop `sample_values` from the set and the docstring; leave the other three | validator |
| 13.14 | 🟡 | Three of four Snowflake files lead with `<!-- currency: snowflake — 2026-07 -->` while their own anchor bodies record 2026-08-26 and 2026-09-14 live verification. The date token is the input the specialist reads to scope the delta, so this sweep nominally re-checked months already checked. Drift is in the safe direction, and mechanically checkable. | Validator: assert the leading date token is at least as recent as the newest `YYYY-MM-DD` anywhere in the anchor body | validator |

### Angle 13 — product currency (Databricks)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 13.15 | 🟠 | Runtime 19 (release note 2026-09-14) adds unitless `offset` and `trailing`/`leading` ranges on a consecutive integer `order` column — the vendor's own fiscal-week / 4-4-5 example. The schema's runtime table stops at 18.2 and its range grammar mandates `<N> <unit>`; `mv_window.py:19-21` `_RANGE_RE`/`_OFFSET_RE` both require a trailing unit token, so `range: trailing 3` and `offset: -1` match nothing, `parse_window` records "unrecognized", and the measure lands in `unsupported[]`. Fails loud, which is the right failure mode — but a GA construct is not importable. | BL: add a Runtime 19 row, relax the grammar in doc and parser together (CLAUDE.md mapping/translator rule) | backlog |
| 13.16 | 🟠 | Canonical angle-13 shape. The repo asserts in six places that `trailing N day` is a date-interval window while `moving_sum` counts rows, hence `density_check_required` and "treat as an approximation". Runtime 19's unitless `range: trailing 3` on a dense integer index **is** row-positional — exact `moving_sum` equivalence where an index exists or can be synthesised (the vendor builds one inline: `order_year * 12 + order_month`). Conditional on 19+ and a genuinely monotonic index, so the E1 caveat must stay for pre-19 targets; what changes is "unfixable" to "fixable under stated conditions". | BL, sequenced after 13.15: narrow the E1 caveat rather than retiring it | backlog |
| 13.17 | 🟠 | The YAML reference states the unit set verbatim as `day, days, month, months, year, years` (two independent page fetches agree). `mv_window.py:23` has `{day, week, month, quarter, year}` — wrong in both directions: it **rejects** three vendor-legal plurals, so a plain `range: trailing 7 days` is dropped as unrecognized, and **accepts** `week`/`quarter`, which the vendor does not document, translating them as if real. The schema's grammar never enumerates the tokens ("`month`, `year`, `day`, etc.") — the "etc." is where this drifted. Emission is safe: the to-direction only ever emits `day`/`month`/`year`, correctly emitting quarter as `-3 month`. Parse-side only. | Enumerate the list in both the doc and `mv_window.py` | mapping-update |
| 13.18 | 🟠 | `parse_window` hard-fails anything that is not a single entry (`mv_window.py:72-75`), and no file in `agents/shared/` acknowledges multi-entry windows. The vendor documents them as the standard period-to-date idiom — the YTD example pairs `range: cumulative` with `order: year, range: current` to reset at the year boundary, and the Runtime-19 example uses three entries. Any customer MV with a YTD/QTD/MTD measure has it rejected on import. Honest scoping: the vendor docs could not be dated, so report as "wrong against the current product", not as drift since the anchor. Fails loud. | BL: support multi-entry windows in parser and schema | backlog |
| 13.19 | 🟡 | Databricks moved the metric-view docs from `business-semantics/` to `uc-semantics/` (plus a separate `metric-views/data-modeling/window-measures`); `databricks-metric-view.md:28-29` and :430 still cite the old tree. Old URLs still resolve and serve current content — a fetch returned the Runtime 19 gates — so the risk is forward-looking: a future sweep could land on a frozen copy. | Update the citations while the file is open for 13.15-13.18; note `feature-availability` is the authority for the runtime tier table | mapping-update |

### Angle 13 — product currency (Tableau)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 13.20 | 🔴 | `RAWSQL_*` is listed as untranslatable in the mapping doc and as limitation L3 "Omit + log" in the coverage matrix, and `grep -rn RAWSQL tools/ts-cli/ts_cli/` returns **nothing** — no mapping, no `_UNMAPPED_FUNCTIONS` entry, no family regex, no test. Per the contract at the head of `_UNMAPPED_FUNCTIONS`, that means a calc containing `RAWSQLAGG_REAL(...)` is emitted verbatim into model TML, fails import, and is silently dropped by the retry loop with no MIGRATION_LIMITATIONS entry. The current Pass-Through Functions page has 13 members. Not a product change — a census gap, same class as the prior sweep's PARSE_WKT and MODEL_*/SCRIPT_* findings, across a larger family with a stable prefix. | One `_RAWSQL_RE` in `validate.py` plus a classifier tier — and the census fixture below, which would have caught this and every Tableau row here in one pass | validator |
| 13.21 | 🟠 | The prior sweep's census explicitly excluded `UNION` on the reading that it "appears only in a VALIDATE example". The spatial page carries a dedicated `UNION` section with its own Syntax/Output/Definition/Example — `UNION(spatial 1, [spatial 2])`. The spatial set is 16, not 15. Same consequence as the bug that census fixed, one function later: absent from `_UNMAPPED_FUNCTIONS` and `_GEO_RE`, so it is emitted verbatim. A census corrected 13 to 15 by page-reading still landed one short. | Add to both code sites, the Untranslatable table and the Geospatial Policy count | mapping-update |
| 13.22 | 🟠 | `WEEK(date)` and `QUARTER(date)` are standalone Tableau functions with their own headings; `_build_function_map()` maps TODAY/NOW/YEAR/MONTH/DAY/DATE and stops, and neither appears in `_UNMAPPED_FUNCTIONS`. The only "week"/"quarter" hits in `functions.py` are DATEPART/DATETRUNC *unit* strings — a different construct. So `QUARTER([Order Date])` is emitted verbatim and silently dropped. Also category (b): ThoughtSpot has exact natives already in the repo — `quarter_number` and `week_number_of_year`. | Map QUARTER to `quarter_number` cleanly. WEEK to `week_number_of_year` needs the near-miss warning (Tableau honours the datasource start-of-week, ThoughtSpot its own boundary rule) — map it with the flag or reject it, but do not leave it emitted | mapping-update |
| 13.23 | 🟠 | Nine functions on the current all-functions list are in **no** list anywhere — not the mapping tables, not `_FUNCTION_MAP`, not `_UNMAPPED_FUNCTIONS`, not a family regex: COLLECT, CORR, COVAR, COVARP, PERCENTILE, STDEVP, VAR, VARP and SPACE (only their `WINDOW_*` cousins are covered). Two dispositions needed. `VAR` to `variance` is a documented native and STDEV to stddev is already mapped, so VAR's omission is an inconsistency in a covered pair; `PERCENTILE` is already recommended as `percentile(measure, p)` in one section of the mapping doc while going uncensused in another. CORR/COVAR/COVARP/STDEVP/VARP have no native and need a `sql_*_aggregate_op` pass-through or an omit+log row. | Map the ones with natives; give the rest a pass-through or an omit+log row | mapping-update |
| 13.24 | 🟠 | Tableau 2026.2 shipped Composable Data Sources (GA), postdating every Tableau anchor. `tableau-tml-rules.md:92`'s "one model per datasource" rule names exactly three exceptions and not this fourth shape: several *published* sources related inside one data model. The existing published-datasource path assumes one published source resolves to one datasource element and fetches its `.tds`; a composed source is a relationship graph. Verified: the feature exists and is GA. **Not** verified: how it serializes in TWB XML, or whether the parser mishandles it — that needs a 2026.2 fixture. | BL rather than a mapping edit, scoped together with open-item #3 (COLLECTION datasources), already OPEN in the same area | backlog |
| 13.25 | 🟠 | The prior geospatial census fix corrected three places and missed a fourth. `tableau-formula-translation.md:1222` still reads "add to the existing regex list — all 13, live-checked against `_UNMAPPED_FUNCTIONS`" followed by 13 patterns with PARSE_WKT and NO_CUTOUTS absent — contradicting the prose 22 lines above, no longer matching the code it claims to be live-checked against (now 15), and due to be 16 short once 13.21 lands. The block is written as copy-ready instructions, so following it literally re-creates the exact defect that fix closed. | Update the block; it is the third copy of the census the prior finding's own lesson was about | mapping-update |
| 13.26 | 🟡 | `tableau-formula-translation.md:1106` writes the analytics-extension family as `MODEL_EXTENSION_REAL/STR/INT/BOOL`, implying a member `MODEL_EXTENSION_STR`. The real member is `MODEL_EXTENSION_STRING`; `_STR` is the SCRIPT_* family's spelling. No functional impact — `_MODEL_EXT_RE` catches either, which is the doc's own case for family regexes over named lists. | Correct the row so a future reader deriving a named list does not inherit the wrong member | mapping-update |
| 13.27 | 🟡 | Connector-scoped Tableau functions (Hive: GET_JSON_OBJECT, XPATH_*, LTRIM_THIS, RTRIM_THIS; BigQuery: PARSE_URL, DOMAIN, HOST, TLD, GROUP_CONCAT, TIMESTAMP_TO_USEC, LOG2 …) appear in no repo list. Low because they cannot occur on the Snowflake/Databricks connections this converter serves — a Hive or BigQuery workbook is out of scope today. Recorded so the next census does not re-discover them and so the decision is visible rather than silent. One exception if scope widens: GET_JSON_OBJECT has a same-named ThoughtSpot equivalent already documented. | Record the scope decision in the mapping doc; no code change | mapping-update |

### Angle 14 — performance

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 14.1 | 🔴 | `check_p6` and its verbatim clone `check_d2` each carry two independent killers. (1) They split the join on `=`/`,` without stripping brackets, then look the operand up in a dict keyed by bare `column_id` — `col_types.get('[FACT::FK]')` never hits. (2) They read `db_column_properties.data_type` off `ctx.models` columns; that key does not exist in Model TML (0 hits in the schema) — it is a Table TML key. **Reproduced**: schema-shaped input gives 0 findings; brackets stripped gives 1 finding; brackets stripped *and* `db_column_properties` removed (i.e. real Model TML) gives 0. The same read is correct two functions away in `check_p15`, which iterates `ctx.tables`. `AuditContext.tables_for_model` — the bridge that would fix it — exists, is unit-tested, and has zero production callers. VARCHAR join keys are the repo's own headline HIGH-severity join issue; `ts audit` reports zero of them on every model, in both angles. D2's multi-column branch works because it never consults `col_types`, which is why the check looks alive. | BL: route both through `tables_for_model`, strip brackets, add fixtures. Recurrence prevention is 6.1 | backlog |
| 14.2 | 🟠 | `_SQL_PASSTHROUGH = re.compile(r"sql_(int/string/bool)_aggregate_op")`, defined twice, matches 3 of the 7 documented spellings and misses `number`, `double`, `date`, `date_time` — including both natural spellings for a numeric measure, the commonest pass-through in a real revenue model. **Reproduced** against `check_d9`: 10/10 `sql_number_aggregate_op` gives no finding; 10/10 `sql_int_aggregate_op` gives "10/10 formulas use sql_*_aggregate_op (100%)". The emitted detail string claims a coverage the regex does not have. Secondary: the `checks_perf.py:9` copy is dead — referenced by none of its sixteen checks — so the performance angle reports nothing about pass-throughs at all. | Derive the alternation from the `sql_*_aggregate_op` rows in `thoughtspot-formula-patterns.md` rather than hand-listing it twice; delete the dead copy | validator |
| 14.3 | 🟠 | `check_p18` fires only on `columns[].properties.aggregation == "COUNT_DISTINCT"` — the form this repo tells authors never to produce (schema warns twice, `tml_lint.py:187` actively fails it). Every converter emits the other form: `formula_common.py:154` maps to `unique count`, and four platform modules produce it. **Reproduced**: schema-mandated form gives 0 findings; schema-discouraged form gives 1. So on any model this repo generates the COUNT_DISTINCT cost check reports zero, and the only models that trip it are already caught louder by `ts tml lint`. | BL: scan `formulas[].expr` for `unique count` and `group_unique_count`, in addition to the aggregation property | backlog |
| 14.4 | 🟢 | `build_context` computes `scoped_guids` and `all_guids` from an identical expression and drives two batched `metadata/search` sweeps over it (batch 50 with `include_headers`, batch 15 with dependents) — `ceil(N/50) + ceil(N/15)` round trips where one pass may suffice. Recorded FYI only: whether the endpoint honours both flags in one request is unverified, and the differing batch sizes suggest the dependents response is deliberately kept small. The rest of the module batches correctly and token caching remains correct (no client constructed inside any loop, AST-verified). | No action pending an MCP spec read of `searchMetadata` | none |

### Angle 16 — dependency / supply-chain currency

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 16.1 | 🟠 | `agents/databricks/SETUP.md:18` lists the prerequisite as `pip install databricks-cli` (v0.18+) — the legacy Python CLI Databricks deprecated in favour of the unified `databricks` CLI (v0.200+). The same file's next step and `deploy.sh` use `databricks bundle deploy`, an Asset Bundles command that does not exist in v0.18 at all. A reader following the prerequisite literally installs a CLI that cannot run the very next step. | State the unified CLI (v0.200+) and drop the `databricks-cli` pip line | backlog |
| 16.2 | 🟡 | Four Databricks notebook/skill files `%pip install` requests and pyyaml with no version constraint. The CVE gate audits only the `tools/ts-cli` environment (core + `[snowflake,qlik]`), so this second dependency surface is never scanned and never floor-checked. Low — it resolves to latest at runtime, so it is unlikely to be *behind*; the gap is that nothing asserts a minimum or notices an advisory. | Small validator requiring these `%pip install` lines to carry the same floors as `pyproject.toml` | validator |

### Angle 18 — harness / framework currency

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 18.1 | 🔴 | `.claude/rules/repo-audit.md:265` documents the full cadence as "all angles, **incl. 15**" and the 2026-09-08 History row unparks angle 15 as "Full sweep only". `.claude/workflows/repo-audit.js` has no angle-15 finder — grep for `15` or `fidelity` returns nothing, and the `scope === 'full'` branch adds only the internal angles, the conversion auditor and the angle-17 review. A run invoked exactly as the rubric documents produces a report headed `scope=full` in which fidelity was never measured, and the report cannot tell the reader that. This is the rubric's own recorded lesson — "when an angle's enforcement names an agent rather than a validator, check that something actually invokes it" — which claimed angle 12 was the only other angle in that shape. | Either wire a finder (real work: live warehouse + cluster + ~100 disposable objects) **or** amend the Cadence row and the 2026-09-08 History row to say angle 15 is operator-run from `docs/reviews/2026-09-08-sv-patterns-roundtrip-fidelity.md`. The two files must stop disagreeing | backlog |
| 18.2 | 🟠 | `commands/alias.py:30` pins `_CLAUDE_MODEL = "claude-sonnet-4-20250514"`, used as the `model=` behind `ts alias translate --translator claude`. That id is listed under deprecated models; the guidance is to migrate to the current Sonnet. Canonical angle-18 shape, but now in **production code** rather than config, so `check_harness_routing.py` (reads `.claude/agents/*.md` only) and the prior sweep's "no dated model ids in `.claude/`" check both look past it. On retirement, every `--translator claude` run fails with a 404-class error. Companion pointer, unverified and owned by the Snowflake lens: `alias_translate.py:16` pins `_CORTEX_MODEL = "llama3.1-70b"` the same way. | Use the undated alias and expose `--model` so the next lineup change is a caller decision. Gate: fail any `claude-[a-z]+-\d.*-20\d{6}` literal outside tests | validator |
| 18.3 | 🟠 | `PLATFORMS` in the workflow lists four platforms; the repo ships **seven** mapping families (looker, powerbi, qlik, sisense, tableau, ts-databricks, ts-snowflake) and nine converters. Neither the rubric nor the workflow mentions looker, qlik, sisense or powerbi. Self-documenting consequence: the Looker mapping files and `qlik-app-ir.md` carry "(inaugural anchor; verify in next external sweep)" — an instruction addressed to a sweep that structurally cannot reach them — and `check_mapping_currency` will nudge them stale from ~2027-01 with no lens able to clear it. Angle 9 already discovers converters at run time so a new one is audited from its first commit; angle 13 kept the hand list, so the two halves of converter coverage have drifted apart. | Copy the forcing pattern that already exists: every `agents/shared/mappings/<family>/` must have a `PLATFORMS` entry or a documented exemption, gated the way `PLATFORM_DOMAINS` is | validator |
| 18.4 | 🟠 | The `SessionStart` hook runs `python3 tools/validate/check_audit_freshness.py --root . 2>/dev/null` with a trailing `|| true` — both paths relative, failure swallowed twice. **Reproduced**: run from a subdirectory it exits 2 with "can't open file" and, in the hook's wrapped form, prints **nothing** — indistinguishable from "no sweep is due". From the repo root the same command prints both nudges (external 27d overdue; full worth considering, 74 commits). The branch echo keeps working because git walks up, so the hook looks alive while its more important half is dead. This matters because finding 18.8's whole point was making the freshness protocol deterministic, and the external cadence has no other trigger. | Use `${CLAUDE_PROJECT_DIR}` for both the script path and `--root` (as `pre-commit.sh:366` already does with `$REPO_ROOT`) and drop the stderr suppression. One-rule gate: every hook command naming a repo path uses `${CLAUDE_PROJECT_DIR}` | validator |
| 18.5 | 🟠 | `model-routing.md` requires every review gate at `effort: high` to `max` and "never downgrade a gate". `.claude/agents/conversion-consistency-auditor.md` declares no `effort:`, and its call site in the workflow passes none — so angle 9's judgment half, the half found running nowhere at all last sweep and covered by no validator, runs at whatever the operator's session happens to be, while the angle-17 gate two lines below is pinned `max`. The asymmetry is the tell. The governing policy also notes effort can downgrade silently, so "the session default was strong" is never evidence. | One line in the agent frontmatter or at the call site. `check_harness_routing.py`'s "deliberately NOT asserted" reasoning covers *disagreement*, not *absence* — asserting an agent declares an effort at all is mechanical | validator |
| 18.6 | 🟠 | `.claude/agents/repo-publisher.md` ends with prose guardrails — never push to `main`, never force-push, never `git add -A`, never stage-sync before merge — and declares no `tools:`, no `disallowedTools:`, no `permissionMode:`, no `hooks:`. It inherits the full tool set, so every rule is enforced by the prompt alone. This is the repo's own finding-18.6 lesson ("a description that says read-only without a grant is a comment, not a control") applied to the two read-only agents but never to the agent that commits and pushes. Branch protection covers `main`; nothing covers a force-push to a `wip/*` branch the user has parallel sessions on, or a `git add -A` sweeping unrelated work into a publish commit. | Subagent frontmatter now supports `disallowedTools`, `permissionMode` and subagent-scoped `hooks` with an `if:` permission-rule filter — deny the exact commands the prose forbids, and note which guardrails remain advisory | backlog |
| 18.7 | 🟡 | `model-routing.md:1`'s currency anchor enumerates the model lineup, and the enumeration is already incomplete against the current roster. Cosmetic in effect — nothing depends on the list — but it is a model enumeration in the one file whose own requirement is "never hardcode a model name into a rule meant to outlive a release". The anchor is a staleness clock, not a rule, which is the defence. | Record the context without the roster, e.g. "validated against the current Claude Code + model lineup" | mapping-update |
| 18.8 | 🟡 | The whole documented cadence is expressed as `Workflow({name: "repo-audit", …})` and the script is checked in, but `.claude/settings.json` declares neither `enableWorkflows` nor `disableWorkflows` — so whether a contributor can run the repo's own documented audit falls back to their plan default. Same shape as finding 18.1 of the prior sweep, which was fixed in the **committed** file precisely because it is shared repo state. Marked low deliberately: the key and its scope are verified, but nobody is demonstrably blocked (this sweep ran), and forcing an unnecessary key through a gate would be over-reach. | A deliberate decision either way — declare it, or one line in the rubric saying availability is per-account | backlog |

### Angle 1 — dead files

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 1.1 | 🟢 | `tools/ts-cli/ts_cli/domo/` exists in the working tree containing only `__pycache__/` with six `.pyc` files; `git ls-files` filtered for domo returns nothing — the sources live only on the unmerged `fix/domo-round5-findings`. Gitignored, so this is local litter, not a repo defect. Worth knowing only because a bare package dir with live `.pyc` files can be importable under some interpreter configurations and makes `ls ts_cli/` suggest a domo converter exists on main. | `rm -rf tools/ts-cli/ts_cli/domo` on this machine. No validator warranted — gitignored local build output is out of scope for a repo gate | none |

### Angle 4 — tools quality

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 4.1 | 🔴 | `_git.py`'s docstring names two fail-open defects it exists to fix; the BL-218 migration closed the octal-path one everywhere and left the returncode one live in ~8 enumeration call sites (`check_patterns.py:171,251`, `check_file_size.py:70`, `check_module_health.py:122`, `check_skill_context_cost.py:88`, `check_yaml.py:90`, `check_slash_command_refs.py:121`, `check_skill_versions.py:119`, plus `check_open_items.py:127`). Worse, `check_patterns.py`'s own `_RAW_GIT_ENUM_RE` guard `continue`s on any line containing `-z`, so a raw `git ls-files -z` with no returncode check is **permanently exempt** from the rule meant to force it through `_git`. **Reproduced**: a scratch tree with a real Check-5 violation gives "No anti-patterns found." exit 0 when not a git repo; the same tree after `git init && git add -A` gives `FAIL … direct-api-call-in-skill`. On failure the tracked set becomes a single empty string, so Checks 5-8 scan nothing while the script prints its success line. The live trigger is not "not a repo" — it is `index.lock` contention, which `_git.py` names explicitly and which parallel sessions against this repo invite. | Add `check=True` to all enumeration call sites (or route through `_git`) **and** remove the `-z` exemption from the guard, so the rule can see the shape it was written for | validator |
| 4.2 | 🔴 | `io_helpers.py:76-82` builds `source ~/.zshenv && ts tml import …` and runs it via `bash -c`. Three defects: (1) the `source X && cmd` form hard-fails when X is absent, so `run_tml_import` returns a failure whose stderr tail is a *bash* error reported to the user as a ThoughtSpot import failure — and `~/.zshenv` is a convention, which `.claude/rules/security.md` itself contradicts by naming `~/.bashrc` and PowerShell `$PROFILE`; (2) this exact bug was already found and fixed in `run_smoke_tests.py:189`, whose comment carries the post-mortem and whose corrected form guards the file's existence — the fix never reached here, and three smoke tests still use the broken form; (3) `bash` does not exist on Windows, a declared-supported platform, so this raises an uncaught `FileNotFoundError`. No `timeout=` either. Blast radius: this is the import step of both `ts snowflake build-model` and `ts databricks build-model`. Nothing needs a subshell — the CLI is importing TML through its own CLI. | Invoke `["ts", …]` directly with no shell, as `tools/smoke-tests/_common.py:83` already does; add a timeout. Extend `check_patterns` Check 6 (which already bans this shape in SKILL.md) to ts_cli product Python | validator |
| 4.3 | 🟠 | `check_module_health` covers `tools/ts-cli/ts_cli` and `agents`; `check_file_size` covers only the former. Neither sees `tools/validate/` (20,001 lines across ~55 validators). The docstring justifies this as excluding "this validator's own tree" — a reason that covers one file, not the other 54. Measured with the gate's own tool: `check_patterns.py::main` **cc 97 (F)**, `check_tml.py::validate_model_tml` 57, `check_sv_yaml.py::validate_sv_yaml` 50, `check_sv_yaml.py::main` 42, against a CAP of 15 — and the worst *baselined* function in product code is 47, so the top validator is more than double it. Three validate/ files also exceed the 500-line soft warn. `check_patterns.py::main` being a ~200-line god-function holding Checks 1-8 plus both guards is why 4.1 sits inside it unnoticed. | Add `tools/validate` to `INCLUDE_ROOTS`, run `--update-baseline` once — the ratchet then prevents growth without demanding an immediate refactor | validator |
| 4.4 | 🟠 | AST sweep of `ts_cli/**/*.py` (tests and binary opens excluded): **46 of 160** text-I/O call sites pass `encoding=`, 114 do not — the same module often does both, the signature of an unenforced convention rather than a decision. `requires-python = ">=3.10,<3.15"` so PEP 686 never applies across the supported range; on Windows these resolve to the ANSI codepage. The two sites where non-ASCII content is the whole point: `commands/alias.py:267` reads the localization translation CSV with a bare `read_text()` (the alias skill exists to localize, so CJK/accented payloads are expected), and `migrate/mapping.py:13,24` write and re-read the column-mapping CSV that is the **state ledger for a destructive Org cutover** — a lossy round-trip there corrupts what the rollback depends on. No `PYTHONUTF8` setting anywhere and no prior backlog entry, so this has not been consciously accepted. | Validator requiring `encoding=` on text-mode I/O in `ts_cli`; fix the alias and migrate ledger sites first | validator |
| 4.5 | 🟡 | Structural-equivalence sweep found six parser helpers duplicated verbatim between `sv_sql.py` and `databricks/mv_sql.py` — `_call_args`, `_call_count`, `_call_extract`, `_call_pass_through`, `_call_date_trunc`, `_expr_units` — differing only in an error-message string. Not for want of a shared home: both already import from `formula_common`, and `sv_sql.py:285` carries "Canonical map now lives in formula_common so both engines share one copy". The `_Cursor` layer around those imports was simply never lifted with them. | Overlaps BL-217 part 2 and angle 9's implementation-drift remit — likely folds in rather than needing its own id | backlog |
| 4.6 | 🟢 | `vulture --min-confidence 80` returned three hits; one is a documented deliberate no-op. The other two are real: `run_smoke_tests.py:29` `import signal` is unused, left behind by the process-group reap that file's own comment records being written and removed; and `commands/model.py:276` `_select_formulas` declares `all_flag` and never reads it, so `ts model promote-formula --formula "X" --all` silently promotes only X. Unlike the sibling `--all`, this flag's help text does not say it is a no-op, so it reads as functional. | Delete the import; either honour `--all` (names plus all gives all) or reject the combination | none |

### Angle 5 — ts-cli gaps

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 5.1 | 🔴 | `ts-object-model-aggregates/SKILL.md:772-782` says column security "cannot be reliably auto-detected today" — citing the dependency-manager open item as evidence — and prompts the operator for Y/N, "if unsure, check the UI". Both halves are now readable: `ts security column-rules get <table>` returns one row per (table, column) with `group_names`, and `ts share status <guid> --columns` covers column-level sharing (live-verified 2026-07-26). The command landed in PR #347 (v0.109.0); this SKILL.md has not been touched since PR #338 — the claim is simply older than the command. Not cosmetic: the step exists to stop an aggregate being created without equivalent column security, and a guessed "N" ships an unsecured aggregate that 26.6 aggregate-aware routing then sends queries to. The identical stale-verdict class was already found and fixed in `ts-object-answer-promote` last sweep; this instance was missed. | Replace the prompt with the two commands. Prerequisite: 5.2 | backlog |
| 5.2 | 🟠 | `ts-dependency-manager/references/open-items.md` #9 calls CSR **retrieval** the open question, claiming it works only via the UI zip or the git-commit workflow. `ts security column-rules export` does exactly this — `export_associated: true` plus `export_options: {export_column_security_rules: True}` — and its docstring states "CSR round-trips through TML as a sibling document". Two downstream consequences: `dependency-types.md:31` still reads "retrieval mechanism unverified", in violation of that file's own instruction to update both sides together; and `ts-audit` open-item #7 says the flag still needs wiring, when it is wired — just on `ts security column-rules export` rather than `ts tml export`. | Close #9 and row 9; decide separately whether the flag is *also* wanted on `ts tml export`. This is the citation 5.1 rests on | backlog |
| 5.3 | 🟠 | `dependency-types.md:32` (row 10) calls column-alias retrieval "No on this build … unverified (open-item #10)" although `ts alias export` retrieves it via `export_with_column_aliases: true` behind a full shipped `export/translate/build/import` pipeline. Separately, **`open-item #10` does not exist** — the file's headings are 2,3,4,9,11,13,14,15,16,18,20-25 — and neither does `#12`, cited by row 1. A reader chasing the justification for "unverified" finds nothing at all. | Update row 10 to Implementable; fix or remove both dangling citations. Recurrence: 5.6 | backlog |
| 5.4 | 🟠 | Grepping for `model_instructions` in `agents/shared/schemas/thoughtspot-model-tml.md` returns **nothing** — the field is absent from the canonical Model TML schema while being load-bearing in `audit/checks_ai.py`, `report/tml_probes.py`, two test files and the from-snowflake-sv skill. `.claude/rules/api-research.md` names these schema files as authoritative for TML shape, explicitly above the MCP. A field the CLI reads in two modules but the authoritative schema omits is the exact drift that rule prevents — and it is the direct cause of 13.2's open item still reading "not in thoughtspot-model-tml.md". | Document `model.model_instructions.data_model_instructions` in the schema; unblocks 13.2's premise | mapping-update |
| 5.5 | 🟡 | Both inline-`requests` gates scan Markdown only: `check_no_inline_requests.py` enumerates `base.rglob("*.md")`, and `check_patterns` Check 5 runs over files named exactly `SKILL.md`. CLI skills do ship executable Python — `ts-object-model-erd/build_erd.py`, invoked directly by its SKILL.md, and `ts-dependency-manager/references/build_coverage.py`. Both are clean today (no `requests`/`urllib`/`httpx`), so this is a scope hole, not a live violation — but the anti-pattern would land in a shipped skill file with both gates reporting PASS, and the gate's own docstring frames its value as covering "the one place code that will actually be executed lives", which a skill-local `.py` is more literally than a fenced block. | Widen both globs to skill-local `.py` | validator |
| 5.6 | 🟡 | Inbound `open-item #N` citations are never resolved. `check_open_items.py` flags UNTESTED/placeholder findings and now checks `#N` id novelty across branches, but neither pass resolves a reference — so 5.3's dangling `#10` and `#12` are silent. This matters for angle 5 specifically because the citation is what a reader follows to decide whether a CLI gap is real, and two findings this sweep rest on citations that are stale or missing. | Every `open-item #N` reference inside a skill's own `references/` must match a heading in that skill's `open-items.md` | validator |
| 5.7 | 🟡 | `ts tables create` exposes only `--retries`/`--retry-delay`/`--profile` and reads a JSON **array** from stdin — no `--file`, no single-object tolerance. The aggregates pipeline writes `table_spec.json` as a single object, so the skill bridges two of its own CLI commands with a `python3 -c` shim — the shape `.claude/rules/ts-cli.md` asks skills not to accumulate. | Add `--file`, or accept a bare object and wrap it; the shim then disappears. Cleanup, not a defect | backlog |

### Angle 6 — testing-framework value

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 6.1 | 🔴 | *(merged: angle 6 + angle 14)* **26 of the 50 checks** in the `ALL_CHECKS` registries `audit/__init__.py:322` iterates are never executed by any test — perf 10, data 7, human 5, security 4 untested (ai is clean). Coverage: `checks_perf` 30%, `checks_data` 40%, `checks_human` 48%, `checks_security` 52%. What stands in for coverage is the tell: every test file imports all checks by name and closes with `assert len(ALL_CHECKS) == N`, so the file reads as complete and adding a check forces an author to bump a number that ratifies registration only. `test_checks_perf.py:2` imports `check_p6` and never calls it. The risk is silent: a check whose key convention is wrong returns an empty list and the audit reports the environment clean — which is exactly 14.1, 14.2 and 14.3. The audit fixtures do not close it (they synthesise Finding objects directly, never exercising a check against TML-shaped input), and the only end-to-end net, `smoke_ts_audit.py`, is Live tier and runs in no CI job. `tools/ts-cli/CLAUDE.md:210` already says to add unit tests per check — unenforced prose. | **`check_audit_check_coverage.py`**: every function in each module's `ALL_CHECKS` must be called by name in `tools/ts-cli/tests/test_checks_*.py`, with at least one fixture making it return a non-empty list. Replaces the count assertions with something a number cannot satisfy. Highest-leverage item in the sweep | validator |
| 6.2 | 🔴 | `ts_cli/powerbi/parsing.py` is 300 statements, 266 missed, **11%**. Its test file is 1.8 KB and exercises two private helpers added reactively in a PR review. Every front-door entry point — `parse_table_tmdl`, `parse_relationships_tmdl`, `parse_semantic_model`, `parse_report_pbir`, `parse_report_legacy`, `parse_inventory` — has zero test callers repo-wide. An outlier, not the house standard: sisense 69%, qlik 85%, and PowerBI's own siblings 86-88%. The second net is absent too — `ts-convert-from-powerbi` is on the smoke-test ALLOWLIST — so the entire input-parsing stage of a released converter (TMDL indentation/quoting, relationship properties, PBIR visual projection) is covered by nothing. | Dated BL, not a validator — needs fixtures for a TMDL table block, a relationships block and a PBIR report dir. **BL-076 is the wrong home** (see 6.5) | backlog |
| 6.3 | 🟠 | BL-077 is marked DONE on "all 18 validators now have known-bad fixture self-tests". There are now ~44 validators, and three wired as **blocking gates in both pre-commit and CI** have no dedicated test and no entry in `test_known_bad_fixtures.py`: `check_harness_routing.py`, `generate_parity.py --check`, `suggest_dependency_types.py --check`. The first is the sharp one — its own docstring records that its first rewrite shipped a comment-stripping evasion (`model: haiku  # x` slipping past the Haiku check), "fixture-verified" and caught by refute review, and **the fixture that caught it was not kept**. That is precisely the regression class the file exists for. Structural cause: BL-077 was a one-time backfill closed as DONE with nothing keeping the property true. | Add the three fixtures, plus a meta-check that every validator invoked by `pre-commit.sh` or `validate.yml` appears in that file or has its own test | validator |
| 6.4 | 🟠 | Zero `CliRunner` invocations against `ts snowflake` (vs 27 for tableau, 17 for databricks); `commands/snowflake.py` is 319 statements at **23%**, with essentially every subcommand body missed — diff, lint-ddl, exec, parse-sv, translate-formulas, the import helpers, introspect, build-sv. The engines underneath are well tested, so the untested layer is specifically flags-to-engine: argument parsing, file resolution, exit codes, and the stdout/stderr split `.claude/rules/ts-cli.md` makes a hard convention. That convention is verified for Databricks by `test_stdout_is_pure_json`; there is no Snowflake equivalent and no validator covers it. The asymmetry is the argument — the two groups are structurally parallel and the Snowflake pair is the flagship (the only skills with CoCo mirrors), whose only end-to-end net is Live tier needing two profiles. | `test_databricks_build_mv_cmd.py` is the ready-made template — mirror it for `ts snowflake` | backlog |
| 6.5 | 🟠 | `check_allowlist_bl_references()` enforces exemption accountability with `_BL_REF_RE = re.compile(r"BL-\d+")` — it verifies a BL-shaped string is present, never that the item exists, is OPEN, or covers the skill. It is already being leaned on: `ts-convert-from-powerbi` is parked on **BL-076**, which is titled "Smoke-test backfills: ts-object-answer-promote + ts-convert-from-tableau" and whose body never mentions Power BI. Shipping those two smokes closes BL-076 as DONE and the Power BI exemption then cites a closed item while the gate still reports PASS. Compounds 6.2 — the skill is exempt from the smoke requirement *and* 11% covered, and the one honesty mechanism is satisfied by someone else's id. BL-076's target is 2026-09-30, eight days out. | Resolve the cited id in `docs/backlog.md`, require status OPEN, and require the skill name to appear in that item's section — the same discipline `check_backlog_integrity.py` applies to renumbering. File a Power BI-specific BL for 6.2 | validator |

### Angle 7 — PR-validation effectiveness

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 7.1 | 🟠 | *(merged: angle 7 + angle 6)* The "Run credential-free smoke suites" CI step hardcodes five script names. Run with no profile configured on this checkout, at least four more exit 0 — `smoke_ts_convert_from_qlik`, `smoke_ts_load_source_data`, `smoke_ts_object_calendar_builder`, `smoke_ts_object_model_erd` — and the README's Pure tier lists eight (the fifth, the to-databricks-mv suite, fails only on this machine's Python 3.9 and would run in CI). They are CI-runnable and run in no CI job. The only other runner, `pre-push.sh`, derives its list purely from touched skill paths — so a PR editing only the engine under `tools/ts-cli/ts_cli/` runs none of them anywhere, which is the exact gap the CI step's own comment says it exists to close, closed only for the five it happened to name. `check_smoke_tests.py` checks skill-to-file correspondence only, never the CI list. Currently benign — all the affected engines have pytest suites CI does run — so the finding is the drift shape, not present damage. | Derive the CI list from the README's Pure tier (already validated for completeness by `_find_undocumented_smoke_tests`), or assert the two lists are equal | validator |
| 7.2 | 🟡 | `pre-commit.sh` ends with an interactive main-branch skill-inventory confirmation gated on `CURRENT_BRANCH = main`. CLAUDE.md and the branching rule forbid committing to main at all (branch protection with `enforce_admins`), so every real skill addition arrives on a `feat/` or `wip/` branch and never triggers it; there is no CI counterpart, and a non-TTY commit aborts rather than checking anything. It reads as coverage in `docs/quality-gates.md` while enforcing nothing on the only path changes take. | Move the check to the branch paths that actually carry skill additions, or retire it and correct the catalog | backlog |

### Angle 9 — conversion consistency (semantic invariants + implementation drift)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 9.1 | 🔴 | `ts-convert-from-looker` instructs `is_hidden: true` on every joined dimension PK — `references/step-6-model-joins.md:68` in the Model TML template, repeated at SKILL.md:404, step-6:64 and :132, step-5:264, coverage-matrix:36. **I11 says never set it during conversion**, and `thoughtspot-model-tml.md:243` — a schema this skill itself cites — repeats the prohibition. Concrete wrong output: every joined dimension's PK ships hidden, producing the locked-visualization and join-path failure I11 names. The skill has already accreted a workaround for a downstream symptom (table-prefixed names, because ThoughtSpot rejects duplicate display names even among hidden columns) rather than dropping the construct. Nothing enforces I11 anywhere. | Remove the construct; add an `is_hidden: true` check to `tml_lint.py`'s model pass under the CANONICAL-RULE-SET marker, which every converter already invokes | validator |
| 9.2 | 🔴 | `ts-convert-from-snowflake-sv/SKILL.md:759-796` is the untranslatable-classification step and simply says "ask whether to proceed without them" — no instruction to open the translation reference first. Its **CoCo mirror carries the full `MANDATORY (I7)` block**, added in a mirror version whose changelog claims it mirrors CLI v1.5.0, while the CLI v1.5.0 row covers only N1 and I5. The invariants doc names this very skill as an establishing source for I7. Wrong output: SV metrics with documented ThoughtSpot equivalents (NON ADDITIVE BY, OVER (PARTITION BY …), PARTITION BY EXCLUDING) are dropped on syntax recognition alone. | Add the gate to the CLI primary; reconcile the mirror changelog claim | backlog |
| 9.3 | 🔴 | The I7 MANDATORY gate is present in 3 of 9 converters (from-tableau, to-snowflake-sv, both CoCo mirrors) and **absent from 6** that each have an untranslatable/skipped classification step: from-snowflake-sv, from-databricks-mv, from-powerbi, from-qlik, from-sisense, to-databricks-mv. from-looker states the rule and checklists it without the marker — a partial pass. | Mechanically checkable: assert the marker appears within N lines of an untranslatable/skipped classification step in every `ts-convert-*` SKILL.md | validator |
| 9.4 | 🔴 | `sisense-formula-translation.md:11` says a measure's JAQL `agg` becomes a TML `aggregation:` keyword and :19 maps `count` to `COUNT_DISTINCT` — mirrored in the coverage matrix and in `sisense/functions.py:29`. **I5's live-proven failure**: `aggregation: COUNT_DISTINCT` on a column_id-backed entry silently flips column_type MEASURE to ATTRIBUTE with no import error. Sisense `count` IS the platform's default measure, so this is the common path. Every other mapping family routes distinct-count to `unique count(...)` — six of them. Compounding: the code path is dead (`translate_agg` has no caller outside tests; the live emitter never returns COUNT_DISTINCT), so the doc specifies behaviour the emitter lacks while a doc-executing reader follows the doc. | Fix both sides. Validator: no mapping doc may route a distinct-count row to a TML `aggregation:` value | validator |
| 9.5 | 🔴 | The CoCo from-snowflake-sv mirror contains **no I14 rule** — no role-play, alias-node or duplicate-join-pair guidance anywhere. The CLI primary added Step 7.5 plus a reference file at v1.20.0 together with lint invariant I14; CoCo has no `ts` CLI and executes the doc, so the rule must be prose there. Wrong output: an SV joining one fact to DIM_TIME on order date and ship date emits two joins on the same ordered pair and **the Model will not load** (observed at 21 joins across 7 pairs on a real customer SV). Second-order: `agents/SYNC-DEBT.md:10` records the CLI side at v1.19.5 while the skill is at v1.22.2, so three minor versions are outside the ledger — and `check_mirror_sync.py:56-66` collects only the path from each ledger row, never comparing the recorded version against the actual one, so **any SYNC-DEBT row acknowledges its mirror permanently**. | Add I14 to the mirror; make `check_mirror_sync` parse the ledger's recorded CLI version and fail when it is behind | validator |
| 9.6 | 🔴 | `model_builder.filter_unresolvable_formulas` computes the drop set to a fixpoint via `_cascade_drop_dependents`, whose docstring names the transitive case explicitly. Its only caller is `commands/tableau.py:613`. Three sibling skills instruct the executor to do it by hand, one hop deep, with the identical sentence — from-qlik:92, from-powerbi:86, from-sisense:78. Wrong output: with C referencing B referencing rejected A, B is dropped and C survives carrying a dangling `[formula_B]` — an I13 violation and a hard rejection (14516), or a dangling `formula_id` and an I1 silent drop. This is BL-217 / the prior sweep's 11.4, still open. | Expose the retry loop as a shared command or helper the three skills invoke | backlog |
| 9.7 | 🔴 | `formula_common.py:417` declares `quote: str = "'"`. Both live callers override it to a double quote, and `qlik/functions.py:227` carries the reason verbatim: taking the default made Qlik alone emit the single-quoted outer template while its siblings and every example in the patterns schema use the double-quoted one — "only the single-quoted form is unverified against the parser". So the default is a form nothing emits and nothing has verified, reachable by omitting one keyword argument, in the helper BL-171 created to stop emitting forms ThoughtSpot rejects. The prior sweep's 17.2 was fixed at the call sites, not at the default. | Change the default to the double-quoted form, or make the parameter required | backlog |
| 9.8 | 🟠 | *(merged with the timeintel instance)* Grepping `DONT_INDEX` returns exactly two emitters (`sv_build_model.py`, `databricks/mv_build_model.py`). The shared `model_builder._build_model_columns`, which Tableau and Qlik route through, builds formula columns with `column_type` and `aggregation` only; PowerBI and Sisense define their own emitters and also omit it — including `powerbi/timeintel.py:91-95`, which mints new measures and whose comment "shape mirrors `_add_formula` exactly" is accurate and is the point. Meanwhile from-tableau's step-5 states "I3 — add `index_type: DONT_INDEX` on **every** columns[] entry that has a formula_id" and from-looker checklists it. I3 is advisory (search behaviour, not import), so WARN — the finding is the doc/code contradiction: the skill promises the property and the codified path it mandates does not deliver it. | Extend `check_converter_parity` with an I3 shape rule (its docstring already names I3 as deliberately-not-yet-enforced, BL-217 remainder) | validator |
| 9.9 | 🟠 | PT2 hole. `qlik-thoughtspot-formula-translation.md:183` (S04 `Capitalize`) has mapped value "No direct equivalent" and status ok, but its **Notes** cell instructs "combine `upper()` on first char and `lower()` on the rest" — both strikethrough rows in the patterns schema, live-disproved 2026-06-13. An executor following the note hand-authors a formula rejected at import with 14516. `check_formula_catalog` inspects mapped-value cells, not Notes — precisely the parser blind spot PT2 exists to cover. | Extend `check_formula_catalog` to scan every cell of a mapping row for a disproved function name, not just the target column | validator |
| 9.10 | 🟠 | `databricks/mv_tml.py:90` defines `validate_tml_invariants`, whose docstring says it holds "the two hard invariants `lint_tml` does NOT check" — `db_column_name` presence and I6 (connection block carries `name:` only, never `fqn:`). Neither is platform-specific, yet it lives in a platform package with two callers (the Databricks command and the Genie skill). I6 is not in `tml_lint.py`'s CANONICAL-RULE-SET, so **no other converter has any runtime I6 or db_column_name gate on the TML it generates** — only `check_tml.py`, which runs over committed repo files, not conversion output. Latent today because every emitter hardcodes the name-only connection block, but it violates `formula_common`'s stated rule: "Never fork these into a platform module; import them." | Relocate to a shared module and call it from every converter's lint step | backlog |
| 9.11 | 🟠 | `promote_duplicate_column_ids` (the I8 auto-fix) is imported by 2 of 7 emitters, and `check_converter_parity`'s requirement B asserts only `resolve_name_collisions` and `fix_double_aggregation` — so a converter emitting two metrics over one physical column has no auto-fix and relies on lint's I8 finding to fail the import. Caught rather than silent, but at import instead of at build: the difference between a clean conversion and a manual retry loop. | Add it to `FORMULA_COLUMN_HELPERS` with the same shape-conditional trigger | validator |
| 9.12 | 🟠 | One fix, four live mechanisms: `formula_common.wrap_passthrough_calls` (qlik, powerbi), `tableau/functions.py:156-229` hand-rolled `_ARG_HANDLERS` lambdas, `sv_sql.py:266` `_PASS_THROUGH_HINT`, `databricks/mv_sql.py:271` `_PASS_THROUGH_HINT`. Tracked as BL-217 and recorded in `HELPER_IMPLEMENTATION_NOTES`; still open. The consequence is the one BL-217 names — a new converter has no single thing to copy, and PR #440 copied the one skeleton with no pass-through map at all, shipping `upper([Region])` as "Migrated". Tableau's is genuinely blocked (its lambdas do arity-dependent compositions the helper cannot express); `sv_sql` and `mv_sql` are straightforward and near-identical to each other. | Consolidate the two SQL-side copies first; document Tableau's as a reasoned divergence | backlog |
| 9.13 | 🟠 | `model_builder._build_model_parameters` is the single place I10 is applied — it defaults `data_type` to CHAR and forces CHAR whenever `list_config` is present. PowerBI defines its own `build_model_tml` and never reaches it: `powerbi/build_model.py:292-294` assigns parameters straight from `overrides.json` with no normalisation. Wrong output: an overrides file with `data_type: VARCHAR` and bare-string `list_choice` entries reaches Model TML verbatim and the import fails with "Invalid YAML/JSON syntax" — I10's stated failure, for both halves. `ts tml lint` does not cover I10. PowerBI is also the one converter that emits a parameter unprompted (`timeintel.py:71-75`), so the path is live. | Route PowerBI through the shared parameter builder, or apply the same normalisation | backlog |
| 9.14 | 🟠 | `ts-convert-from-looker/SKILL.md:25` cites the invariants doc as "Hard rules — I1-I8" and :413-421 presents a Step 6e checklist headed "Run through all 8 invariants". The doc declares I1-I15, N1, PT1, PT2. The omitted ten include **I11, which this same skill actively violates** (9.1) and I14, which is live for Looker because its models routinely join several dimension tables. Identical staleness class to the auditor's own former hardcoded "I1-I7, N1": a checklist that reads as exhaustive while covering half the surface. | Assert any enumerated invariant range in a converter SKILL.md matches the headings the schema currently declares — the mechanism `check_lint_invariant_list.py` already applies to the CANONICAL-RULE-SET marker | validator |
| 9.15 | 🟡 | PT1 requires every `sql_*_aggregate_op` to carry a flag-for-review marker. Missing on eight rows: from-tableau coverage-matrix:110-112 (partitioned RANK, RANK_DENSE/MODIFIED, RANK_UNIQUE), :118 (SIZE unpartitioned), :119 (LISTAGG), step-5:436, and `tableau-formula-translation.md:853,858`. Sibling rows in the same files carry it correctly, so this is inconsistency within one converter rather than a policy gap. | Mechanically checkable: any line containing `sql_\w*_aggregate_op` in a mapping doc or converter reference without the marker on the same row | validator |
| 9.16 | 🟡 | from-powerbi, from-qlik and from-sisense contain **zero** invariant ids in SKILL.md (one incidental mention in qlik's coverage matrix) and none links the invariants doc, while the other four converters all cite it. All three invoke `ts tml lint`, so the mechanically-gated set is covered; the uncovered set they carry no guidance on is I3, I6, I7, I9, I10, I11, N1, PT1, PT2. Root cause behind several findings above rather than an independent defect. | Trivially checkable: every `ts-convert-from-*` SKILL.md must link the invariants doc | validator |
| 9.17 | 🟢 | The two Genie-runtime converters were discovered by the run-time glob and audited, despite sitting in the fourth runtime that `runtime-coverage.md` deliberately excludes from the mirror tooling. `from-databricks-mv` calls `validate_tml_invariants` plus `lint_tml`, inheriting the full mechanical set and being the only non-CLI caller of the I6 check in 9.10. `to-databricks-mv:149` lists untranslatable formulas with no MANDATORY I7 gate, matching its CLI counterpart's gap. | No action beyond 9.3; recorded so the exclusion is not read as "unaudited" | none |
| 9.18 | 🟢 | False-positive floor recorded ahead of any I1/I4 doc-level validator. A YAML-parsing scan flagged eight "unpaired formula" sites and one I4 "join with no matching model_tables entry" — every one is a single-fence fragment whose paired block sits in an adjacent fence or in the surrounding prose. An I1/I4 doc validator must fire only on fences containing both keys. Real violations from the same scan: **zero** for I2, I9, I12, I13 and I15 across all converter skills, references and mapping docs — I15 fully clean, confirming BL-232 closed all five sites. | No action; scoping input for the validator in Codification below | none |

### Angle 11 — codification

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 11.1 | 🟠 | `ts-object-model-alias` drives a fully codified export/translate/build/import pipeline and then stops codifying at Step 12: "build a comparison table from the new `existing_aliases` against the translations just applied", with a per-row OK/MISMATCH column and a closing "{n} of {n} confirmed". Both sides are JSON envelopes the CLI already emits, and the comparison is a set-diff on the `(column, locale, org, group)` key `--merge` is documented to match on. Step 11 states `build` warns above 20 MB and errors above the 25 MB platform limit — so the input to this hand-diff is explicitly sized in tens of thousands of entries. An LLM cannot compare that exhaustively, and the output format invites reporting `n of n` from a sample. No verify exists (`ts alias --help` lists build/export/import/translate; `import` offers only `--dry-run`). Demonstrably codifiable here: `ts tenancy verify` does exactly this and is the only one of five Org-family pipelines that codified its read-back. | `ts alias verify` (or `--verify` on import), exiting 1 on mismatch | backlog |
| 11.2 | 🟠 | `ts-object-answer-promote` Step 12 writes the merged YAML to a temp file, **then ignores it** and builds a JSON-array payload to pipe into a bare `ts tml import` via `bash -c`. `--help` documents the replacement as mode 1 and gives it as the first example: `ts tml import --file model.tml`. This is the 2026-07 migration `ts-convert-from-looker/SKILL.md:889` records finishing — applied to the convert family and not here. The guard written for this exact pattern cannot see it twice over: `check_skill_cli_usage.py` globs `agents/cli/ts-convert-*/SKILL.md`, and its `PAYLOAD_ASSEMBLY_RE` requires a literal `open(`, so a variable payload slips it. Both fixes are one line. Residual shapes for the same pass: from-snowflake-sv:558 and :984, from-databricks-mv:645 — bare `ts tml import` with no input flag and no statement of how stdin is built. | Migrate the call to `--file`; widen the glob and relax the regex | validator |
| 11.3 | 🟠 | Three `agents/shared/**` files are cited by **no** SKILL.md and no skill `references/` file: `references/profile-select-and-authenticate.md`, and two Tableau worked examples. The first is load-bearing — it is the canonical flow the prior sweep's 11.2 remedy depends on, and 11.2 is fully open: 14 CLI skills still instruct a hand-read of `~/.claude/thoughtspot-profiles.json` verbatim. Its sibling `connection-select.md` has 2 adopters. `check_orphan_references.py` cannot report any of this for two independent reasons, and fixing only the first would still report PASS: it iterates `ALL_RUNTIMES`, which does not include `agents/shared`; and its "cited" test is the substring `references/<name>.md` in **any** repo file, so the CoCo stage-copy list and the shared directory map — both inventories, not consumers, and the stage sync is dormant — make all three read as cited. | Widen the scan to `agents/shared`, **and** restrict the citer to a `SKILL.md` or a skill `references/*.md`. Then finish 11.2's adoption | validator |
| 11.4 | 🟠 | `ts-object-model-erd/SKILL.md:217` is the skill's only invocation of its own deterministic builder and uses a repo-relative path: `python3 agents/cli/ts-object-model-erd/build_erd.py …`. Per the symlink contract the skill is consumed from `~/.claude/skills/…` (and the Cortex path), so this resolves only when cwd happens to be the repo root — everywhere else it is a bare `[Errno 2]`. Nothing in Prerequisites mentions a checkout, and `--out` writes relative to the same unstated cwd. The repo already knows the fix: `ts-object-calendar-builder` spells out the `{skill_dir}` convention with both runtime examples. Scanned every code block in every SKILL.md and `references/*.md` across all runtimes: this is the **only** hit, so the gate has a green baseline. | Adopt the `{skill_dir}` convention here; add the one-rule scan | validator |
| 11.5 | 🟡 | `ts-publish-orgs/SKILL.md:3` — the frontmatter description, i.e. the skill's discovery surface — advertises "the `ts publish export/resolve/apply/verify/rollback` pipeline", repeated at :12. There is no `ts publish verify`; the registered set is apply/export/push/resolve/rollback/run/status/unpush. The stage was specified in the design doc and `status` shipped instead. Step 11 correctly runs `ts publish status`, so nothing breaks at runtime — a user acting on the description just gets a click error. Nothing can catch it: `check_skill_flag_usage.py` states in its docstring that it verifies flags and never command names, and this is prose and frontmatter, outside its extraction anyway. Green baseline: scanning all `agents/cli/**/*.md` prose backticks returns only this plus deliberate forward-references and two known deprecated mentions. | Correct the description; add a command-name check scoped to frontmatter and prose | validator |
| 11.6 | 🟡 | **Carry-forward, re-verified at `54c3aaf` so synthesis does not read silence as closure.** Prior 11.5: `ts tml export-corpus` still does not exist, so ~85 inline lines in model-coach are reproduced by the executor every run. Prior 11.6: `check_patterns` Check 8 still scans only SKILL.md, `ts-migrate-orgs/references/running-a-migration.md:304-336` still carries six `from ts_cli…` imports across five modules, and the dated `_TS_CLI_IMPORT_ALLOWLIST` entry is still live with its removal condition unmet. Prior 11.4: no shared cascade+retry helper landed (see 9.6). **Closed since:** prior 11.1 (all three converters now pass `--connection`; ts-audit records a reasoned divergence) and the skill half of prior 17.3 (qlik and sisense now lint the liveboard before import). | Keep the three open items dated; no new id needed | backlog |

### Angle 17 — change correctness (delta backstop)

| # | Sev | Finding | Outcome | Bucket |
|---|---|---|---|---|
| 17.1 | 🔴 | PR #484/#519 split `_extract_joins` into `tableau/joins.py` and changed how the `(left_table, right_table)` pair is resolved: `_join_sides:113` now returns the tables named by the **first qualified comparison in the clause**, falling back to child order only when no operand carries a qualifier. Tableau fixes join direction by **child order** — the first `<relation type='table'>` is the preserved left side — and the module's own docstring asserts the clause may name its operands the other way round. When it does, the whole join is emitted reversed. **Probed against the shipped code**: a `join="left"` relation with children orders, returns and a clause written `[returns].[OrderRef] = [orders].[OrderKey]` returns `left_table='returns'` with no warning; the same clause in child order returns `left_table='orders'`. It propagates through `build_model_tml` into the emitted model as a join on `returns` with `orders`, LEFT, MANY_TO_ONE — Tableau meant all `orders` rows preserved with `orders` on the one side; **both row preservation and cardinality attribution invert**, and it imports and lints clean. The fix is contained: `_clause_join_keys` already re-orients each key against whatever pair it is given, so child order can stay authoritative for orientation on a flat two-table join without reintroducing the mis-pairing #484 fixed — clause qualifiers are genuinely needed only for the nested three-table case. *(Separate and pre-existing: the emitted `type` is `"LEFT"`, not ThoughtSpot's `LEFT_OUTER`.)* | BL + fix PR: restore child order for flat joins, keep qualifier resolution for nested ones, and add a reversed-clause fixture | backlog |
| 17.2 | 🟠 | `item_novelty_violations` (PR #522) applies `(head_ids - merge_base_ids) & base_ids` and runs in exactly one place — the PR-only CI step, with `--base origin/$GITHUB_BASE_REF`; no local hook passes `--base`. On a `pull_request` event the checkout is the merge ref, which already merges the base in, so `merge_base` equals `origin/main`, `merge_base_ids == base_ids`, and the intersection is empty **by construction**. The rule is vacuous every time it runs. **Verified with the real validator against a fixture**: two branches each adding a different `## #4` auto-merge into different hunks; on the merge ref the validator prints "PASS … all items resolved" and exits 0 with two `## #4` sections in the tree, while the same command against the branch head exits 1 naming `#4`. No safety net here specifically — `check_backlog_integrity`'s Rule 4 is vacuous the same way, but its Rule 1 catches the merged state, and open-items has no equivalent (the within-file duplicate deliberately cannot be made strict, and `parse_open_items` dedups by number, so the second item silently disappears from the generated index). | Point the novelty check at the PR **head** sha rather than the merge ref, or add a within-file duplicate rule scoped to items the diff introduces | validator |
| 17.3 | 🟡 | `_extract_joins:256` appends only when join keys were found. When the clause list is empty but the child-order fallback resolves both tables, the clause loop never runs, the key list stays empty, and the relation is skipped with **nothing appended to `warnings`** — probed: returns two empty lists. This contradicts the contract the same delta introduced: the one-table-child/no-clause shape IS warned about and has a test; only the two-table variant falls through silently — the exact silent-loss class `join_warnings` was added to close. Low because reachability from real Tableau output is not established (Tableau normally writes a clause for every physical join). | Emit a warning on the two-table no-clause path; fold into 17.1's PR | backlog |
| 17.4 | 🟡 | `check_version_sync.novelty_violations:148` opens with a guard returning "refusing to pass rather than skip silently" when either base file is `None`. `main()` supplies both from `_novelty.read_at`, which returns an empty string on failure, never `None` — the branch is dead on the only path that calls it. Failure scenario: a partial/blobless clone where the commit resolves but the blob cannot be fetched, so `read_at` returns empty, the guard does not fire, the parsers return nothing, every comparison is False, and the run prints PASS — the false green the docstring says it refuses. | Have `read_at` distinguish "path missing at that rev" from "could not read the rev", or map the empty string to `None` in `main()` | backlog |

## Codification opportunities (manual to automated)

The rubric's management goal is migrating angles from the manual column into the automated one.
This sweep is unusually rich in validator-ready items because so many findings are *gates that
cannot fire* — and a gate that cannot fire is, by definition, mechanically detectable.

**Build first — each closes multiple findings:**

1. **`check_audit_check_coverage.py`** (closes 6.1; prevents 14.1, 14.2, 14.3 recurring) — every
   function in each `ALL_CHECKS` registry must be called by name in `test_checks_*.py` with at
   least one fixture producing a non-empty result. Replaces five `len(ALL_CHECKS) == N` assertions
   that an author satisfies by bumping a number. **Highest-leverage item in the sweep**: it is the
   mechanism that let three broken checks ship.
2. **Git-enumeration returncode enforcement** (closes 4.1) — add `check=True` at the ~8 call sites
   or route them through `_git`, **and** remove the `-z` exemption from `_RAW_GIT_ENUM_RE` so the
   guard can see the shape it exists for. Without the second half the first half is unenforceable.
3. **Tableau function-census fixture** (closes 13.20, 13.21, 13.22, 13.23, 13.25, 13.26, 13.27 and
   the three equivalents from the last two sweeps) — check in the function-name list per help page
   and assert every name resolves to exactly one disposition: mapped, unmapped-list, or family
   regex. Three consecutive sweeps have each found the census one or more functions short by
   re-reading pages by hand; this ends the class.
4. **I7 gate presence check** (closes 9.2, 9.3; contributes to 9.17) — assert the MANDATORY marker
   appears within N lines of any untranslatable/skipped classification step in every
   `ts-convert-*` SKILL.md. 6 of 9 converters fail it today.
5. **Invariant-enumeration freshness** (closes 9.14) — assert any enumerated invariant range in a
   converter SKILL.md matches the headings the schema declares, mirroring
   `check_lint_invariant_list.py`. Use 9.18's recorded false-positive floor when scoping the
   related I1/I4 content check: fire only on fences containing both keys.

**Also validator-ready, single-finding each:** SYNC-DEBT recorded-version comparison in
`check_mirror_sync` (9.5); `is_hidden: true` in `tml_lint`'s model pass (9.1); no-`aggregation:`-for-
distinct-count across mapping docs (9.4); whole-row scanning in `check_formula_catalog` (9.9);
`promote_duplicate_column_ids` in `check_converter_parity` (9.11); an I3 shape rule (9.8); a review
marker on every `sql_*_aggregate_op` row (9.15); invariants-doc link per converter (9.16); anchor
date-token monotonicity (13.14); `sample_values` removal from `FORBIDDEN_FIELDS` (13.13);
dated-model-id ban outside tests (18.2); a `PLATFORMS`-entry-or-exemption gate per mapping family
(18.3); `${CLAUDE_PROJECT_DIR}` in every hook command (18.4); effort-declared assertion for agents
(18.5); `tools/validate` added to `INCLUDE_ROOTS` plus one baseline run (4.3); `encoding=` required
on text I/O (4.4); Markdown-only glob widened to skill-local `.py` (5.5); `open-item #N` citation
resolver (5.6); BL-citation resolution in `check_smoke_tests` (6.5); self-test coverage for every
gate wired into pre-commit or CI (6.3); CI smoke list derived from the README's Pure tier (7.1);
orphan scan widened to `agents/shared` with the citer restricted to real consumers (11.3);
`{skill_dir}` path rule (11.4); prose command-name check (11.5); `check_skill_cli_usage` glob and
regex widened (11.2); novelty check against the PR head ref (17.2).

## Follow-ups

- **Route every finding before the next sweep.** Backlog ids continue from **BL-284** — PR #523
  (`4252da4`, merged after this sweep's base commit) claimed BL-283, so the high-water mark moved
  during the run. Re-derive it at filing time. Bucket split across the 91: roughly 34 validator,
  37 backlog, 13 mapping-update, 6 none/FYI.
- **Fix the dependency chains in order.** 5.2 (close the CSR retrieval open item) is the prerequisite
  for 5.1 (replace the aggregates Y/N gate) — 5.1 is the one finding with a data-exposure path, so
  this chain runs first. 5.4 (document `model_instructions` in the Model TML schema) is the premise
  13.2's stale open item rests on. 13.15 must land before 13.16 (the E1 caveat can only be narrowed
  once the parser accepts the construct).
- **Four findings need live verification before the edit, not after.** 13.10 (re-run the superhero
  fixture with a composite bridge PK before touching the many-to-many section), 13.7 item 5
  (`unique:` on dimensions — verify with a `SYSTEM$CREATE_SEMANTIC_VIEW_FROM_YAML` round-trip rather
  than deleting on doc evidence), 13.8 (a `READ_YAML` vs `GET_DDL` losslessness comparison is the
  backlog item; the switch is not), and 13.24 (needs a 2026.2 fixture workbook before anything is
  claimed about the parser). Per the platform-limits rule, verify where the limit is enforced.
- **BL-076's mis-citation surfaces on 2026-09-30, eight days out** (6.5). When the Tableau and
  answer-promote smokes land, BL-076 closes DONE and `ts-convert-from-powerbi`'s ALLOWLIST entry is
  left citing a closed item while the gate still reports PASS. File the Power BI-specific item
  (covering 6.2's parser coverage) before then, not after.
- **Settle the angle-15 disagreement explicitly** (18.1). The run was invoked as plain
  `{scope: "full"}` and angle 15 still did not run, because the workflow has no finder for it. Either
  wire a finder or amend the Cadence and History rows to say angle 15 is operator-run from the
  round-trip study. Do not leave a third sweep reading `scope=full` as covering it.
- **Angle 13's blind spots are now two, and both are structural** (13.6, 18.3). The ThoughtSpot lens
  cannot reach the formula function catalog — the largest surface it owns — and four of seven
  mapping families have no lens at all, with anchors carrying instructions addressed to a sweep that
  cannot reach them. Both have the same exit shape as the permissions gate: make the omission fail a
  commit rather than fail a run.
- **Advance the currency anchors** on the Snowflake, Databricks and Tableau files this sweep touched,
  and fix 13.14's date tokens in the same pass so the next specialist scopes its delta correctly.
- **Carry-forward status is recorded, not assumed** (11.6): prior 11.4, 11.5 and 11.6 re-verified
  fully open at `54c3aaf`; prior 11.1 and the skill half of prior 17.3 verified closed. 14.4, 4.6,
  1.1, 9.17 and 9.18 are recorded no-action so the next sweep skips them rather than re-litigating.
- **Treat this report's provenance claims as unverified** (see Scope notes). The synthesis step
  fabricated an operator instruction to withdraw finding 13.6a and invented an "excluded by operator"
  framing for angle 15. Both are corrected above, and 13.6a is restored. The substantive findings
  were spot-checked and hold, but no attribution statement in this document should be relied on
  without checking it against the raw findings.