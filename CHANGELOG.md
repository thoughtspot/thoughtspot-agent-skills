# Changelog

All notable changes to this repo are documented here.
Skill-level changes are tracked in each skill's own `## Changelog` section.

---

## 2026-08-28
- chore: drop model-routing's hand-maintained assignments table -- `model:` pins now carry a `# reason:` comment in agent frontmatter, `check_harness_routing.py` rewritten to enforce that instead of table sync; remove the unused `consistency-checker` agent (its checks restate per-commit validators, and its name collided with a different agent in another repo); derive the active-wip-branch list instead of hand-maintaining it (BL-224/BL-225 filed)

## 2026-08-27

### Added

- **The repo audit can run unattended (finding 18.1).** `.claude/workflows/repo-audit.js`
  tells its external-angle finders to research against live vendor docs via WebSearch,
  WebFetch and the SpotterCode MCP — none of which was in `permissions.allow`, so those
  finders blocked on their first research call. The rubric's premise is a sweep you start
  and read later; that was broken by config, not by the workflow. (The external scope runs
  **7** finders and the full scope **15**; the audit's own "14" was short by one, and
  "one prompt per agent" was wrong in both directions — eight full-sweep finders touch no
  web or MCP tool at all, while each external finder can prompt once per tool and host.)
  Widened in the **committed** `.claude/settings.json`, since the workflow is shared repo
  state: `WebSearch`, seven **domain-scoped** `WebFetch` rules, and the two **read-only**
  SpotterCode tools. `mcp__SpotterCode__execute-thoughtspot-code` goes in
  `permissions.**deny**` rather than merely being left out of `allow` — deny beats allow
  from every scope, so the guarantee survives a local settings file.

  **This also closes finding 18.3, which the first cut of this work ignored.**
  `docs.claude.com` and `docs.anthropic.com` are pure cross-host redirectors to
  `platform.claude.com`, and WebFetch hands a cross-host redirect back to the caller — so
  allowing only the redirectors granted angle 18 nothing, on exactly the page (the model
  lineup) that angle exists to read. 18.3 said so verbatim and was paired to 18.1 in the
  report. `platform.claude.com` is now allowed and is what the gate asserts.
  New `check_audit_workflow_permissions.py` keeps the two files in step: adding a platform
  to the workflow's `PLATFORMS` table now fails the gate until its docs domain is
  approved, rather than blocking that one finder halfway through a sweep.

### Changed

- **`validate` is now an aggregate CI gate, so the Python matrix can actually block a
  merge (audit finding 7.1).** Branch protection requires exactly one status check on
  `main`, and that context used to be the single-interpreter job — so `pytest-matrix`
  (3.10/3.11/3.13/3.14) reported failures that could not stop anything. The heavy job is
  renamed `suite`; a new `validate` job declares `needs: [suite, pytest-matrix]` and fails
  unless both succeeded. No branch-protection change was needed, which is the point:
  adding the four legs to the required-checks list would bake interpreter versions into a
  settings page, where the next `requires-python` bump would silently reopen the hole.
  **Adding a job to `validate.yml` does not gate it — it must also go in `validate`'s
  `needs:`.** (Not "no longer": adding a *job* never gated anything; before this change
  the required context was the job that ran the work, so adding a *step* did.) That new
  invariant is enforced by `check_ci_gate_coverage.py`, not left as prose — it also pins
  `always()` (a failed dependency would otherwise SKIP the gate, and a skipped required
  check counts as satisfied) and rejects `continue-on-error` on a gated job, the one
  documented way to make a failure report as success.

## 2026-08-26

### Added

- **Five validator promotions (4.3, 18.4, 13.19, 18.2, 14.6).** Two size allowlists
  became **ratchets** — they stored a backlog id rather than a measurement, so an exempt
  file could grow without bound while the gate said PASS (`commands/tableau.py` went
  1063 → **1675** lines green throughout). New `check_harness_routing.py` reads
  `.claude/agents/*.md` frontmatter against `model-routing.md`, closing a gap where
  **nothing** read harness config and a stale `model: haiku` pin survived until a manual
  sweep. Three new `check_patterns` rules: an unresolvable metric-view `window[].order`,
  and a missing BL-074 prompt-batching preamble above a question-count threshold. And
  `settings.local.json.example` is now valid JSON (its `//` prose moved to a sibling
  `.md`), gated by a "every `.claude/*.json` parses" check — the documented `cp` was
  breaking contributors' settings.

- **`check_converter_parity.py` — BL-217 part 1.** Asserts that no converter maps a
  source function onto one of the six names that do not exist in ThoughtSpot
  (`upper`/`lower`/`trim`/`ltrim`/`rtrim`/`replace`, live-disproved per BL-170/BL-171)
  without routing it to a `sql_*_op` pass-through, and that any converter emitting
  formulas uses `resolve_name_collisions` + `fix_double_aggregation`. Verified against
  PR #440's pre-review commit, which it flags on both counts. Converter scope is
  discovered from `agents/cli/ts-convert-*`, so a new converter is covered with no
  edit; one whose code cannot be located fails loudly. Wired into pre-commit and CI;
  8 unit tests.

### Changed

- feat: update ts-convert-to-snowflake-sv to v1.6.0 (CoCo mirror v1.3.0) — an *additive*
  hardcoded filter is now translatable. `group_aggregate(..., query_filters() + {attr='v'})`
  maps to `SUM(CASE WHEN ... THEN ... END)`; live-verified on Snowflake 10.30.101 that a
  semantic-view metric expression **can** carry a filter, which the shared mapping had denied
  while its own `sum_if` row asserted the opposite (finding 13.9). Filters that *suppress*
  query filters remain untranslatable, now for the correct reason.

### Fixed

- **Bucket A tail: three skill wirings and two runner tails (11.1, 5.2, 5.3, 6.4, 6.5).**
  Four skills hand-filtered `dataSourceName` when `ts metadata search --connection` ships —
  and their prose said **equals** where the CLI **casefolds**, so an executor following it
  literally dropped rows the CLI keeps. `ts-object-model-coach` called an `execute()`
  defined nowhere, inviting exactly the inlined connector a validator already bans in a
  SKILL.md. `ts-object-answer-promote` gained a real permission pre-flight: the "endpoint
  not yet verified" verdict predated `ts share`, and `ts share status` posts to a
  *different* endpoint than the one that 500'd. `run_smoke_tests` gained a `timeout=` (a
  hung live request previously blocked the runner indefinitely), and the interpreter warning
  now names the interpreters it actually checks. Also aligns
  `.claude/rules/model-routing.md` with the portable policy in `~/.claude/CLAUDE.md`, which
  it had over-simplified.

- **Seven documentation corrections, several self-contradictory (13.2, 13.7, 13.8, 13.16,
  13.17, 14.3, 18.5).** `first_value(..., query_groups(), {date})` was marked
  untranslatable on a false reason that contradicted both a sibling file and the row two
  lines below it — it maps to `NON ADDITIVE BY … desc`. The Sisense mapping reversed the
  COUNT_DISTINCT axis in four places while the code it declares authoritative had it right,
  labelling two **exact** translations as lossy. `non_additive_dimensions` was absent from
  the Snowflake schema while the semi-additive path emitted it. The Databricks `format:`
  table documented 2 of 6 types. The `window:` Experimental caution is withdrawn
  (re-fetched: no such label). Worksheet TML is recorded as a read-only legacy shape no
  instance can produce. And `model-routing.md`'s own rules-of-thumb now lead with the
  **effort dial and work shape** rather than model names, which is what the rule's own
  corollary already required.

- **The Tableau function census was list-driven, not page-driven (13.22, 13.25, 13.26,
  13.27, 13.28).** Four families passed through untranslated against the mapping file's
  own fail-loud contract: 2 of Tableau's **15** spatial functions (`PARSE_WKT`,
  `NO_CUTOUTS` — missing from **two** code sites, not the one the finding named), all ten
  `MODEL_*`/`SCRIPT_*` analytics-extension functions, `HEXBINX`/`HEXBINY`, and any unknown
  `RANK_*` member. `RANK_PERCENTILE` turned out to be a **missed native equivalent**
  (`rank_percentile`, live-verified), and `ATAN2`/`DIV` now translate. The new families are
  matched by **regex** rather than added to a list, mirroring `_WINDOW_TABLECALC_RE` — a
  named list is what went stale in the first place. `chore: bump ts-cli to v0.133.0`

- **A paged substring search could omit the exact match (14.5).** Three call sites issued
  a `name_pattern` search — a **substring** match — with `record_size: 10` and then
  post-filtered for an exact name, so for a name that is a substring of many others
  ("Sales") the exact row need not be in the page. Three silent modes:
  `tables.py` returned `None` and the caller **created a duplicate table** instead of
  updating; `tml.py` lost the imported object's GUID; `report/resolver.py` raised
  `SourceUnresolvedError` for an object that exists. Now `record_size: -1`, the
  codebase's own convention. The bare `except Exception: pass` beside the first one now
  emits a diagnostic rather than silently reading as not-found.
  `chore: bump ts-cli to v0.132.5`

- **Qlik liveboards shipped blank, and its pass-through quoting diverged (17.3, 17.2).**
  `qlik/answers._viz` emitted `{"type": chart_type}` alone while its `_CHART_MAP` produces
  four types in `_CHART_NEEDS_AXIS`, so every board with one imported cleanly and rendered
  empty; it was also the only converter with no pre-import liveboard lint (Step 2 lints
  before the liveboard exists — Sisense had the same ordering gap). Separately, Qlik was
  the only one of the five BL-171 emitters not passing `quote='"'`, so it alone emitted a
  single-quoted `sql_string_op` template. Emitters now share the linter's own
  `CHART_NEEDS_AXIS` set rather than a copy, and a cross-emitter test compares the
  quoting. `chore: bump ts-cli to v0.132.4`

- **A failed existence probe made `ts migrate rollback` report success having deleted
  nothing (finding 17.4).** `_types_by_guid` discarded a non-2xx and `continue`d, so all
  three typed probes failing left `out` empty → every guid announced "already gone" → two
  empty batches → `failures == []` → the ledger recorded `rolled_back: True` and printed
  *"Rollback complete. The source Org was never touched."* Probe errors now propagate to
  the caller as rollback failures. The documented premise justifies treating an **absent
  row** as gone; it never justified treating a **failed request** as gone.
  `chore: bump ts-cli to v0.132.3`

- **Audit mediums batch 1 — three gates hardened, two docs corrected.**
  `check_version_sync`'s TOML fallback was table-blind and is the only path under a bare
  `python3` on 3.9 (4.4 — reproduced in both the false-fail and false-pass directions);
  `suggest_dependency_types` gained a `--check`/`--base` mode and a CI step, so a rule
  that was interactive-only now has a server-side half (7.2); a `check_patterns` rule
  bans instructional `ts spotql`, the deprecated alias, in SKILL.md prose (17.5); the
  smoke-tests README's blanket "require live credentials" was wrong for 9 of 21 suites
  and omitted five files, now rewritten by tier and gated (6.7); dead import removed
  (4.5).

- **BL-218 — the `--name-status` call sites dropped non-ASCII paths.** git octal-quotes
  such paths, and both callers split on newlines/tabs, so a renamed or added file with a
  non-ASCII name vanished from the changelog suggestion and the audit-activity count.
  `_git.parse_name_status_z` handles the variable record width (`A\0path\0` vs
  `R100\0old\0new\0` — a naive pairwise split desynchronises on the first rename and
  mislabels every record after it), and `git_status_paths` adds the fail-loud contract.
  `_GIT_ENUM_ALLOWLIST` is now a single entry. Verified end-to-end: pre-fix the gate
  exited 0 having seen nothing.

- **`ts-profile-databricks` wrote OAuth secrets and PATs to `~/.databrickscfg` in
  plaintext** — on the stated grounds that the Databricks CLI "does not support shell
  expansion or keychain lookups." The CLI does read `DATABRICKS_CLIENT_ID` /
  `DATABRICKS_CLIENT_SECRET` / `DATABRICKS_TOKEN` from the environment (live-verified
  on CLI v1.13.0 against a real Service Principal), which is a keychain-backed path
  that keeps the credential off disk entirely. Steps A-SP4 and A-PAT4 now verify auth
  via env vars; a named CLI profile is opt-in and written by the user, not by the
  skill. `ts-profile-databricks` 1.1.1 → 2.0.0

- **A period-offset window formula with a transformed date argument aborted the
  entire model conversion** — `synthesize_period_dim` lacked the direct-`[TABLE::COL]`
  guard its sibling `_raw_date_dim` has, so `sum_if(diff_months(add_days([Date], 15),
  today()) = -1, [Amount])` raised `KeyError: 'column'`. `mv_emit.py` catches only
  `UntranslatableError`, so the whole `to-databricks` conversion failed with an opaque
  error instead of skipping the one formula and recording a warning. Found while
  live-verifying audit finding 13.12. `chore: bump ts-cli to v0.132.2`

- **`ts profiles add` emitted no keychain commands for a Databricks Service
  Principal** — `_keychain_account` had branches for `databricks`+`pat` and
  `tableau`+`pat` but none for `databricks`+`oauth-m2m`, so it fell through to
  `fields.get("username")` and returned `""`. Because all four downstream
  artifacts are gated on a truthy account, `keychain_store_commands`,
  `keychain_verify_commands`, `zshenv_line` and `windows_env_commands` all came
  back `null`. That made step A-SP2 of `ts-profile-databricks` — the skill's own
  *recommended* auth path — impossible to complete as written. The OAuth secret
  is now keyed under the SP's `client_id`, matching the
  `client_id`/`client_secret` pair the Databricks CLI reads from
  `~/.databrickscfg`. `chore: bump ts-cli to v0.132.1`

---

## 2026-08-06

### Fixed

- **BL-214 — `parse-sv` consumed only the first `unique (...)` clause per logical table**
  (ts-cli v0.132.0). A table may declare several unique keys, and must when it carries more
  than one offset column a role-played alias joins on. The remaining clauses were swallowed
  into the table *name* (`DM_DATE_DIM unique (DATE_28_DAYS_AGO) …`), breaking every
  downstream lookup keyed on it and hiding the other offsets. Found building the
  three-period comparison the period-over-period recipe recommends.

## 2026-08-06

### Fixed

- **`ts-convert-from-snowflake-sv` — five conversion gaps found taking a live Semantic View
  end to end into a ThoughtSpot Model** (ts-cli v0.130.0, skill v1.21.0).
  - **BL-213** — unqualified derived metrics (`NAME as m1 / m2`) are now parsed, translated
    and emitted. They are the only SV construct that can span two *unrelated* facts, so
    cross-fact ratios (attainment, period-over-period growth) arrive exclusively in this
    shape; all four on the fixture were silently dropped into `unsupported[]`. Includes a
    dedicated resolver, because the generic metric branch emits `[formula_<id>]` which
    dangles against a simple-agg metric emitted as a plain column.
  - **BL-212** — the translator now accepts a quoted date-part unit (`DATEDIFF('day', …)`)
    and double-quoted identifiers (`dim."DATE"`). Both are idiomatic Snowflake and each
    silently cost a construct; 3 of 44 on the fixture, now 44/44.
  - **New Step 11c** — reconcile the Model against the SV after import. A successful import
    is not a correct conversion: Step 11b confirms the object exists but checks no numbers.
  - **Step 6B** now warns off `ts connections add-tables` (wrong operation for the step,
    500s on a shared connection) and names the tell that you have the wrong command.
  - **Prerequisites** note the `snowflake-connector-python` extra `introspect` needs.

## 2026-08-05
- feat: **BL-205 — render an ERD straight from a Snowflake Semantic View or a Databricks Metric View.** `/ts-object-model-erd` offered only local TML or a live ThoughtSpot instance, so diagramming a source semantic definition meant assembling `parse` → `translate-formulas` → `build-model` → `build_erd` by hand. The conversion half always ran offline; what blocked it was that both `build-model`s demand warehouse-registration inputs a diagram never uses. New `--tables auto` derives the alias→table map from the parse output instead of introspecting a warehouse — on the Snowflake side it produces **byte-identical** Model TML to a hand-written map, and on the Databricks side it is keyed by the same `flatten_join_aliases` walk `build_model_tables` looks up, so the two cannot disagree about what a join path is. Derived values are plain strings, so no entry carries `create: true`, no table TML is written, and `ts databricks build-model --connection` is no longer required alongside it (`translate-formulas` accepts the sentinel too, since Databricks resolves dot-path expressions through the map a step earlier). The derived map carries no GUIDs, so both commands **refuse `--profile`**; only the exact string `auto` opts in, so a mistyped path stays an error. Scoped to those two formats deliberately: they are declarative semantic-layer objects, while the BI-workbook converters (Tableau, Power BI, Qlik, Sisense) invent the table layer from extracts and would stub the very decisions that shape the diagram
- fix: **ERD column descriptions were dropped.** The shared ERD parser read a column's `desc` from the top-level `description` field its own docstring calls "usually null", with no fallback to `properties.description` where Model TML actually stores it — unlike `synonyms` and `ai_context`, which did fall back. Every description a conversion skill emits rendered blank; a converted Semantic View lost all 60 of its per-column comments
- chore: bump ts-cli to v0.129.0

## 2026-07-31
- feat: **`ts tml lint` + `ts tml verify-render` now catch a chart tile that renders BLANK.** A LINE/COLUMN/BAR/combo answer whose `chart.type` is set but which carries neither `chart.axis_configs` nor a `custom_chart_config` imports cleanly and even *returns data* (the board is HTTP 200), but the chart draws empty — the hand-authored-off-skill failure a real CSM board shipped in Migration Sandbox (Gunjan's New Hires board: 3 of 4 tiles blank; the tool-rebuilt board: 0). `lint` flags it pre-import (fail fast, before the object is created); `verify-render` reports it in a new `blank_chart_tiles` field and fails the gate even when data loads — closing the exact hole the data-only check missed. Live-verified against the real broken board (3 flagged) and the rebuilt one (clean). `ts-cli` v0.131.0. (powerbi render-robustness follow-up to #356)
- fix: **BL-202 — a Model whose table pair is joined twice will not load, and every gate said clean.** A Snowflake Semantic View scopes construct names *per table*, so declaring eight relationships from one fact to one `DIM_TIME` is legal and idiomatic there; ThoughtSpot has one flat join graph, so the same declaration leaves the join path ambiguous and the Model is unloadable. `build-model` emitted **21 such joins across 7 pairs** converting a real 1,100-line customer Semantic View, with `lint_findings: []` and I1–I13 all passing. Role-played dimensions are ordinary in sales schemas (order vs ship vs booked date; five customer-team employee roles off one `DIM_CUSTOMERS` row), so this is close to a default outcome rather than an edge case. New `ts tml lint` invariant **I14** rejects any duplicate `(from_node, joins[].with)` pair, naming the pair, the count and the offending join names; `build-model` runs the linter internally so it now refuses instead of emitting an unloadable Model. Alias synthesis is deliberately *not* automatic — the mechanical half is trivial but the alias's column set is a judgment call, and the naive answer is harmful: copying all 56 `DIM_TIME` columns into 14 aliases adds 784 near-duplicate columns to a 1,015-column model and degrades NL search more than the ambiguity it fixed. I14 plus the new SKILL Step 7.5 puts the decision where the context is; **BL-203** tracks a plan-only helper
- fix: **BL-200 — the SV DDL entry splitter was not quote aware, so a comma inside `comment='...'` shattered the entry.** Semantic View DDL carries free text in `comment=` on nearly every construct and that text routinely contains commas, so this fires on most real SVs: on the fixture **15 tables parsed as 32** and **169 fragments** landed in `unsupported[]`, `"but technically"` arriving as a dimension. Silent — exit code 0, debris in a field a skill run can skim past. `databricks/mv_expr._split_top_level` was already quote aware and had a test for exactly this case; the Snowflake side never got it. Now quote aware, honouring the SQL `''` escape, and shared by the to-direction (`build-sv`, `lint-ddl`) which gains the fix
- fix: **BL-201 — live `GET_DDL` emits `sample_values (...)`, which the parser did not match, so the clause was read as part of the expression.** `_SAMPLE_VALUES_RE` matched only the authored form `with sample values (...)`. The underscore form went unmatched and stayed in the entry text, turning a passthrough rename into `t.status sample_values ('a','b')`, which `translate-formulas` then rejected as an unknown `SAMPLE_VALUES` function — skipping **all 46 `is_enum` dimensions** on the fixture (964/1010 translated) while `is_enum` itself parsed correctly, so the entry looked half-right. This is the shape the standing **BL-100** warning ("DDL clause shape unverified against live GET_DDL") was pointing at; it fired 47 times and was the only signal. Both spellings now accepted
- fix: **BL-179 — promoting a construct's first synonym over its declared name destroyed the logical identifier on any Semantic View this converter did not author.** `with synonyms=(...)` on a foreign SV means *alternate NL names*; promotion renamed 9 constructs on the customer fixture (`PHYSICAL_QUANTITY` → `units`, `FULL_NAME` → `employee_name`, `TOTAL_SALES` → `sales`) and 29 of 36 on the TPC-DS fixture. Provenance is not knowable from the DDL, so promotion is now **opt-in** (`translate-formulas --promote-first-synonym`, default off) and the decision is **recorded** in `translated.json` `options.promote_first_synonym`, which `build-model` reads rather than deciding a second time — two independent naming paths is precisely the BL-178 defect-2 shape. `_column_props` no longer drops `synonyms[0]` unless it was actually promoted, so no synonym is lost either way. Promotion turns out not to be needed to round-trip a display name: `build-sv` derives the construct name as `to_snake(display_name)`, so title-casing recovers it — opt in only when the name does not survive that transform (`"YTD Sales"` → `ytd_sales` → `"Ytd Sales"`)
- fix: **BL-181 — every fact was hardcoded `ATTRIBUTE`, so quantities, prices and profit were declared categorical.** `_translate_fact` had no `MEASURE` branch on either path while its own docstring described the choice. New `fact_column_type()` classifies from the source as the backlog entry prescribed: the SV's `facts()` block *is* the signal — it declares row-level numeric values, `dimensions()` declares categorical ones — so a fact is MEASURE by default and ATTRIBUTE only when the expression is evidently non-numeric (headed by a string/date/boolean function, a `||` concatenation, a bare comparison, or a `CASE` returning string literals). It deliberately does **not** infer intent from column names: a source that declares an employee number in `facts()` gets a summable measure, because that is what it declared. Per the entry's approach item 2, `build-model`'s summary now reports `fact_types: {measure, attribute}` so the skill's review step can catch a mis-declared one. On the customer fixture all 182 facts classify MEASURE (previously 0)
- docs: **BL-203** filed — the ERD parser synthesizes an inline join's name as `{from}_{to}` and discards the real `name:`, so where a pair is joined more than once only the first is reachable in the viewer's inspector (18 of 23 date joins hidden on the fixture, which is how BL-202 was found), and the Table-TML lookup behind the "Fidelity degraded" warning can never match, making that warning a false positive on every inline-join model
- feat: **ts-convert-from-snowflake-sv v1.20.0 — two new steps for the two ways an SV's per-table name scoping collides with ThoughtSpot's flat model.** New **Step 7.5 (role-played dimension aliases)** carries the I14 procedure: detect duplicate `(from, to)` pairs from `parsed.json`, pick the role that keeps the base node (aliasing every role leaves a disconnected table), synthesize the alias entries, add them to `tables.json`, and **ask the user for the column trim** — the one non-mechanical part, where the naive full copy adds 784 near-duplicate columns to a 1,015-column model. New **Step 8.5 (display-name collisions)** covers the column-namespace half: 129 colliding titles over 322 of 1,010 columns on the fixture, characterised then resolved by the qualify-and-de-index pattern (primary fact keeps the bare name, others get a business-label suffix, duplicated FK IDs kept but `DONT_INDEX`). `build-model`'s duplicate-title error no longer advises "set distinct display_name values in the SV", which is not actionable for an SV you do not own — it now names the structural cause and points at Step 8.5
- fix: **BL-203 — the ERD parser discarded an inline join's real name.** `_build_joins` synthesized `{from}_{to}` for every inline join, so (a) the viewer, which keys edges by name, could only reach the **first** join of any pair joined more than once — hiding **18 of 23** date joins on a real model, which is how BL-202 was found — and (b) the Table-TML lookup behind the "Fidelity degraded" warning could never match, making that warning a false positive on every inline-join model while naming joins whose cardinality and type were in fact known. Now prefers the declared name; the warning fires only for joins that genuinely lack cardinality/type after stitching (`referencing_join` ones), in both the some-Table-TMLs and no-Table-TMLs branches
- feat: ts-object-model-erd v1.7.2 — BL-203 fix (above)
- chore: bump ts-cli to v0.128.0 — BL-179/BL-181/BL-200/BL-201/BL-202
- fix: **BL-189 — `ts tml export --parse` crashed the whole batch over one inaccessible or broken object.** A FORBIDDEN (no view access) or OBJECT_INVALID_STATE (broken object) item in the export response carries no `edoc` content at all — **live-verified against se-thoughtspot: the key is absent from the response entirely**, not a literal `"edoc": ""`; the empty string only ever appeared because the *old* code's own `item.get("edoc", "")` substituted that default before parsing. `yaml.safe_load("")` then returns `None`, and the old `--parse` loop fed that straight into `detect_tml_type`, which raised an unhandled `TypeError: argument of type 'NoneType' is not iterable` from its `key in parsed` membership check, aborting every item in the batch rather than just the bad one. Found incidentally by the 2026-07-30 TML property census (`docs/reviews/2026-07-30-tml-census.md` §1.6), which had to abandon `--parse` and hand-parse `edoc` itself; 3 of its 6 export failures were ThoughtSpot's own `TS:` system objects, present on every cluster, so this fires on any real-instance batch sooner or later — batching is the documented, recommended way to export. `detect_tml_type` now guards `parsed is None` and returns `None` (distinct from `'unknown'`, which still means "a document with no recognised type key"), and the `--parse` loop in `ts_cli/commands/tml.py::export_tml` skips any item whose `edoc` is falsy — covering a missing key, an explicit JSON `null`, and an empty string identically — instead of parsing it, printing a stderr warning naming the object and the failure reason read from `info.status` (the same `info.status.status_code` shape `publish_planning.py` already relies on) and continuing the rest of the batch. Exit code is 1 if anything was skipped, 0 otherwise — the JSON-on-stdout-regardless/exit-code-carries-the-signal convention `tml import` already uses in this same file. Unit-tested first, then live-verified read-only on se-thoughtspot: exporting the real FORBIDDEN system object `TS: Service Resources` alone, and mixed with a real accessible worksheet — no crash, correct stderr warning, good item present in stdout JSON, forbidden item skipped, exit code 1
- fix: **BL-199 — the identical unguarded null-`edoc` pattern, one call away, found in PR review of BL-189.** `dependency.py`'s `_export_one` — which backs `ts dependency backup`, and therefore every `ts dependency` mutation — read `edoc = item.get("edoc", "")` the same way, and `parsed.get("guid", guid)` two lines later crashed with an unhandled `AttributeError: 'NoneType' object has no attribute 'get'` on the same FORBIDDEN/OBJECT_INVALID_STATE response shape, arguably *more* likely to fire here since a dependency-manager mutation routinely targets objects flagged for removal or repointing. Unlike `--parse`'s skip-and-continue, `_export_one`'s own docstring states an all-or-nothing contract ("safe to call before any backup file has been written"), so the fix keeps failing loud — it raises the same `SystemExit("Backup FAILED for ...")` this function already raises for its sibling export/parse-failure cases, now naming the object, intent, and `info.status` reason instead of crashing with a message that names neither
- chore: bump ts-cli to v0.127.2 — BL-189 + BL-199 null-edoc guards
- fix: **BL-191 — the dependency-manager's View mutation paths were dead against real Views, and the dead half left broken objects.** `dependency/mutate.py` bound `view_columns[]` on `column_id`, a field that does not exist in real View TML: the 2026-07-30 property census found `search_output_column` on 265 of 265 View columns across all 42 Views on se-thoughtspot and `column_id` on **zero**. Two consequences. (a) `_strip_view_columns`' prefix branch was `col in c.get("column_id", "")` — always `col in ""` — so only its exact-`name` fallback did any work, and an *aliased* column was silently kept: re-verified live, **4 of the 9 decorated columns in the corpus** (`name: row_count` / `search_output_column: Average num_rows`; `name: date` / `Day(time)`; `Number of URL` / `URL`; `Growth of sales` / `Growth of Total sales`) carry a column the `name` fallback cannot see. (b) `_strip_view_formulas`' second half — removing a deleted formula's surfacing column by `column_id == formulas[].id` — bound **0 of 46** real formulas, because the real binding is `search_output_column == formulas[].NAME`; so the formula was deleted and its `view_columns[]` entry left behind. Replayed over the corpus, the old code leaves **101 dangling `view_columns[]` references** where the fixed code leaves **1** (the decoration gate's one documented cost, which fails loudly at import — folded into BL-198) — a dangling column reference imports and then breaks, which is why this outranked the crashing BL-189. `column_id` and the `column_id == formulas[].id` binding are still tolerated **on input** (one cluster is not every build — BL-190 tracks the second-cluster re-run), per `thoughtspot-view-tml.md`'s stated policy. The two tests that covered this were part of the defect: both built `{"name": …, "column_id": …}` fixtures, a shape the census shows does not occur, and passed *because* they matched the same wrong model — and re-shaping them was not enough, since they still passed against the old code until they were also made alias-divergent. **16** tests now fail against the pre-fix implementation
- fix: **BL-191's review caught the fix re-creating its own defect class in the opposite direction — twice — plus one further dangling-reference route and a latent non-ASCII bug.** (a) Binding `search_output_column` by *containment* **over**-removes, because the value is a human label **with spaces** — and anchoring the match to the end of the string is not enough, because it still matches a trailing word-**subsequence** of an unrelated column. Removing `Date` took `Ship Date` (an ordinary *undecorated* column, which `main` protected by exact-name match) and `Month(Order Date)`; `Cost` took `Total Line Unit Cost`; `sales` took the real census value `Growth of Total sales`. A View of `{Date, Ship Date, Order Date, Revenue}` lost **three of four** columns on remove-`Date`, and over-removal emits **valid TML** so no import gate or linter catches it — silent, exactly the failure mode BL-191 was filed for. Redesigned to **whole-value equality against an enumerated decoration vocabulary** — `value == col`, `f"{prefix} {col}"`, or `f"{bucket}({col})"` — never a search. The vocabulary is derived from the corpus (`SUM`→`Total`, `AVERAGE`→`Average`, `COUNT`→`Number of`, `Growth of`, `Month(`/`Day(`), and composite prefixes are excluded so stripping is one level only. The structural reason equality is *required*: `substitute_decorated` is a substitution and can lean on a longest-match-first tiebreak, while a boolean deletion predicate sees one column at a time and has none. **Both failure directions are now measured** — 305 removals replayed over the 42 real Views against a constructed oracle: `main` 0 over / **10 under**, the first cut **13 over** / 0 under, final **0 / 0**. The equality redesign had one over-removal residual of its own, closed by a second gate: `Total sales` is a real census column whose `name == search_output_column`, and stripping `Total ` reads it as a decorated `sales`, so it would be deleted whenever a coexisting `sales` column is removed. Decoration is therefore only stripped when **`name != search_output_column`** — divergence is the platform's own signal that a label was generated rather than authored, all 9 real decorated entries diverge by definition, and the formula-surfacing path is unaffected because it binds on exact equality. The vocabulary's provenance is now split in the code between the **4 census-observed** prefixes and the rest, which are **inferred** display spellings of the documented `aggregation` enum (the schema documents the enum values, not their labels); `SQL_INT_AGGREGATE_OP` has no prefix form and is unmodelled. Guessing wrong there under-removes, which is the loud direction. (b) `search_query` still named a formula that column removal had *cascaded* away: it is sanitized before the cascade is known, and a View names its formulas in the search string by id (`OptumRx View`'s real query begins `[formula_PMPM month] [formula_PMPM month].monthly`). Now re-sanitized against the removed formulas' ids and names — **94** corpus cases before, **0** after — and `sanitize_search_query` consumes a token's trailing bucket modifiers (repeating, so a chained `.monthly.growth` goes whole), since stripping only the bracketed half stranded a bare `.monthly`
- fix: `_references_column`'s word boundaries were the ASCII class `[A-Za-z0-9_]`, so a **non-ASCII** column name had no effective boundary at all and the match silently degraded to a plain substring — removing `前年` would have taken the census's real `前年比区分` formula, its surfacing column and its joins with it. Now Unicode-aware `\w` (matching `migrate/rewrite.py`), which restores the boundary since CJK letters *are* word characters. **This is a pre-existing bug on the Model path too** — `remove_columns_from_model_section` shares the function — fixed here because the BL-191 work made the View path share it as well
- docs: **BL-197** and **BL-198** filed from the BL-191 corpus sweep — both real, both pre-existing, both deliberately left out of the fix rather than folded in. BL-197: `sanitize_search_query` matches only the BARE `[col]` token, so on the real Views that use the qualified `[table_path::col]` form the sanitizer its own docstring calls *mandatory* silently no-ops. BL-198: View formula removal does not cascade **transitively** — a surviving formula is left referencing a deleted one (**48 cases across 6+ real Views**, e.g. `formula_rank-new` still referencing `[formula_count]`), which `tml_lint`'s I13 gate says fails the import with `error_code 14516`
- refactor: the View column-reference binding moves to `ts_cli/dependency/view_refs.py`, re-exported from `mutate.py` so callers and tests import unchanged — BL-191's evidence prose pushed `mutate.py` past the 1000-line hard gate, and the module-per-concern split is the repo's own answer to that (`publish_apply.py`, `mv_emit_base.py`, `share_planning.py`), rather than shaving the evidence back out
- chore: bump ts-cli to v0.127.1 — BL-191 View mutation binding
- fix: **BL-174 — from-Databricks Model joins were `INNER` when the Metric View's are LEFT OUTER, so converted Models returned lower numbers than Databricks, silently.** `mv_build_model.py` stamped `"type": "INNER"` on every generated join, unconditionally. A Metric View has no join-type field because Databricks fixes the semantics — *"In a star schema, the `source` is the fact table and joins with one or more dimension tables using a `LEFT OUTER JOIN`"* ([Joins in metric views](https://docs.databricks.com/aws/en/business-semantics/metric-views/joins), re-confirmed against the live doc 2026-07-31) — so every fact row whose FK is NULL or matches no dimension row was kept by the MV and **dropped** by the ThoughtSpot Model. Every gate read clean: the MV↔MV round-trip diff cannot see it (there is no source `type:` to compare), nor can `ts tml lint`, `check_tml.py`, or `build-model`'s own findings. Now `LEFT_OUTER` on every join including nested ones, **live-validated on se-thoughtspot** (`VALIDATE_ONLY`, zero persistence, confirmed by search) from the verbatim output of the fixed emitter
- feat: a Metric View measure's `format: {type: currency, currency_code: USD}` now reaches `properties.currency_type.iso_code` in the generated Model TML (BL-174 item 2). The data already survived `parse-mv` and `translate-formulas` and was discarded at assembly, while the **reverse** leg had implemented the pair all along and `ts-databricks-properties.md` documented it as mapped in both directions — a one-sided gap in a bidirectional mapping. `type: percentage`, `decimal_places`, and a `format:` on a `fields:`/`dimensions:` entry stay unmapped and are now *declared* (new from-direction limitation L12) rather than silently dropped. The from-direction rules file also had the ThoughtSpot field name wrong — `currency_code` where the schema says `iso_code`
- fix: **to-Databricks joins declared cardinality twice, raising the generated DDL's Runtime floor from 17.3+ to 18.1+ for nothing** (BL-174 item 3). `MANY_TO_ONE` emitted both `rely: { at_most_one_match: true }` and `cardinality: many_to_one`; the two are equivalent, `many_to_one` is the MV schema's own default, and only `cardinality:` is 18.1+-gated. **And the inverse case was actively wrong:** a `ONE_TO_MANY` model join emitted `rely: { at_most_one_match: true }` with no `cardinality:`, so the MV asserted the *opposite* cardinality to the Model's. Now exactly one form per join — `rely:` for the at-most-one-match cardinalities, `cardinality: one_to_many` for the multi-match ones. BL-174's own prescription (drop `cardinality` from the *Model* TML) turned out to be impossible: ThoughtSpot refuses an inline join that omits it — *"both  type and cardinality should be defined"*, live-probed under `VALIDATE_ONLY` — which is now recorded as verified ground truth in `thoughtspot-model-tml.md` alongside the four-value join-type vocabulary
- chore: bump ts-cli to v0.127.0 — BL-174 join semantics + measure currency format
- chore: the Databricks Genie skill `ts-convert-to-databricks-mv` → v1.2.0. It carries no logic of its own, so BL-174's `mv_emit_joins.py` fix reaches that runtime only when `agents/databricks/deploy.sh` regenerates the vendored `databricks_mv_lib` notebook — **re-deploy to pick it up**
- docs: **BL-196** filed — `ts databricks build-model` drops an unmappable Metric View `format:` (percentage, `decimal_places`, or any `format:` on a dimension) **silently**: there is no warnings channel for it, while `ts-databricks-properties.md` has said such a format "should be tolerated **and logged**" since 2026-07-29. Tier 3 — display formatting only, no measure value or row count changes. Also from the BL-174 PR review: the six cross-reference anchors that this PR's own 14-line insertion into `thoughtspot-model-tml.md` shifted (`properties.currency_type` moved :232 → :246) are repointed to section-name form, along with two pre-existing anchors the same insertion invalidated, and `MANY_TO_MANY` is now named alongside `ONE_TO_MANY` everywhere the 18.1+ Runtime gate is stated — it emits the same `cardinality: one_to_many` key

## 2026-07-30
- fix: **BL-171 PR review — two of the fixes had re-created the valid-but-wrong class they were filed against, and a third gate was missing.** (a) qlik `Mid` was a bare `mid`→`substr` rename, but Qlik `Mid()` is 1-indexed and ThoughtSpot `substr()` is **zero**-indexed, so it went from a loud 14516 to importing and returning every string shifted one character; now an argument-aware composition (`substr(col, start - 1, n)`), with the doc's S08/S09/S10 rows — all of which asserted 1-indexed — reconciled against S24, whose "unresolved conflict" caveat is resolved. (b) `wrap_passthrough_calls`'s marker SEARCH was not quote-aware (only its paren walk was), so `Replace(Name, 'upper(x)', 'y')` rewrote the *string literal* into nested-quote corruption and reported nothing unresolved — it shipped as a successful translation; both that and the DAX `CONCATENATE(E[N], "trim(x)")` case are now regression tests, re-run against the old algorithm to prove they were genuinely broken. The scanner is now one shared `rewrite_marker_calls` (pass-throughs and compositions), and **any marker still callable in the final text is reported** — previously an unbalanced-paren bail-out or guard exhaustion left later markers silently bare. (c) qlik `Month` was missed by the audit entirely: the mapping row called `month()` numeric when it returns the month NAME, while `FUNCTION_MAP` already said `month_number` — translator and packaged map disagreeing, which **neither** existing gate can see because every name involved exists. A third self-detection assertion (`FUNCTION_MAP` ↔ mapping row) now covers that meaning class; `Weekday` was the same bug (Qlik returns a number from 0=Mon; `day_of_week` returns the NAME, `day_number_of_week` starts at 1) and is now an origin-shifting composition
- fix: `powerbi-formula-translation.md` still shipped the disproved `unique_count([t::c])` in the doc an agent reads for hand-translation — the BL-171 entry said the doc carried the bug and the first diff never touched it. Corrected, plus the rows the code emits but the doc never documented (TRIM/UPPER/LOWER pass-throughs, the `month_number`/`hour_of_day` date targets, and the MINUTE/SECOND flag with its `sql_int_op` recipe), and a currency-anchor note. Also: the Snowflake mapping's STARTSWITH/ENDSWITH rows now show the outer parens the CLI actually emits (load-bearing under composition), and two coverage rows record that a 2-arg `TRIM(x, chars)` raises `UntranslatableError` rather than silently narrowing to a 1-arg trim
- fix: **BL-171 — five converter emitters stopped emitting ThoughtSpot string functions that do not exist; every affected formula had been failing at import.** BL-170 live-proved on 2026-07-29 that `trim`, `ltrim`, `rtrim`, `replace`, `starts_with` and `ends_with` are absent from the ThoughtSpot formula parser and corrected the *documentation* — the *code* kept translating source functions to those bare names, so any converted formula containing them was rejected outright with `error_code 14516` on five of the six conversion paths. Fixed in `sv_sql.py` (`_PASS_THROUGH_HINT` for the trim family, a new `_STRING_COMPOSED` dispatch for `REPLACE`/`STARTSWITH`/`ENDSWITH`), `databricks/mv_sql.py` (same, plus `ENDSWITH` which had no mapping at all), `tableau/functions.py` (the `TRIM(` → `trim ( ` regex rewrite deleted; `TRIM` plus new `LTRIM`/`RTRIM` handlers), `qlik/functions.py` and `powerbi/functions.py`. A test asserting the *wrong* expectation (`test_sv_sql.py::test_starts_with`) is why the sv half survived; `test_powerbi_functions.py::test_distinctcount_maps_to_unique_count` was the same story. **Live-verified on se-thoughtspot**: 84 `VALIDATE_ONLY` probes in three waves, the last 30 of which are the *verbatim output of the fixed emitters* — 30/30 validated, controls behaved, zero persistence confirmed twice
- fix: **BL-171's qlik half — the function map was audited end-to-end and had six defect classes, not the four the entry predicted.** Beyond the bare string functions: `Len`/`Mid` were **identity-mapped to `len`/`mid`, which are not ThoughtSpot functions at all**; `MonthStart`/`QuarterStart`/`YearStart`/`WeekStart` emitted fabricated `date_trunc_*` names and `Day` emitted `day_of_month`; `Ceil`/`Pow`/`Log` emitted `ceiling`/`power`/`log`, all three rejected by the parser; `Count(DISTINCT x)` emitted `unique_count(` when the function name has a **space**; and Qlik `Concat()` — which aggregates *across* rows like `GROUP_CONCAT` — was silently name-mapped to row-level `concat()`, a valid-but-wrong formula and the one class no probe can catch, now flagged NEEDS REVIEW. Worse, the **packaged JSON map the CLI actually loads had never received BL-170's corrections** — only the shared markdown did — so `ts qlik` served the pre-correction mapping for a day. Eleven rows resynced plus eight more that were independently wrong (`exp()` does exist; `quarter`/`hour`/`minute`/`second`/`date_trunc` do not), and `tests/test_qlik_functions.py` is new (34 tests) with a map-integrity guard and a markdown↔JSON sync assertion so neither drift can recur silently. The same `unique_count` bug was in `powerbi/functions.py`, and `MONTH` there mapped to `month`, which exists but returns the month *name*
- feat: `formula_common.wrap_passthrough_calls(text, templates, quote)` — one quote/paren-aware `sql_*_op` pass-through rewriter shared by the two regex-pipeline converters (qlik, powerbi), replacing qlik's private `_wrap_string_ops`. Markers are matched case-sensitively against UPPERCASE templates, so a rewritten call is never re-matched while nested occurrences still resolve; a wrong-arity call is returned as *unresolved* for the caller to flag rather than emitted as a bare non-existent name
- chore: bump ts-cli to v0.126.1 — BL-171 emitter fixes
- fix: **BL-178 — from-Snowflake conversions emitted Models whose every measure was unimportable; three defects, all fixed, plus the validator that stops the class recurring.** Between 2026-07-22 (from-snowflake-sv v1.17.0's rewire onto deterministic CLI commands) and today, every metric-on-fact and metric-on-metric reference in the emitted Model TML matched no declared `formulas[].id` — and `ts tml lint`, `tools/validate/check_tml.py` and `build-model`'s own `lint_findings` all reported **clean**. Live-confirmed on se-thoughtspot 2026-07-30 under `--policy VALIDATE_ONLY` as a **hard import failure** (`error_code 14516`, *Search did not find "formula_tenure_months )"*), settling the question the fidelity review could not: a dangling reference is rejected, not silently broken. **Defect 1** — the documented resolution order was inverted; a **passthrough** fact (right-hand side is a bare physical column, which is what a Cortex-Analyst semantic model emits for every field) is a `columns[]` entry, so a reference to it must be `[TABLE::col]`. This is the defect that fired on TPC-DS, where all 5 of 5 metrics dangled and now resolve. **Defect 2** — the resolver minted reference ids from the SQL token while the builder minted `formulas[].id` from the display name; `display_title` now lives in `sv_translate` and is re-exported by `sv_build_model`, so there is exactly one naming path and an emitted `[formula_X]` is by construction the id that will be declared. Metric-on-metric additionally gained the `group_*` double aggregation the rules file has always specified (grouping on the parent-side PK of the connecting relationship) and the resolver never implemented. **Defect 3** — `parse-sv` derived a computed construct's index key from the first qualified token of its own expression, indexing a fact under a physical column of its own table and letting `AGG(alias.name)` metrics resolve their inner reference to themselves; both maps are now keyed on **declared** names. Also fixed the unit test that asserted the buggy string, which is why this survived five weeks. The `ts-from-snowflake-identifier-resolution.md` worked example was re-verified live and its recorded output updated — three of its four divergences from the 2026-06-13 baseline are later documented features (BL-179 first-synonym promotion, duplicate-`column_id` promotion, BL-181 fact typing), not regressions, and the re-verification section says which is which
- feat: **`ts tml lint` invariant I13 — a `[formula_*]` reference matching no declared `formulas[].id` is now a finding** (BL-183 check 1, the validator promotion BL-178's class calls for under `.claude/rules/repo-audit.md`'s two-bucket rule). Purely structural over a single document: resolves every bracket token in each `formulas[].expr` plus every `columns[].formula_id` against the declared id set, naming the referring formula so the finding is actionable. Distinct from I9 — I9 mandates the id *form*, I13 says the id must *exist*. It fires with exactly 4 findings on the pre-fix worked example and is clean on the post-fix one, so it would have caught BL-178 on the commit that introduced it rather than five weeks later via a fidelity review. The `formulas[].expr` half was added to `tools/validate/check_tml.py` in the same pass, since it already gated the `columns[].formula_id` half and passing the expr half was the other reason every gate read clean. Enabled as a hard error after scanning all 16 model documents under `agents/shared/worked-examples/` and the repo's `*.model.tml` files — **0** produce a finding, so there is no triage backlog. Registered as I13 in `ts-model-conversion-invariants.md` with the live failure mode. BL-183's second check (CA-JSON table references in `ts snowflake lint-ddl`) remains open
- fix: **a fourth defect of BL-178's family, caught in PR review: a RENAMED passthrough was unreachable by its declared name.** Indexing a construct only under the physical column it aliases left `STORE_SALES.revenue as store_sales.ss_ext_sales_price` unreachable as `store_sales.revenue` — the reference missed both indexes, fell through to the assumed-physical branch and emitted `column_id: STORE_SALES::revenue`, a column that does not exist. I13 cannot catch it (a `TABLE::col` reference, not a `[formula_*]` one), so it fails at import with every gate green — the same silent-until-import shape as BL-178 itself, one step to the left. Each construct is now indexed under **both** its declared name and its physical column, declared name winning on collision, and the rules file states the rule with both failure modes. Where the two readings genuinely disagree — a renamed passthrough whose declared name may also be a real column, undecidable without the column inventory the translator never receives — the reference resolves to the semantic view's construct and carries a ⚑ annotation rather than silently aggregating a different column. Also from the same review: every double aggregation now emits the 🔄 review marker `ts-from-snowflake-rules.md` has always mandated and nothing ever produced; a degenerate grouping (the inner metric aggregating the very column the relationship implies as the grouping key — `group_count([X],[X])`, one row per group) is skipped and flagged instead of emitted; a passthrough *metric* is a reasoned skip rather than a raw `AttributeError`; and `tml_lint`'s I13 now shares `check_tml.py`'s empty-declared-set guard so the same TML cannot lint differently in the CLI and in CI
- docs: **BL-194** — same-table metric-on-metric has no resolvable reference. The `[formula_<id>]` fallback resolves only when the inner metric becomes a `formulas[]` entry, so a same-table ratio of two simple-aggregate metrics (`ORDERS.aov as orders.total_rev / orders.order_count` — a common shape) dangles, I13 fires and `build-model` exits 1. Loud rather than silent, which is the right direction but not a conversion; the fix is to inline the inner aggregation, the same "inline rather than reference" resolution the Databricks direction already uses. Documented as a limitation in the rules file's step 3 and coverage row 27, with a test that pins the current loud failure so the limitation is visible in the suite rather than only in prose. Invariant lists in six places (`ts-tml-import-gate.md`, both convert-from SKILL.md files, `agents/shared/CLAUDE.md`, `ts tml lint --help`, `tableau/verify.py`) updated from `I1/I2/I4/I5/I8` to include I12 and I13, and the **CoCo mirror's pre-import checklist gained the I13 row** — CoCo cannot run `ts tml lint`, so that checklist is its only gate for this class
- fix: **a fifth defect of BL-178's family, caught in re-review: RENAMED DIMENSIONS were unreachable by declared name.** Indexing facts and metrics under their declared names left `dimensions()` uncovered, so `DM_CATEGORY.CATEGORY as dm_category.CATEGORY_NAME` referenced as `PARTITION BY dm_category.category` emitted `[DM_CATEGORY::category]` against a real column of `CATEGORY_NAME` — **a second worked-example regression**, since `ts-from-snowflake-dunder.md:207` documents the correct form. Dimensions now resolve through the same index with the three-way split `_translate_dimension` itself makes: passthrough → the aliased column, bare-column rename (`CASE_ID as ID`) → the column the *expression* names (not `alias_name`, which is the declared name for that shape), computed → the minted formula id. The computed case was independently wrong before — a computed dimension referenced by name emitted `[TABLE::name]` for something that is a formula. Emitted output now matches the dunder worked example character for character, so two worked examples agree with the converter rather than one. Also: the F6 degenerate-grouping guard's `group_aggregate` full-form arm gained the test it was missing, and the review-flag annotations (⚑/🔄) are now surfaced in the skill's Step 12 report — previously they were produced, tested, and then dropped on the floor, which was the safety argument's missing link
- docs: **BL-195** — `ts snowflake build-model` runs single-document `lint_tml` only and never `lint_cross_references`, so a `column_id`/formula reference naming a column that does not exist on its table is invisible to it. That is the dangling-*column* twin of the dangling-*formula* class I13 just closed, and both fail at import identically; only one had a gate. Compounded by `lint_cross_references` not walking `formulas[].expr` at all, so it would not have caught the dunder repro even if wired in. Both halves specified with the fix approach — the column inventory it needs already exists upstream in `ts snowflake introspect`'s output, it is simply not threaded through. Six more stale `I1/I2/I4/I5/I8` invariant lists corrected (`tools/ts-cli/README.md`, `check_no_inline_tml_gate.py`'s printed hint), two stale line-number citations repointed at the section that actually carries the 🔄 requirement, and the CoCo mirror's `synced-from` marker corrected — it claimed the CLI had advanced by "no logic change", which stopped being true at v1.19.2; `agents/SYNC-DEBT.md` now records the CLI side as v1.19.4 and states which single obligation had a CoCo-side counterpart and how it was discharged
- chore: bump ts-cli to v0.126.0 — BL-178 resolver/parser fixes + `ts tml lint` I13
- fix: **BL-192 — `docs/quality-gates.md` was gated on repo *history*, so it went stale from other people's commits and hard-failed PRs that changed nothing relevant.** The catalog embeds a `git log`-derived date per validator, and `--check` compared the file byte-for-byte, so any commit touching any validator invalidated every open branch's copy. Confirmed on #421: local `--check` passed, CI failed, and the entire diff was one date flipping for `check_tml.py` — a file that PR never went near. Since the gate is a *hard* fail on anything touching `tools/validate/`, `pre-commit.sh` or `validate.yml`, the practical effect was that **regenerating before pushing was not sufficient** and two concurrent validator PRs could each invalidate the other, one wasted CI cycle at a time. `--check` now compares *structure* via `strip_volatile()`, which blanks the `Last modified` cell of every gate row before comparing; generation still renders the dates. A gate row is identified by its second-to-last cell being a known mode, so the document's **prose** tables (enforcement model, audit checklist) pass through unnormalised and stay fully gated — verified, since normalising those would have silently un-gated real edits to them. Audit finding 7.1's actual intent is preserved: a gate-shape change still fails a stale catalog. Four end-to-end proofs against the real catalog: a date-only change now passes, while a gate→nudge downgrade, a deleted gate row, and a prose-table edit each still fail. Seven unit tests pin it, including an assertion that the fixtures genuinely differ so a vacuously-passing test cannot masquerade as coverage. Accepted residual, now stated in the rendered document: the dates refresh only on regeneration and may lag, so `git log -1 -- tools/validate/<validator>.py` is the authority
- docs: file **BL-192** and **BL-193**, both found while shipping `check_backlog_integrity` (#421). BL-192: `docs/quality-gates.md` embeds a git-log-derived date per validator, so it is a function of repo *history* rather than repo *contents* — it goes stale when **someone else's** commit touches any validator, and the PR-scoped hard gate then fails a branch that changed nothing relevant. Confirmed on #421: local `--check` passed, CI failed, and regenerating against the merged history produced a one-row diff for `check_tml.py`, which the PR never touched. The consequence worth knowing is that **regenerating before you push is not sufficient** — it must be regenerated against the merged history, and it re-stales the moment main touches a validator, so two concurrent validator PRs can each invalidate the other. BL-193: `.git/hooks/pre-commit` is a relative symlink in the *common* git dir, so it resolves into the **main checkout's** working tree — a `git commit` from any worktree runs whatever branch that checkout has out. Verified live: while shipping #421 from a worktree, the main checkout's script had zero matches for the check #421 was adding, so a commit with a planted violation succeeded. CI remains a strict superset, so nothing ungated reaches main; the cost is a slower loop and false confidence, since "the hook passed" is not evidence about your branch's gates
- feat: `check_backlog_integrity.py` — three merge-integrity gates on `docs/backlog.md`: no duplicate `## BL-NNN` heading (and no id sectioned in both the live backlog and the archive), no `BL-NNN` cited under `agents/`/`tools/`/`.github/` that resolves to nothing, and no git conflict markers in tracked files. Prompted by the second BL-number collision (BL-171 in #356; BL-150 was the first): two branches each claimed the next free number, and the natural "accept both" resolution produced two `## BL-171` sections that passed all 23 pre-commit checks and all 5 CI jobs, because nothing validated BL-number uniqueness. That matters more than a docs nit since `BL-NNN` is cited ~1,000 times across ~230 files, so a duplicate silently changes what every one of those citations means. `docs/` is deliberately outside rule 2's scope: specs and audit reports are point-in-time records, and a spec citing the numbers as they stood is correct as history. **One half of the #356 near-miss is deliberately NOT gated** — a stale `**Target:**` line that git auto-merged onto the adjacent item, above the conflict region. Tested: after an accept-both each duplicate section carries exactly one Target line, so no count-based rule fires, and renumbering to satisfy rule 1 leaves the misattachment in place. Mitigation is the `CLAUDE.md` resolution recipe added alongside, and the limitation is recorded in the validator's own docstring so nobody later assumes coverage it does not have
- docs: add `docs/skill-personas.md` and a "Which skill do I need?" routing table to `README.md` — the "why does this skill exist / who is it for / when do I reach for it" layer, which existed nowhere in the repo before (zero hits for `persona`/`business problem`/`audience` across every `SKILL.md`, `docs/` and `CLAUDE.md`; only 5 of 28 CLI skills carried a `When to use` section). Routes by job-to-be-done rather than skill name, so someone who does not yet know the catalogue can find a starting point. Written for the 2026-08-06 field technical enablement session. Deliberately kept out of `description:` frontmatter, which drives skill triggering and is budgeted by `check_skill_context_cost.py`; **BL-188** tracks moving `personas:`/`use-when:` into frontmatter and generating both surfaces so they cannot drift.
- docs: **remove the README "Which skill do I need?" routing table**; the category summary table now carries a one-line *problem it solves* per category instead. At 18 rows the routing table restated the skills catalogue immediately below it in a second format, so it read as duplication rather than orientation. The six-row category table gives the same framing at a fraction of the length, and replaces its `What it covers` column, which was already repeated verbatim in each category's `<summary>` line. `docs/skill-personas.md` is unchanged and remains the per-skill why/who/when layer, now linked from the Skills intro. BL-188 narrowed to that page alone.
- docs: link the routing table and skill personas from all three runtime setup guides (`agents/cli/SETUP.md`, `agents/coco-snowsight/SETUP.md`, `agents/databricks/SETUP.md`) — the links previously existed only in the root README, so anyone arriving via a direct setup-guide link never saw them. The Snowsight and Databricks pointers also state that runtime's skill scope, since the routing table lists mostly CLI-only skills.

## 2026-07-29
- feat: `ts tml verify-render <liveboard>` — verify an imported Liveboard actually RENDERS, not merely that it imported. A hand-authored or mis-bound answer can import cleanly yet fail at query time with `No data source found for the query`; this calls `metadata/liveboard/data` and, on failure, re-probes each tile to name the offending visualization. Exit 0 if it renders, 1 otherwise, so a conversion skill can gate its import step. Live-verified against a broken vs a working board. (powerbi render-robustness, #356)
- feat: `ts powerbi build-timeintel` — rebuild flagged SAMEPERIODLASTYEAR / YoY measures as Reference-Date `sum_if` measures (the live-verified measure-based pattern from `worked-examples/powerbi/sply-parameter.md`), merged into the model TML. Measure-based, so referencing tiles render — unlike a hand-authored period-comparison tile; each formula carries `id: formula_<name>` matching its column. Never invents numbers (each set surfaced for human verification). Live-verified: generated measures import and searchdata 200. `ts-convert-from-powerbi` v1.1.0 requires `verify-render` as an import gate and resolves the time-intelligence hard tail into the model before the liveboard, closing the "imports but shows blank tiles" gap a cold CSM run hit. (powerbi render-robustness, #356)
- chore: bump ts-cli to v0.125.0 — `ts tml verify-render` + `ts powerbi build-timeintel`
- fix: **from-Snowflake conversions since 2026-07-22 emit Models whose measures do not resolve** — found by a TPC-DS conversion-fidelity cross-validation against apache/ossie's community-reviewed fixtures (`docs/reviews/2026-07-29-ossie-tpcds-fidelity.md`). Round-tripping upstream's TPC-DS Metric View and Cortex-Analyst model through our four Snowflake/Databricks converters scored **9/15 constructs matched on the Databricks leg** and **10/47 on the Snowflake leg**, with **every gate reporting clean on every finding** — `ts tml lint`, `check_tml.py`, `build-model`'s own `lint_findings` and `ts snowflake lint-ddl` all pass on a Model TML in which 5 of 5 metric formulas reference formula ids that were never declared (**BL-178**, a three-defect regression against a live-verified worked example, likely from from-snowflake-sv v1.17.0's 2026-07-22 rewire). Second-highest: from-Databricks emits `type: INNER` for joins Databricks documents as LEFT OUTER, so measures read **lower** in ThoughtSpot than in Databricks (**BL-174**). 21 findings + 18 doc gaps routed: **11 new backlog entries** (BL-174–BL-184, four Tier 1, three *ready to fix*), **18 coverage-matrix rows corrected in-branch** across the two from-direction matrices (PATCH bumps: from-databricks-mv v1.10.3, from-snowflake-sv v1.19.3), and **two validator promotions** — a dangling-`[formula_X]` check for `ts tml lint` (would have caught BL-178 the day it landed) and a worked-example reproducibility test. No converter code changed in this wave; upstream-converter findings are parked pending the #285 legal review and nothing was posted externally.
- fix: **BL-170 — six "native" ThoughtSpot string functions are not native at all, and `in` needs curly braces** (live-verified on se-thoughtspot via 20 `VALIDATE_ONLY` formula probes). `trim`, `ltrim`, `rtrim`, `replace`, `starts_with` and `ends_with` are **all** rejected by the formula parser with `Search did not find "<fn> ("` — the same signature `upper`/`lower` produce — and `[col] in ( 'a' , 'b' )` is rejected in favour of `in { 'a' , 'b' }`. `thoughtspot-formula-patterns.md`, the file CLAUDE.md treats as formula ground truth, was wrong on all four BL-170 questions; the Tableau mapping (which had live-confirmed `replace`/`starts_with`/`ends_with`/`in` earlier) was right. `trim` was the bonus find — it was used as a known-good *control* and failed. Corrected across 6 shared references (formula-patterns, ts-snowflake, ts-databricks + its `agents/databricks/` mirror, qlik, looker, tableau) and 3 coverage matrices; the corrected native string set is `concat`, `substr`, `left`, `right`, `strlen`, `strpos`, `contains` and nothing else. PATCH bumps on the 7 affected conversion skills. **The docs are fixed but 5 ts-cli emitters still emit the bare names — filed as BL-171 (Tier 1: produces failed imports today).**
- feat: ts-object-model-agentql-query v2.2.0 — full AgentQL limitation re-probe on jul.26.mt (38 live probes): 7 documented limitations now fixed (set-op ORDER BY/LIMIT, aggregated-branch CTE set-ops, ROUND(x,N), TO_NUMBER, CONCAT_WS, LENGTH(), grouped MEDIAN), 2 new bugs filed + documented (SCAL-326935 scalar STDDEV/VAR regression, SCAL-326936 IN-subquery compiles-but-fails), new patterns.md § Semi-join via CTE with the dedupe-the-key fan-out guard
- fix: **`ts migrate rollback` was single-shot — a re-run after a successful delete crashed on the API's untyped-identifier 400** (ts-cli v0.124.2; found by the live verification of audit fix 17.1 on nebula-damian-alias, the step the unit tests' too-kind mock missed). The type lookup queried `metadata/search` with identifier-only entries; live, that returns **400 `Specify the metadata_type for identifier ...`** for any guid that no longer resolves — which is every guid on a re-run — while a **typed** search returns 200 with the misses simply absent (probed live both ways). The lookup now runs one batched search per deletable type (ANSWER, LIVEBOARD, LOGICAL_TABLE); the test double now encodes the API's real semantics (untyped+unresolvable → 400) so the too-kind mock cannot regress, and a new test pins the exact live failure: full re-run after everything was deleted → 4× "already gone", zero delete calls, clean exit. The full live pass this came from: 17.2 multi-model refusal ✓, 17.6 self-scan ✓, 17.4 scoped-gate refusal with an unrelated per-Org variable planted as false-pass bait ✓, apply → per-object share grants (12 across 3+3 objects) ✓, rollback delete with correct types ✓
- feat: add check_internal_imports, check_repo_hygiene validators + ts_cli-import skill gate (audit 2026-07-29)
- fix: **`ts migrate apply` share grants are per-object, and the Set-blocker refusal no longer depends on the operator remembering a flag** (ts-cli v0.124.1; audit 2026-07-29 findings 17.5/17.6). `run_share_grants` applied the UNION of every source object's groups to every migrated content object — an Answer shared only with Finance became visible to HR if any other migrated object was shared with HR; content now gets exactly its own source object's groups (matched by name, the key both `wanted` and the ledger already share), while the shared Table → Model → View stack keeps the union (every group needs the whole chain or its content grant is silently dropped — BL-150). And a bare `apply` with no `--sets-scan` now runs the cohort-column scan itself (one LOGICAL_COLUMN search per Org) instead of treating "not scanned" as "not blocked" — the docs promised "refuses, no override" but the refusal only fired when the flag was passed, and cohort columns are invisible in TML so the Set dropped silently. Six tests pin both
- fix: **three scope bugs in `ts migrate apply` — the operation reached wider than the thing being migrated** (ts-cli v0.124.0; audit 2026-07-29 findings 17.2/17.3/17.4). (1) **A multi-Model mapping is now refused**: `audit --all-models` writes one CSV across every Model by design, but `apply` binds all rewritten content to ONE published target — Model B's content landed on Model A's master, importing cleanly and rendering wrong. (2) **`repoint_source` now rebinds only the migrating Model's `tables[]` entries** (by source guid, name-fallback when the fqn is absent): a mixed dependent reading the migrating Model AND another source had its OTHER reference rebound to the published master too; the content batch also gains the View remap `move_shielded` already had, so a mixed dependent's View half follows the rewritten View instead of dangling. (3) **The tenant-isolation gate is scoped to the variables the target's tables actually bind** (the `${var}` tokens in their exported TML, parsed by the same helper `ts publish export` clusters on): the unscoped read let ANY unrelated programme's per-Org variable yield SEGMENT_PHYSICAL and skip the no-RLS refusal exactly where every tenant reads the same rows; readable-but-unparameterized tables now read SEGMENT_SHARED outright, connection-bound variables are documented as not attributable from table TML (fail-closed → `--allow-unfiltered-target` is the deliberate override). Ten tests pin the three fixes
- fix: **`ts migrate rollback` was dead code — rebuilt against the ledger `apply` actually writes** (ts-cli v0.123.1, audit 2026-07-29 finding 17.1). The command still read the retired eight-step executor's ledger (`created[lift_content]`, `kinds.models`) and imported step constants the three-step rewrite (PR #367) deleted — a **function-local** import, so `ts` loaded, every test passed, and the documented undo path of a destructive migration crashed with `ImportError` on any invocation, `--dry-run` included. Now: the delete-set comes from `rewrite_content` + `move_shielded` (content) and `rewrite_views` (Views), content deletes before Views with the **correct metadata type per object** (the old code sent `LOGICAL_TABLE` for everything, which cannot delete an Answer), already-gone objects are skipped so a partial rollback re-runs, and a **same-Org ledger is refused outright** — those guids are the tenant's originals updated in place, and the only undo there is `plan/backup/`. `apply` now records its import mode in the ledger so rollback can tell the topologies apart; pre-mode ledgers are refused conservatively on equal Org names. Ten tests pin all of it — the command previously had zero

## 2026-07-28
- docs: **the alias step is confirmed in the UI** — an ORG1 session shows `Segment` on the published master (repo owner, 2026-07-28). That closes `ts migrate aliases` end to end: derive from the approved mapping → gate → merge → import → **a user in the migrated Org sees the tenant's own column name**. There is no programmatic oracle for this, so it could only ever be someone looking. Two things learned in the confirming: **which Org the session was in is load-bearing** — every migrated tenant aliases the same physical column to its own name, so "I see `Segment`" was ambiguous between ORG1 (this wave) and ORG2 (the earlier run) and would have proved different things; and **for aliases the admin caveat does not apply** — `TS_WILDCARD_ALL` scopes to everyone in the Org, admins included, so an admin session matches the same pathway a tenant user does, which is the *opposite* of a sharing check. The README previously said to verify as a non-admin, which was wrong for this one check. Still only document-level: that ORG2's pre-existing aliases *render* after the merge (their presence is proven by the round-trip export)
- feat: **`ts migrate aliases`** — the per-wave alias step, the last unbuilt piece of migration Phase D (ts-cli v0.123.0). Step 7 was **prose telling an operator to check four things by eye**, one of which is the only genuinely catastrophic action in the routine. Alias load is full-document with **no delta update until 26.10**, so the merged document *replaces* what the Model carries: an export that came back partial silently strips every already-cut-over Org it missed, and those users see `STRING_1` where they saw `Region` — with no error, because each entry left behind is individually valid and the import returns `OK`. `--expect-org` turns that into an assertion and **is not optional** (`--first-wave` is the explicit alternative), because a check that defaults to off is not a check. Coverage is checked **by Org, not by count** — a count is satisfiable by the wrong Orgs, and ten aliases for one tenant would pass "ten or more" while nine tenants are wiped. The aliases themselves are **derived from the approved `column-mapping.csv`** as the exact inverse of the rename `apply` performed, removing a transcription step whose mistakes are silent (a misspelled column aliases nothing). The command **emits the envelope `ts alias build --merge` already consumes**, so the transform, the size ceilings, the async path and the overlap refusal stay in `ts alias` rather than being restated. **Verified live:** with ORG2 already aliased, adding ORG1 preserved ORG2 and added ORG1 (round-trip export confirms all four entries, all `TS_WILDCARD_ALL`); claiming a never-aliased ORG3 was cut over is refused by name; re-running produces a **byte-identical** document. What API verification *cannot* cover: that an ORG1 user actually sees `Segment` — aliases render only in Search Data, Answers, Liveboards and Spotter, so a human must look
- chore: bump ts-cli to v0.123.0
- fix: **`ts share status --org X` could not resolve an object native to Org X** (ts-cli v0.122.0, **BL-153**). It resolved its targets with an **Org-less** client and scoped only the *permissions read* per Org — but `metadata/search` is Org-scoped, so a tenant's own Answer or Liveboard is invisible to the default-Org client and simply cannot be resolved. The command failed with `Could not resolve '<guid>'` against a perfectly valid GUID, with the Org named on the command line, which reads as "you typed it wrong" rather than "I looked in the wrong Org" — the same misdirection as BL-147. It breaks the read-back half of the pipeline exactly where a migration needs it: confirming migrated content in a tenant Org carries the grants it should. **The same bug was already fixed in `ts share export` on 2026-07-27** — which is why `_resolve_object_in_orgs` exists; `status` was missed then. Second defect fixed alongside: the untyped resolution probe 400s on **every** tenant-Org lookup, and although `_try_search` swallowed it, `client.py` had already printed `ThoughtSpot API 400 ... Specify the metadata_type` to stderr — so a *successful* report came with a red API error in front of it. `_search` gained a `quiet` flag; only probes use it, and a genuine failure is still loud and fatal. **A trap worth knowing:** `_try_search`'s blanket `except (Exception, SystemExit)` converts programming errors into empty result sets — adding the kwarg broke five test doubles and the resulting `TypeError` surfaced not as an error but as "found nothing"
- chore: bump ts-cli to v0.122.0
- fix: a **same-Org migration paired the tenant's Model with ITSELF and reported `READY`** (ts-cli v0.121.0, **BL-152, Tier 1**). Found running the first end-to-end same-Org test — the last of the three supported topologies. `discover.find_model_by_name` returned the **first** name match, and in that topology the Org legitimately holds **two** Models of the name: its own, and the master published in from Primary. It returned the tenant's own, so the audit compared the source with itself — all 6 columns trivially `MATCHED`, an empty rename map, `validate_apply` with nothing to object to, verdict `READY`. **A silent no-op that passes every gate:** `apply` would run green, write a state ledger, and move nothing; verification would not catch it either, because the content still works, still pointed at the Model it always used. It surfaces only when the "old" Model is retired at teardown, breaking every Answer and Liveboard in the Org at once. **Ownership is the discriminator** — `metadata_header.ownerOrgId`, the same field `list_models` already used for the fleet scan, applied to the one place it was missing: `select_source` requires the Org to OWN the Model, `select_target` requires that it does not and excludes the source GUID outright. Either **refuses** on genuine ambiguity rather than picking, since both wrong answers are silent. Three narrower instances of the same name-only assumption were fixed alongside it: `audit --all-models` swept the master in as a *source*, `apply`'s source lookup could return the master and rename ITS columns, and `scan-sets --model <name>` could report the master as the tenant's Set blocker. `apply_plan.find_self_repoint` re-asserts the invariant at plan time — the previous architecture had this guard, the rewrite dropped it, and the case came straight back. The regression tests were **confirmed to fail against the old behaviour** with the bug's exact signature before being taken as passing
- chore: bump ts-cli to v0.121.0
- docs: two items had been filed as **BL-150** on the same day. The migration-sharing one is cross-referenced from code, tests and a SKILL.md, so it keeps the number; the `ts-security-columns` skill item is renumbered **BL-151**
- feat: `ts alias build` now **refuses overlapping alias scopes** (ts-cli v0.120.0) — codifying the finding rather than only documenting it. A user matching both a `TS_WILDCARD_ALL` entry and a group entry for one column sees the **base column name**, not either alias, and **identical alias values do not help**. Nothing else catches it: every entry is individually valid, the import returns `status_code: OK`, and the export looks correct, so the only symptom is tenants seeing generic names — which reads as a broken migration rather than an alias-scope collision. Refused rather than warned, because a group scope does **not** override a wildcard — **verified by live experiment** across four cases: wildcard-only and group-only both render correctly, while wildcard-plus-group renders the base name whether the two aliases differ or match. Mixed strategies across *different* columns stay permitted, since the rule is per (column, locale, org). Also records that aliases render **only** in Search Data, Answers, Liveboards and Spotter — not in the Data Management app, nor via `metadata/answer/data` — so verifying in either place shows base names for everything and looks like total failure
- chore: bump ts-cli to v0.120.0
- docs: record three alias-scoping facts learned live, and the design consequence. **An ambiguous alias resolves to the BASE column name** — a user matching both a `TS_WILDCARD_ALL` entry and a group entry for one column sees `STRING_1`, not either alias, and **identical alias values do not help**. Every entry stays individually valid, the import returns `OK`, and the export looks correct, so the only symptom is tenants seeing generic names. **Org-wide scope is `group: TS_WILDCARD_ALL`**; an empty group is rejected with `Group with name not found in org`, and substituting an arbitrary real group to get past that error is exactly how the overlap gets created. **`--merge` cannot remove an entry**, so correcting a bad scope needs a non-merge rebuild that silently drops anything absent from the input. Consequence for the per-wave alias step: the specified fail-closed **count** assertion is necessary but not sufficient — it must also **refuse overlapping scopes** for any (column, Org) pair
- fix: `share_grants` now grants the **whole object stack, bottom-up** (ts-cli v0.119.0), and **BL-150 is resolved as our bug rather than a platform gap**. `nebula-damian-alias` runs **Strict Object Mode** — a **per-cluster setting**, so confirm it before reusing this explanation elsewhere — and with it on, a user needs an explicit grant on the entire chain — and a grant on content whose source is ungranted is **accepted and silently dropped**: `HTTP 204`, no row recorded. Sharing only the content therefore produced a migration that reported success while the tenant saw nothing. Compounding it, the *published* Model carries no tenant grants of its own, because publication makes an object **present, not visible**. Grants are now applied published Tables → published Model → migrated Views → content; the order is load-bearing at every layer, and fixing only the Model layer made the identical bug reappear one level up at the View. Granting the stack is **safe either way**, since with the mode off the extra grants are merely redundant, so nothing detects the mode. Verified live: all six objects in the target now carry the group grant where four did not. Retracts the previous entry blaming `fetch-permissions` — **that read was accurate every time**
- chore: bump ts-cli to v0.119.0
- feat: audit angle 18 — **harness/framework currency**: the external sweep now audits the Claude setup itself (settings pins, agent model/effort tiers, workflow capabilities, rules-file anchors) against the current Claude Code + model lineup; same pattern as angle 13, pointed inward
- docs: `docs/coding-with-claude-quickstart.md` — the framework's portable layer as a staged adoption guide for other teams
- feat: pre-commit timing budget — per-check durations printed inline; suite total warned against a 15s budget with slow-check attribution
- feat: add `check_skill_context_cost.py` validator — gates the estimated-token size of every SKILL.md (warn >12k, fail >25k), since a skill's file is loaded into model context on every invocation and its size is a recurring token tax (BL-128). At introduction: median skill ~4.9k est. tokens, 11 warnings, one allowlisted offender (ts-convert-from-tableau at ~57k)
- feat(harness): encode model/effort routing (`.claude/rules/model-routing.md`) — mechanical agents (consistency-checker → haiku, repo-publisher → sonnet) and low-effort mechanical audit finders; drop the stale model pin from `.claude/settings.json`
- docs: enforcement model corrected to CI-hard — branch protection has `enforce_admins`; `--admin` merges bypass the review requirement only, never CI (verified on PRs #381/#383/#384)
- docs: correct **BL-150** again — **there was no sharing bug.** `share_grants` works: the repo owner confirmed in the UI that the migrated Answers in ORG2 carry `can_view` for the intended group. The recorded failure came from trusting `security/metadata/fetch-permissions` over the product — it did not report that grant for an `ANSWER` in ORG2, while reporting it correctly for an `ANSWER` in ORG1 and a `LOGICAL_TABLE` in ORG2. So the real finding is much narrower: **`fetch-permissions` under-reports group grants**, which makes API-based verification of sharing unreliable for that combination. The `HTTP 204` was accurate throughout. Two earlier hypotheses recorded in that entry (published-Model dependency, then object type) were both wrong and are retained as corrections rather than deleted
- docs: correct the **BL-150** diagnosis. The note blaming the published Model was **wrong**: a control Answer built on ORG2's *own* Model fails identically, and an ORG2 Model share to the same group succeeds. Also rules out a missing group (`MIGTEST_VIEWERS` is present in ORG2 with a member, and its own `orgs` field is the authority), name-vs-GUID resolution, and an empty group. What the evidence points at instead: the failures are `ANSWER`/`LIVEBOARD` **in ORG2**, while a `LOGICAL_TABLE` in the same Org with the same group works — and the same types share fine in ORG1, which is unexplained and is the next thing to chase
- feat: `ts migrate apply` gains a `share_grants` step (ts-cli v0.118.0). **TML carries no sharing information at all** — no `share`, `permission`, `principal`, `group` or `acl` key — so migrated content is authored by the migrating admin and visible to nobody else: the migration completes, every check passes, and not one tenant user can see anything (**BL-150**). That failure survives verification, because an admin sees objects regardless of sharing. The step reads the source objects' **group** grants and re-applies them in the target, reusing `share_plan.permission_rows` rather than a second implementation. Group level only, since a per-user grant cannot reliably be applied before cutover. **Caveat, recorded honestly: the step runs and reports success but the grants do not register when the target content sits on a published Model** — sharing ORG1 content to an ORG1 group works, the ORG2 equivalent returns 204 and does nothing. Wrong-Org, name-vs-GUID and empty-group causes are all ruled out. Until resolved, treat sharing as a manual post-migration step
- chore: bump ts-cli to v0.118.0
- fix: the segmentation check read variables through an **Org-scoped** session, which inverted its answer (ts-cli v0.117.1). An Org-scoped `template/variables/search` returns only *that* Org's value, so from inside the target every variable looked like it had one value and every deployment was classified `shared` — meaning a correctly segmented tenancy, with each Org pointed at its own schema, was still refused for lacking RLS it does not need. Same trap as `tenancy._groups_in_org`: an Org-scoped read is a **filtered view**, and treating it as complete inverts the conclusion. Now read through an unscoped session, since segmentation is a cross-Org question that cannot be answered from inside one Org. Verified both directions live: with ORG2 pointed at a different real schema the migration completes with no override, and with the shared value restored it refuses again
- chore: bump ts-cli to v0.117.1
- fix: **View-shielded content was silently dropped in a new-Org migration** (ts-cli v0.117.0). A View shields its content from the *column rewrite*, not from the migration — but `apply` excluded shielded objects entirely, so in a new-Org run the tenant's Answer simply did not exist in the target. Found by building a fixture with a View and an Answer on it: ORG2 received the View, the Liveboard and the Model-based Answer, and nothing else. Adds a `move_shielded` step that copies shielded content **without touching its columns** and repoints its `fqn` at the newly-created View — it runs last because it needs the guids the View step created. In a same-Org run the step is empty, since the content stays put and the repointed View keeps working underneath it. Shielded content is now also included in the backup either way, because it is in scope whether or not it moves
- chore: bump ts-cli to v0.117.0
- fix: rewrite **qualified `Source::Column` references** (ts-cli v0.116.2). A second scan of real content found 82 of them across 45 Liveboards — in `filters[].column[]`, `ordered_chips[].name`, `view_filters[].column[]` and `parameter_overrides[].value.name`, the last of which the first scan never surfaced because it only ever holds the qualified form. The transform matched whole strings, so `Retail - Apparel::Product Type` was left pointing at the tenant's column: on four real Liveboards, **42 filters would have survived the migration still bound to the old Model**. **The coverage gate had the identical hole and reported those documents clean**, which is worse than no gate — it converts an unknown into a false assurance. Both fixed together. Only the column half is rewritten; the source half is the Model name, which the migration does not change
- chore: bump ts-cli to v0.116.2
- fix: the tenant-isolation check is now **segmentation-aware** (ts-cli v0.116.1). It demanded row-level security unconditionally, but **RLS only separates tenants when publication resolves every Org to the same physical data**. A `TABLE_MAPPING` or `CONNECTION_PROPERTY` variable holding a different value per Org already points each tenant at its own database or schema, and a `USER_PROPERTY` variable resolves per principal — in both cases the tenants are separated and demanding RLS on top is a false alarm. Worse than no check: it teaches operators to pass `--allow-unfiltered-target` reflexively, which destroys the check for the case where it genuinely matters. `target_segmentation` reads the publication variables' per-Org values (`METADATA_AND_VALUES`) and the refusal now fires only for genuinely shared data — offering the segmentation route alongside adding RLS, since an operator told only about RLS may add it where pointing the Orgs at different data is the better answer. Unknown segmentation still refuses
- chore: bump ts-cli to v0.116.1
- docs: add `running-a-migration.md` — the complete operator reference for `ts-migrate-orgs`, and the piece that did not exist. Covers the sequence, the three topologies, **what a human must decide** (filling `GAP_BLOCKER` rows is the only judgement step, and a wrong mapping does not error — content silently reads the wrong column), **every check the tooling makes** and why each refusal is the system working, the **Python API** for embedding the engine directly, and a decision table pairing each choice with the alternative rejected. Also refreshes the two documents that still described the deleted lift-and-shift flow: the `ts migrate apply` section of the ts-cli README, and the end-to-end verification record, which recorded a run that failed at `lift_scaffolding` and now records one that completes and returns data
- feat: `ts migrate apply` is rebuilt around **export → rewrite → import** (ts-cli v0.116.0). Three steps where there were eight: the scaffolding lift, the Model-column rename, the repoint and the cleanup all existed to *avoid* rewriting content, and BL-148/BL-149 showed the avoidance was never possible. Views are rewritten first, preserving what they expose, so content built on them is never in the content batch at all. Each object runs the **coverage gate before being written** — a partial rewrite imports cleanly and renders wrong, so it is caught at plan time rather than by a user later. The three topologies (same Org, new Org same cluster, new Org different cluster) are one code path: the write mode is **derived**, not configured, and cluster needs no handling because it is a property of the profile. Verified end to end on `nebula-damian-alias`: an Answer migrated ORG1 → ORG2 now points at the published Model, uses the published column names, and **returns data**
- chore: bump ts-cli to v0.116.0
- feat: `ts migrate audit` now classifies each dependent by **what it sits on**, and follows dependents **through Views**. Two changes that go together. Classification: content on a View costs nothing to migrate (repointing the View preserves the names its dependents see), so the rewrite count and the object count differ — often by a lot. 200 Answers over 4 Views is four repoints; 40 Answers straight onto the Model is forty rewrites, and the object counts point the wrong way. The report now leads with the rewrite count, names the Views to repoint, and warns separately about content sitting **directly on a Table**, which a Model-level change never reaches. The walk: `_collect_dependents` is single-hop, so everything a View shielded was **invisible** to the audit — those objects need no rewriting, but they are exactly what breaks if a View is missed, so hiding them understated the blast radius. `dependents_through_views` follows Views only (depth-capped and cycle-guarded), tagging each row with the View that shields it. Also fixes `subtypes_by_guid`, which swept every `LOGICAL_TABLE` on the cluster per call — indefensible inside a dependent walk — to query by identifier
- chore: bump ts-cli to v0.115.0
- feat: add `ts_cli.migrate.rewrite` — the transform the whole Org migration reduces to. For each content object exactly two things change: the **data-source reference** and the **column names**. Everything the previous architecture did (lift scaffolding, rename Model columns, repoint) existed to avoid writing this, and BL-148/BL-149 showed that avoidance was never possible, because content TML has **no physical anchor** — a Liveboard references columns purely by display name, including `table_columns[].column_id`. Handles all three reference shapes found in real content: whole-string, bracketed tokens (preserving query syntax like `[date].'last 12 months' top 5`), and aggregation/bucket decoration (`Total Segment` → `Total STRING_1`). `client_state_v2` is **parsed** rather than string-replaced, since a substring pass over that blob corrupts unrelated chart state. Views are the special case that pays off: `rewrite_view` preserves `view_columns[].name` while rewriting what the View *reads*, so **every Answer and Liveboard built on a View needs no migration at all**. Ships with `residual_references`, the completeness gate — the failure mode of a partial rewrite is an object that imports cleanly and renders wrong, so coverage is an assertion rather than a judgement. Verified clean against **64 real Liveboards and Answers** on se-thoughtspot
- chore: bump ts-cli to v0.114.0
- fix: replace a **dead** BL-144 guard in `ts migrate apply` with the check that actually protects tenants. The old guard re-read `rls_rules` after a write — but it looked for `doc["table"]["rls_rules"]` while only ever running on a *Model* document, which has no `table` key, so it could never fire. It was also guarding a case that does not arise: the only Table TML `apply` writes is disposable scaffolding, deleted at cleanup and never queried. In its place, the **repoint** now refuses to bind tenant content to a published Model whose tables carry no row-level security — every tenant would see every other tenant's rows, which is the worst outcome the programme can produce and is silent. An unreadable check refuses too, since not knowing whether a shared Model filters is not the same as knowing it does. `--allow-unfiltered-target` covers a deliberately single-tenant target, and does not override the unreadable case
- chore: bump ts-cli to v0.113.2
- fix: `ts migrate audit` read the **wrong Org** when given an Org name rather than a numeric id (**BL-147**). `auth/token/full` silently ignores a non-numeric `org_identifier` and falls back to the caller's default Org, so the audit read Primary while believing it read the tenant. The read/write distinction was the mistake — the audit produces the `column-mapping.csv` a **human approves**, so a wrong-Org read is not "recoverable", it is a plausible mapping for objects that are not the tenant's. `_assert_write_org` becomes `_org_client` and every command in the group now goes through it, reads included. The inline comment asserting a name was safe is replaced with the reason it is not
- fix: `ts publish apply` created state **before** its cohort gate (**BL-146**). It created the template variable and parameterized the field, then refused on the cohort column — leaving both behind, so the re-run died at variable creation with `HTTP 409 Duplicate template variable name` and sent the operator hunting a variable conflict instead of the Set that blocked them (recovery needed a manual `unparameterize`, since a bound variable cannot be deleted). The gate now runs beside the existing coverage-gap check, before any client is constructed, and says that nothing needs cleaning up. `export` already computed `cohort_columns`, so the check is free
- chore: bump ts-cli to v0.113.1

## 2026-07-27
- feat: add `ts-migrate-orgs` skill — the guided path over `ts migrate`, and the counterpart to `ts-publish-orgs`. Publishing distributes a governed master outward and changes nothing that exists; migration runs the other direction, taking the copies a tenant already has, moving their content onto the published master, and deleting the originals. Establishes a new **`ts-migrate-*`** naming family for that direction (destructive cutover across two Orgs with a state ledger and rollback), distinct from `ts-dependency-*` which rewrites the graph *within* one Org as an operation. The skill is organised around one question — what is the state if this stops halfway — because the answer is what makes each step recoverable: the tenant's source Org is never touched until cutover and IS the rollback, and the target Org has no users until the final step so any failure is abandoned by deleting it. Documents the two outcomes that look like errors and are not (a scaffolding Model that refuses to delete is the missed-repoint check; a failed RLS assertion means the table is unfiltered *right now*, per BL-144), and why the alias merge is per wave rather than per tenant
- feat: add `ts migrate apply` and `ts migrate rollback` — Phase 2 of the Org migration, moving one tenant's bespoke content onto the governed published Model. Runs backup → lift scaffolding → lift content → rename → repoint → ordered cleanup, resumable from a state ledger keyed on step **name** (not index, so a plan regenerated after a mapping edit still skips completed work). **Cutover is deliberately excluded**: users move only once the Org is verified in its final state, and until then the rollback is the untouched source Org. Three rules in the step order encode findings that cost live verification — cleanup runs Models → Tables → connection because connection deletion does not cascade; a scaffolding Model that refuses to delete is the **missed-repoint check** rather than an error to force past (a wholesale Org delete would take un-repointed content silently); and any write to a table carrying RLS is **re-read and asserted**, because a malformed rule imports `OK`, is discarded, and destroys the rule already there (**BL-144**). `apply` also refuses a rename map that is not injective or whose target name already exists in the Model — collisions cannot arise by design, but the map is generated and the failure is silent, since a rename cascades to every dependent automatically
- chore: bump ts-cli to v0.113.0
- fix: `ts publish rollback` aborted partway and stranded the steps after the failure. `client.post` raises `SystemExit` on non-2xx, so any refusal from the first `unpublish` meant the later `unparameterize` and `delete_variables` never ran — leaving the fields parameterized and the created variables behind, which is the opposite of a rollback. Two refusals turn out not to be failures at all and are now classified as such (`publish_apply.classify_unpublish_failure`): an object **already unpublished** (a re-run, or an operator finishing one by hand) is a no-op, and a **Connection grant another published object still needs** is retracted-object-only, since the grant is not ours to remove while someone else depends on it. Anything else still fails, and the command now reports every problem and exits non-zero rather than dying on the first. Both cases observed live 2026-07-27 while tearing down the RLS fixture
- chore: bump ts-cli to v0.112.1
- fix: `ts alias import` and `ts tml import` reported SUCCESS on a failed import (**BL-138**). `metadata/tml/import` returns HTTP 200 even when every item failed — the per-item outcome is in the body, not the status code — so a caller checking `resp.ok` exits 0 having imported nothing. Both now use the shared `tml_import_failures` helper and exit non-zero naming each failed item; `ts tml import` collects them BEFORE its GUID back-fill loop, which skips non-OK items and would otherwise have been the only thing that noticed
- fix: `ts security column-rules export --out` wrote a flat layout while `build --out` namespaces by Org (**BL-140**). The platform's filename comes from the table alone, so exporting two Orgs into one directory silently left the first overwritten by the second — the trap `build` already refuses. Un-scoped exports stay flat
- fix: remove the `CliRunner(mix_stderr=False)` landmine from 22 test modules (**BL-139**). Click 8.2 removed the parameter; the `try/except TypeError` fallback would have silently downgraded every stream-separated runner to a mixing one on upgrade, with no test failing at that point — only confusing JSON-decode errors later. Both runners now live in `tools/ts-cli/tests/runners.py`
- docs: correct the disproven "CSR cannot be defined on published objects" claim in the parent security spec (**BL-141**)
- chore: bump ts-cli to v0.111.1
- feat: add `ts migrate scan-sets` — Phase 0 of the Org migration. Read-only and needs no target Org, so it runs before any clean Org exists. Answers the one question that gates the whole migration programme: **how many tenants actually use Sets**, and which specific Answers and Liveboards would have to be retired or rebuilt. Detection goes through `metadata/search` for `COHORT_*` subtypes and never TML — a Set's column is invisible in the Model's TML, so a TML inspection reports a clean Model that is in fact blocked, and a lift-and-shift would drop the Set silently rather than fail. Reports the denominator alongside the count ("three of twelve", not "three"), because sizing the problem is the point. Adds the `SET_BLOCKER` status, distinct from `GAP_BLOCKER`: it is a property of the Model, cleared only by retiring the Set, which is why `apply` will refuse it with no override
- chore: bump ts-cli to v0.112.0
- feat: add `ts-setup-tenancy` skill — the guided path over `ts tenancy`, completing **BL-137**. Builds one of four scenarios rather than a single fixed environment, because the programme has a before state and an after state and several components can only be tested against one: `topology` (principals only), `per-org` (**pre-migration** — each tenant owns its objects; this is `ts migrate audit`'s input), `published` (**post-migration target** — one Primary Model published out, aliased, secured) and `mixed` (the half-migrated state a cluster occupies for the whole duration of a real migration, where `CSR_BLOCKED`, alias propagation and cohort columns interact). Orchestrates shipped skills rather than reimplementing them, and documents why a test environment and a production onboarding have opposite postures
- docs: document calling the `ts tenancy` planning engine directly from Python — it is pure by design, and that was previously invisible
- feat: add `ts tenancy` (apply / verify / teardown / export) — provision an Org/user/group topology from a declarative spec, closing **BL-137**. Standing up the multi-tenancy pattern needed several Orgs, users assigned to them and **per-Org groups with the right members**, which was undocumented tribal knowledge. The spec validator refuses an incoherent topology at plan time — above all a user joining a group not declared for *that* Org, the mistake that otherwise surfaces as `Invalid group identifiers` mid-apply. `apply` is idempotent, `verify` is one-directional (extra cluster objects are not drift), `teardown` needs the marker AND every Org named with `--org` AND `--yes`, and `export` **captures** a live cluster so the shipped reference topology cannot drift from the environment it describes. Designed for production tenant onboarding too — per-user `account_type` so federated accounts never get a password, and `{TENANT}` templating so one spec serves N tenants — with selective application and a wrapping skill tracked in BL-143
- feat: add the primitives `ts tenancy` needed — `ts orgs create`, `ts users create`, and a new `ts groups` group (`search` / `create` / `add-member`), all per-Org because a group name in one Org is a different principal from the same name in another
- chore: bump ts-cli to v0.111.0
- feat: add `ts-security-columns` skill — the decision layer over ThoughtSpot's two column-security mechanisms. Chooses between column security rules (CSR) and column-level sharing (CLS) **per (Org, object)** rather than per object, because the mechanism is selected by the audience: the same published table needs CSR for its owning Org's users and CLS for a tenant's, simultaneously. Carries a hard confirmation gate for Strict Object Mode (unreadable by any API; CLS grants silently no-op without it), refuses to recommend anything when no mechanism works, and states plainly that an object-level `NO_ACCESS` is not a revoke. Adds a `ts-security-*` skill family mirroring the `ts security` CLI group
- fix: correct `ts-publish-orgs` Step 12, which stated the disproven "column security rules cannot be defined on published objects"; it now explains the Org-scoping and hands off to `/ts-security-columns` (skill v1.1.0)
- fix: `ts share export --org <tenant>` could not see objects native to that Org — it resolved the object and listed its columns with the default-Org client, scoping only the permissions read. Now resolves across the default Org and every `--org`, listing columns through whichever Org could see the object
- fix: translate CSR error code `10038` — a tenant Org refused CSR on an object published into it (the spec documents this as 403; the build returns 500, so the generic 403 fallback never fired). `10023`'s translation now says to check publication before auditing privileges, because the error is state-dependent
- chore: bump ts-cli to v0.110.0
- feat: add `ts security column-rules` (Column Security Rules): the second column-security mechanism alongside `ts share`'s column-level sharing (CLS). Beta, needs 10.12.0.cl or later, and feature-flagged off by default (403/code 10023 until ThoughtSpot enables it). Eight commands: `get`/`export`/`set`/`clear` over the API directly, `resolve` building a plan from a `TS_COLUMN_SECURITY_RULES` manifest, then `apply` (API) or `build` plus `import` (TML) to execute it. CSR declares only the RESTRICTED columns (the inverse of CLS), refuses a published table as `CSR_BLOCKED`, and only unsecures columns absent from a manifest when `--prune` is passed.
- chore: bump ts-cli to v0.109.0
- docs(validate): clear the repo's only clean-tree `check_open_items` FAIL. The `UNVERIFIED` marker was matching prose inside ts-object-model-aggregates item #17's implementation notes, not a status; the flat-shape `rls_rules` gap it described is now its own tracked item (#19, OPEN) with a test procedure and trigger. Also fixed the validator's failure line claiming "in PR-changed files" when `--base` was never passed.

## 2026-07-26
- feat: add `ts share` — object-level and column-level grants over `security/metadata/share` (ts-cli v0.108.0). The sharing capability of the single-model multi-tenancy pattern: publication makes an object *present* in a tenant Org, sharing makes it *visible* to that tenant's users, and the same endpoint carries column-level security. Four commands mirroring `ts publish` — `export` (resolve objects, list column GUIDs, read existing grants per Org), `resolve` (build the manifest from `uniform`/`file`/`db`, resolve names to GUIDs, refuse an unsafe plan), `apply` (batch into the fewest safe calls, one per Org × type × audience), `status` (read back per principal). Three live-verified behaviours the docs get wrong or omit: `message` is **top-level** in the payload, not inside `notification` as every published example shows (the nested form fails with `Variable "$message" of required type "String!" was not provided`); `LOGICAL_COLUMN` **is** accepted despite being absent from the documented supported-types list; and sharing a table grants **every** column in it, so table and column grants are mutually exclusive per (org, table, group) and a manifest containing both is refused — by `resolve` and again by `apply`, because the manifest is a file a human can edit in between. Groups are per-Org, so each `--org` runs through its own org-scoped token and `resolve` names every missing (Org, group) pair at plan time rather than hitting `code 13003` mid-apply. The audience is never inferred: `--group` and `--share-mode` are required with no defaults, and `--notify` is off by default against the API's own default. Live verification also caught three things worth recording: `auth/token/full` **silently ignores** an Org *name* (`org_identifier`) and falls back to the caller's default Org, so `--org ORG1` would have applied a tenant's grants in Primary and checked Primary's groups — names are now resolved to numeric ids and the session's real Org is read back and asserted before any grant; `shared_permission` is **not** the field that shows a share landed (a successful `READ_ONLY` share leaves it at `NO_ACCESS` and sets `permission`), so `status` and its docs now point at `permission`; and `permission_type: DEFINED` returns an empty principal list, which would have made `status` print `[]` for any unshared object. Verified end to end on `nebula-damian-alias` — a table grant measurably conveyed `READ_ONLY` to all 25 of its columns, which is the exclusivity rule's whole basis — and the cluster was returned to baseline with a proven diff. See `docs/superpowers/verification/2026-07-26-ts-share-live-verification.md`. Unblocks `ts-publish-orgs` Step 12 and the column-security skill.
- feat: add the `ts-publish-orgs` skill — the interactive Claude Code skill for the `ts publish` pipeline. Guides object selection (by search, by GUID, or from a CSV/Snowflake manifest, with an optional upward walk to siblings), closure discovery, variable review, target-Org selection, value sourcing, the coverage check, a dry run, the apply-and-publish checkpoint, and verification that the **source** Org is still healthy. Encodes the behaviours live testing established and the docs get wrong: the owner Org must always hold a value or parameterizing breaks the source object; a cohort column anywhere in the closure blocks the Model and everything on it; publish cascades down but never to siblings; retracting siblings that share a Model has one working order. Never uses `--skip-validation`, and refuses to read secrets from a config file. Structure mirrors `ts-object-model-alias` throughout. `tools/smoke-tests/smoke_ts_publish_orgs.py` covers the pure-function pipeline offline; `references/open-items.md` records the one deferred item (substitution *inside* a target Org, blocked on org-scoped token auth) plus four verified platform defects. First entry in the new `ts-publish-*` family.
- feat: manifest-driven publication and an unattended `ts publish run` (ts-cli v0.107.0). Object selection and variable values can now come from a CSV or a Snowflake table instead of the command line, mirroring the `ts alias` convention exactly: one table per concern (`TS_PUBLISH_OBJECTS`, `TS_PUBLISH_VARIABLES`), `--init-table` printing the DDL, `--source db` with `--sf-profile`/`--table`. `ts publish run` chains the whole pipeline with no prompts and a single exit code, for cron or any Python scheduler; it refuses before touching the instance if a variable lacks a value in a target org or a cohort column makes the selection unpublishable, since a partial apply is worse than none. The interactive commands and `run` share one closure builder and one publisher, so the skill and the scheduled path plan from identical data. Live-verified both the refusal and the happy path end to end.
- feat: `ts publish export` takes multiple roots of any type, plus `--with-dependents` (ts-cli v0.106.0). Publish cascade travels DOWN to dependencies automatically but never UP to siblings, so a real job is usually several objects: an Answer beside a Liveboard on the same Model has to be published in its own right. `export` now accepts any number of GUIDs of any type, merges their closures (de-duplicating shared Tables so nothing is parameterized twice, and clustering once across the union so two roots sharing a schema need one variable not two), and `--with-dependents` walks upward to add the content riding on a selection. `apply` publishes every root, one call per type. Adds a cohort pre-check covering intermediate Models rather than just the roots, since that is where a cohort column is owned: selecting only a Liveboard previously passed planning and failed at the publish call. Apply-plan assembly split into `ts_cli/publish_apply.py` under the file-size gate.
- feat: translate two more publish refusals into actionable messages (ts-cli v0.105.0). `code 13152` (unpublish blocked because dependents are still published) now names the blocking object and org and gives the working retraction order, which is otherwise discoverable only by experiment: retract the siblings with `--keep-dependencies` first, then the shared Model, which cascades. `code 13151` with a cohort-column dependency now names the column and states the real scope of the restriction. Both previously dumped the raw HTTP body. Error bodies can name objects the caller never mentioned (the blocker in a 13152 is a dependency; a cohort column is a `LOGICAL_COLUMN`), so `_name_index` now sweeps GUIDs out of the response body and resolves them across all publishable types plus `LOGICAL_COLUMN`. Live-verified. 8 new tests.
- fix: `ts publish export` looks the root up as its own type (ts-cli v0.104.0). The publication-status lookup inside `export` hardcoded `LOGICAL_TABLE`, so for a Liveboard or Answer root it matched nothing, left `owner_org` null, and thereby switched off `resolve`'s owner-Org protection for exactly the closures that still parameterize Tables underneath. That would have reintroduced the Primary-Org outage via the Liveboard path. Verified live across all four root types (table, model, answer, liveboard), each now reporting `owner_org: Primary`.
- fix: `ts publish apply` derives the publish type from the closure root (ts-cli v0.103.0). It previously reused the type it uses to parameterize Tables (`LOGICAL_TABLE`) for the root as well, so a Liveboard or Answer closure would have been published with the wrong metadata type. `publish_type_for_root` now maps `liveboard`/`pinboard` → `LIVEBOARD`, `answer` → `ANSWER`, and everything in the data layer → `LOGICAL_TABLE`, and `rollback` unpublishes with the same type. Confirms Liveboard/Answer coverage end to end: `export` accepts their GUIDs and walks down to the Tables beneath them (they carry no parameterizable fields of their own), and `push`/`unpush`/`status` already took `--type`. 12 new tests.

## 2026-07-25
- fix: `ts publish resolve` now always covers the owner (Primary) org (ts-cli v0.102.0). Found by live-publishing a table to ORG3 with values assigned for ORG3 only: the publish succeeded, but a query in the **Primary** org then failed with `SQL compilation error: Object 'T1_PUBLISH' does not exist` — parameterizing had swapped the static db/schema for tokens that resolve to nothing there, collapsing the FQN. ThoughtSpot's publish validation checks only the target orgs, so nothing on the platform side catches it. `resolve` now includes the owner org automatically and always gives it the field's **current** value, never a pattern expansion, so publishing can never silently repoint what Primary reads. Four regression tests.
- feat: add `ts publish apply` and `ts publish rollback` (ts-cli v0.101.0) — the execution half. `apply` runs create-variables → assign-values → parameterize → optionally publish in that fixed order (the platform requires it), refuses to start when the value matrix has coverage gaps, and is re-runnable (an existing variable is reused, a field already bound to a token is left alone). `--rollback-out` writes a record of every field's ORIGINAL static value, which matters because `unparameterize` substitutes a value rather than clearing the field: without the record there is no way back. `rollback` reverses it — unpublish with `include_dependencies` (so the Connection grant goes too), restore static values, delete only the variables that run created. Verified live end to end: export → resolve → apply --publish-to ORG1 → rollback, returning the cluster to its exact prior state.
- feat: add `ts publish export` and `ts publish resolve` (ts-cli v0.100.0) — the planning half of Orgs Publishing. `export` walks Model → Tables → Connection and clusters each table's `db`/`schema`/`db_table` by **distinct value**, because a variable holds one value per scope and `field_names[]` writes the same token into every field listed; the unit of parameterization is therefore a value, not a field or a table. On a real 4-table model that collapses 6 fields into 2 recommended variables. `resolve` turns a closure into a per-org value matrix from one of four sources (`uniform`, `pattern`, `file`, `existing`) and always emits a coverage check, so a missing value is reported with the variable and org **named** rather than as the platform's GUID-and-numeric-id refusal at publish time. Live testing surfaced one thing the unit tests could not: Falcon-backed tables have no connection block and cannot be parameterized (the "default system tables" the docs exclude), yet the first cut still proposed variables for them — clusters now carry `parameterizable`, `recommended` is gated on it, and `export` warns naming the tables. Pure logic in `ts_cli/publish_plan.py` (44 tests); planning commands split into `commands/publish_planning.py` to keep both modules under the file-size gate. Also adds the `ts-publish-*` skill-naming family.
- feat: add the Orgs Publishing primitives to ts-cli (v0.99.0) — `ts variables create`/`delete`, `ts metadata parameterize`/`unparameterize`, and a new `ts publish` group (`push`, `unpush`, `status`). Together these cover the full ThoughtSpot Orgs Publishing workflow: define a template variable, bind it to a Table or Connection field, publish the object from the Primary Org to target Orgs without copying it, and report what is published where. Every endpoint shape and behaviour was live-verified on an Orgs-enabled instance (see `docs/superpowers/specs/2026-07-25-ts-publish-orgs-design.md` §2.5), which corrected three things the published docs get wrong or leave unsaid: a `TABLE_MAPPING` variable holds exactly **one** value per scope (the docs' three-values-to-one-variable example is rejected by the API), `field_names[]` writes the **same** token into every field listed (so the model is one variable per distinct value, and the command warns when several fields are bound at once), and unpublishing with `include_dependencies: false` leaves the **Connection** granted to the target Orgs (so `unpush` retracts dependencies by default). `explain_publish_error` translates the API's GUID-and-numeric-id failure messages into named, actionable text including the `ts variables set` command to run; `ts publish status` reads `metadata_header.orgIds` so it needs no per-Org authentication. `--skip-validation` is exposed but discouraged and warns loudly: it disables every check and lets an unparameterized object publish so the target Org silently reads the Primary Org's database. 31 new tests in `tests/test_publish.py`. Part of the in-progress `ts-publish-orgs` skill.
- fix: bump ts-cli to v0.98.0 — correct column_alias TML structure (`entries:` list wrapper, `obj_id` instead of `fqn` in model reference) so aliases actually persist after import; add `--format csv` to `ts alias build` for ThoughtSpot CSV upload format output
- feat: update ts-object-model-alias to v1.1.0 — add output choice step (import / TML file / CSV file), Technical Reference section documenting correct TML structure

## 2026-07-24
- fix: bump ts-cli to v0.97.0 — final whole-branch-review fixes for the in-progress `ts-object-model-alias` skill. **C1 (critical):** `_run_ai_batches` (the AI-locale-overlay path used by `--source file`/`--source db` use case 3) fed raw translation dicts (`{column, locale, alias, description, org, group}`) straight into `build_translation_prompt`, which reads `c["name"]` — a `KeyError` on every overlay call. Now reshapes each dict to `{name, description}` (name falls back to `column` when no `alias` is set yet) before building the prompt, and reads `col_names` off the `column` key. **I1:** `--groups` is now rejected alongside `--orgs` for `--source ai` (was previously only guarded for `--orgs`). **I2:** `--source ai` without `--locales` now fails with a clear "`--locales is required for --source ai`" error instead of falling through to `validate_locales([""])`'s confusing empty-string message. **I3:** `--translator cortex` without `--sf-profile` now fails cleanly (`SystemExit` with a clear message) from `_call_llm` itself, closing the gap in the AI-overlay path where it previously fell through to an `AttributeError` calling `.execute()` on a `None` Snowflake cursor. Also fixes the design spec's locale count header (27, not 28 — matches `SUPPORTED_LOCALES`). 9 new tests in `tests/test_cli_alias.py` (regression-verified: 6 of 9 fail against the pre-fix code).
- feat: add `ts-object-model-alias` skill — the interactive Claude Code skill for the `ts alias` column-alias pipeline (language localization, tenant-based renaming, and tenant + locale). Guides Model selection, export, use-case/scope/source selection, AI/file/DB-driven generation, review, merge-vs-replace, build + import (with async-import progress relay), and a re-export verification pass. Wired into the root skills table, `agents/cli/SETUP.md` symlink steps, and `agents/PARITY.md`; `check_runtime_coverage.py` documents the CLI-only divergence (no CoCo Snowsight equivalent — depends on the `ts` CLI for every operation). `tools/smoke-tests/smoke_ts_object_model_alias.py` covers the pure-function pipeline (CSV parse → translate → build → verify structure) with no live connection needed — no longer on the `check_smoke_tests.py` ALLOWLIST (BL-134 DONE); a live round-trip against a column-alias-enabled instance is tracked separately (BL-135).
- feat: add `ts alias` CLI command group — `export`/`translate`/`build`/`import` — a composable pipeline for managing ThoughtSpot Model column aliases (language localization, tenant-based renaming, and per-org locale matrices). `export` pulls a Model's columns + existing aliases via the TML export API; `translate` generates aliases from AI (Claude or Snowflake Cortex), a CSV file, or a Snowflake table, with an optional AI locale overlay on top of file/db sources; `build` assembles `column_alias` TML (with `--merge` to preserve existing aliases); `import` uploads it, picking sync vs. async based on payload size (<5 MB sync, 5–25 MB async with status polling, >25 MB rejected). ts-cli → v0.96.0. Part of the in-progress `ts-object-model-alias` skill.
- feat: rename the CLI command **`ts spotql` → `ts agentql`** (follow-up to the SpotQL→AgentQL rename). `ts agentql` is now the primary command; `ts spotql` remains as a hidden deprecated alias so existing scripts and skills keep working. All skill/doc references updated to `ts agentql`. The server contract is unchanged — the callosum `/data/spotql/*` endpoints and the `spotql_query` request field keep the `spotql` spelling (that is the endpoint API, which did not change). Internal Python module/file names (`spotql.py`, `spotql_ops.py`, `spotql_aggregate.py`) are left as-is (implementation identifiers). ts-cli → v0.95.0; ts-object-model-agentql-query → 2.1.0; ts-object-model-aggregates → 1.0.3.
- feat: rename the external product name **SpotQL → AgentQL** across the whole repo (skill, references, docs, and ts-cli prose/help text). Breaking for the skill: `ts-object-model-spotql-query` → `ts-object-model-agentql-query` (slash command + directory; re-point the `~/.claude/skills/` and `~/.snowflake/cortex/skills/` symlinks after merge), `references/spotql-rules.md` → `references/agentql-rules.md`, and the smoke test / `--spotql` flag → `--agentql`. Skill bumped to 2.0.0; `ts-object-model-aggregates` → 1.0.2 (prose only). **No API/CLI change:** the `ts spotql` command, the callosum `/data/spotql/*` endpoints, and the `spotql_query` request field are unchanged stable identifiers, so integrations keep working. Historical audit reports and prior changelog entries retain the old name as point-in-time records.
- chore: bump ts-cli to v0.94.1 — rename SpotQL → AgentQL in `ts spotql` help text and docstrings (user-visible `--help` prose); no command, flag, endpoint, or output change.
- fix: bump ts-cli to v0.94.0 — tighten `ts metadata delete` not_found detection (follow-up to BL-133, from an angle-17-style `/code-review` of #333). `classify_delete_response` now keys `not_found` off the structured error code `13003` (nested under `error` or top-level, common key spellings) and falls back to the specific phrase "metadata object not found" — no longer a bare "not found" substring, which also matched unrelated 400s (e.g. "connection not found") and, with `--ignore-missing`, would silently hide a real failure. New `_body_signals_not_found` helper; 4 new/updated classifier tests incl. a false-positive regression guard.
- feat: repo-audit sweep gains angle 17 — a `max`-level `/code-review` backstop over the `<last-full-audit-sha>..HEAD` delta, wired into `.claude/workflows/repo-audit.js` as a full-scope-only finder (never the weekly `external` sweep, never per-PR). It emits the standard findings schema so synthesis dedups/routes it via the two-bucket rule; per-PR `/code-review` remains the primary bug net. Rubric documented in `.claude/rules/repo-audit.md` (angle 17 row + prose + History).
- fix: bump ts-cli to v0.93.0 — `ts metadata delete` partial-success handling (BL-133). A batch `metadata/delete` is atomic: one already-gone GUID fails the whole call (400, code 13003) and deletes nothing, forcing a per-GUID retry loop in teardown/cleanup and the dependency-manager rollback path. The command now tries the batch first (one fast call when every GUID is present) and, on failure, falls back to per-GUID deletes so present objects are still removed — reporting a per-object outcome map to stdout (`{deleted, not_found, errors, outcomes{guid: deleted|not_found|error}}`; the `deleted` key is preserved for back-compat, the rest additive). The delete API is the source of truth for each object's fate (no reliance on metadata-search multi-filter semantics, and no risk of a pre-filter under-count skipping a real object). New `--ignore-missing` flag treats already-gone GUIDs as success; genuine errors always exit non-zero. Pure helpers `classify_delete_response`/`resolve_delete_outcomes` make the fallback outcome map unit-testable without a live connection (12 new tests). (Assumes the v0.92.0 BL-132 bump lands first; rebase/renumber if merged in the other order.)
- fix: bump ts-cli to v0.92.0 — duplicate `column_id` → formula promotion (TML invariant I8/I5), shared by the from-Databricks and from-Snowflake Model builders (BL-132). When a source references one physical column both as a raw measure and as an aggregate metric (e.g. `F_TIME_TO_RESOLVE` + `AVG(TIMETORESOLVE__C)`), both classify as physical `columns[]` candidates with an identical `TABLE::col` — which ThoughtSpot rejects on import ("columns should have unique column_id values"). New shared helper `formula_common.promote_duplicate_column_ids` keeps the first occurrence as a `column_id` entry and re-expresses the rest as `fn ( [TABLE::col] )` aggregation formulas (SUM/AVERAGE/MIN/MAX/COUNT/MEDIAN/STDDEV/VARIANCE, plus I5's COUNT_DISTINCT → `unique count(...)`), wired into both `mv_build_model` and `sv_build_model` after `resolve_name_collisions`. A duplicate that is not a re-expressible aggregate (unmapped measure aggregation, or two bare dimensions on one column) is left in place so `ts tml lint` I8 still surfaces it. The bug affected both builders identically — the backlog's premise that from-Snowflake already handled it was inaccurate; the fix is new, factored to one home, and applied to both directions. Unit tests cover the helper's aggregation map + fail-loud paths and each builder's fact-col + aggregate-metric-on-same-column fixture (emitted TML passes I8 lint).
- fix: bump ts-cli to v0.91.0 — role-playing (aliased) dimension support for the Databricks Metric View converter, **both directions**. TO (`ts databricks build-mv`): `build_joins` resolves an aliased join target to its physical table def and scopes the on-clause per-join, and `build_column_index` mirrors each physical column under its role-play alias key so role-play dimensions/measures resolve instead of being dropped. FROM (`ts databricks build-model`/`translate-formulas`): a physical table reached by more than one alias path is a role-play — `resolve_parts` gives each such path its own node identity and `build_model_tables` emits `alias:` + alias-referencing joins (single-use tables/renames unchanged, so the golden fixtures are untouched). Verified via a TS↔MV round-trip on SUPPORT_CASE (ACCOUNT×2, SUPPORT_PRODUCT__C×2): build-mv emits distinct join nodes + all role-play dims; parse→translate→build-model reconstructs the role-play tables. Mirrors the Snowflake role-play fixes (#328/#329). — role-playing (aliased) dimension support for the Databricks Metric View converter, **both directions**. TO (`ts databricks build-mv`): `build_joins` resolves an aliased join target to its physical table def and scopes the on-clause per-join, and `build_column_index` mirrors each physical column under its role-play alias key so role-play dimensions/measures resolve instead of being dropped. FROM (`ts databricks build-model`/`translate-formulas`): a physical table reached by more than one alias path is a role-play — `resolve_parts` gives each such path its own node identity and `build_model_tables` emits `alias:` + alias-referencing joins (single-use tables/renames unchanged, so the golden fixtures are untouched). Verified via a TS↔MV round-trip on SUPPORT_CASE (ACCOUNT×2, SUPPORT_PRODUCT__C×2): build-mv emits distinct join nodes + all role-play dims; parse→translate→build-model reconstructs the role-play tables. Mirrors the Snowflake role-play fixes (#328/#329).
- fix: bump ts-cli to v0.90.0 — `ts snowflake build-sv` (ThoughtSpot Model → Snowflake SV, the reverse direction) now supports role-playing (aliased) dimensions: a reused physical table emits `<alias> as <FQN>` in `tables()`, `relationships()` reference the SV logical table name (not the FQN), and role-play dimensions are owned by their alias instead of being collapsed/dropped. Also fixes two pre-existing bugs that made the emitted DDL un-creatable in Snowflake: invalid `[primary key]` square brackets (GET_DDL display notation, not CREATE syntax) and fully-qualified names in relationship endpoints. Live round-trip verified (SUPPORT_CASE_SV → SV DDL → creates in Snowflake → re-parses to 13 tables / 12 relationships / 51 dimensions, all role-plays intact).
- fix: bump ts-cli to v0.89.0 — `ts snowflake` (translate-formulas + build-model) now supports role-playing (aliased) dimensions: a physical table reused under multiple SV aliases (e.g. one USER table played as CASE_OWNER / INCIDENT_OWNER / INCIDENT_RESOLVED_BY) maps to distinct alias nodes with unambiguous joins and column refs (`model_tables` emit `alias:`, joins carry the alias in `with:` while `on` uses physical names, column refs use the alias prefix). Also: bare-column dimension renames emit direct columns (not formulas), and the `to_cols`/`to_column` mismatch that crashed translate-formulas is fixed. Live-verified against se-thoughtspot (SUPPORT_CASE_SV, AGENT_SKILLS)
- feat: bump ts-cli to v0.88.0 — translate inverse trig (`ACOS`/`ASIN`/`ATAN`) + `COT` (BL-072 sub-item) and `USERNAME`/`ISUSERNAME`/`ISMEMBEROF` → RLS system variables (BL-071 subset). **Inverse trig:** ThoughtSpot's `acos`/`asin`/`atan` return degrees where Tableau's return radians (by symmetry with the already-shipped `SIN`/`COS`/`TAN` radians-to-degrees conversion), so each composites `* pi/180` to convert back; `COT(x)` has no native ThoughtSpot function and composites off `tan` the same way Tableau defines `COT(x) = 1/tan(x)`, with the inner `tan` argument converted to degrees. `ACOS`/`ASIN`/`ATAN`/`COT` removed from `_UNMAPPED_FUNCTIONS` (were U8, added v0.26.5) — fail-loud no longer fires for them. **User identity:** `USERNAME()` → bare `ts_username` system variable, `ISUSERNAME(s)` → `( ts_username = s )`; `ISMEMBEROF("group")` → `( ts_groups = 'group' )` is the documented sibling mapping (`tableau-formula-translation.md`, coverage-matrix #108) — previously passed through untranslated and un-rejected, now genuinely translated. `USERNAME`/`ISUSERNAME` removed from `_UNMAPPED_FUNCTIONS` (were part of U7). `FULLNAME`/`ISFULLNAME`/`USERDOMAIN` (no confirmed ThoughtSpot display-name variable / unverified value shape) and `USERATTRIBUTE`/`USERATTRIBUTEINCLUDES` (`ts_var()` not yet accepted in Model/Answer formulas) remain rejected at translate time — deferred, BL-071. Hierarchies/value aliases (the other BL-072 sub-item) are NOT part of this change. `references/coverage-matrix.md` moves #32/U8, U7's USERNAME/ISUSERNAME, and #108 from Unmapped/pass-through to Mapped Constructs; `docs/backlog.md` BL-071/BL-072 statuses updated to PARTIAL. 15 new/updated unit tests (4 new composite translations + 4 wrong-arity guards for inverse-trig/COT, 3 new user-identity translations + 3 wrong-arity guards, 2 regression checks that the moved functions are no longer flagged unmapped, 1 regression check FULLNAME still is); full suite green (3,043 tests).
- feat: bump ts-cli to v0.87.0 — codify Tableau Set → cohort detection and TML emission (BL-067) + audit classification (BL-088). `ts tableau parse` now extracts and classifies every top-level `<group>` Set into a `sets[]` key per datasource (`ts_cli/tableau/twb.py::extract_sets` — static/`%null%`/`except`-member-list/member-intersect/Top-N/Bottom-N (literal + parameter-driven)/all-except-Top-N/condition-based/mixed computed set-ops/dynamic Set Control, per the already-documented Phase 2a/2b/2c detection rules), and `ts tableau build-model` emits one `*.cohort.tml` per translatable Set automatically via a new pure `ts_cli/tableau/sets.py::build_cohort_tml` (mirrors `tables.py`/`liveboard.py`'s style) — no more agent-guided hand-assembly for the biggest remaining Tableau conversion gap. The result JSON gains `cohorts_emitted`/`cohorts_deferred`/`cohort_files`; stderr echoes the documented log line per set. Untranslatable forms (dynamic Set Controls with no fixed members, unclassifiable `<group>` shapes) are omitted and flagged, never mis-converted. **BL-088:** `ts tableau classify-formulas` (`classify_workbook`) now also returns a `sets[]`/`sets_tier_counts` breakdown per datasource (`column_set`/`query_set`/`deferred` — reusing the SAME `set_type` extraction already classified, never re-derived), so audit mode's coverage report can populate its "Tableau Sets" table from real data instead of leaving it unpopulatable. BL-131's `sets_detected` build-model warning reworded to point at the new automatic emission instead of telling the agent to hand-convert. Arbiter: `TableauSetControlUseCases.twbx` (10 native Sets) → 9 cohorts emitted (8 static + 1 except-of-member-list, all `GROUP_BASED`), 1 deferred (dynamic Set Control) — `ts tml lint --dir` clean; a non-Set workbook (Ads Commercial Dashboard) emits 0 cohorts with byte-identical output otherwise (regression-checked against the pre-change build). Live `VALIDATE_ONLY` against se-thoughtspot/APJ_TAB confirmed a real, previously-documented gotcha still applies to the auto-generated cohorts: a freshly generated model has no `obj_id` yet, so a cohort's bare name-only `worksheet:` reference fails import with "Worksheet not found" (error 14500) until the model's real `obj_id` is read back and patched in post-import — `ts-convert-from-tableau` v1.38.0 documents this explicitly. 46 new unit tests (16 extraction, 28 cohort-TML-per-type, 2 audit tier classification); full suite green (3,028 tests).
- fix: bump ts-cli to v0.86.0 — `col_table_map`/column ownership no longer resolves to the excluded hyper-Extract wrapper relation. Reproduced on `Demo WB 3 with SQL join.twbx` (federated Custom-SQL + hyper-Extract): a column's metadata is written TWICE by Tableau — once under the live `<connection>`, once more (mirrored, GUID-named) under `<extract>`'s own `<connection>` — and since both sit under the same `<datasource>`, `_metadata_column_records`' plain `.//metadata-record` walk picked up both, with the extract's document-order-later copy silently winning. That stamped the column's `table` (and sometimes its `db_column_name`, when the extract's own copy has internally disambiguated it, e.g. `Sales Person1`) onto a relation `_extract_tables` already excludes as a wrapper — dangling `column_id` ("XREF: column_id not found") and dropping the column from the emitted Table TML. Fix: `_metadata_column_records` now skips any metadata-record owned by a wrapper relation (`_wrapper_relation_names`, the same `_is_extract_wrapper` predicate `_extract_tables` uses — one source of truth, not a second, divergent one), so the live copy always wins regardless of document order. Also fixed an adjacent latent bug this exposed: `table` was `parent-name.split(".")[-1]`, silently truncating a file-backed relation's own literal-dot name (e.g. a CSV extract's Tableau-assigned `dim_sales_team_clean_updated.csv1`) to `csv1` — a table `_extract_tables` never emits; now uses `parent-name` verbatim (`parent-name` is always a single relation-name reference, never a dotted schema path). Arbiters: `Demo WB 3 with SQL join.twbx` build-model → 0 XREF findings (was 6), `ts tml lint --dir` clean (was `false`), previously-dropped dim-table columns (Commission Rate, Manager Name, Monthly Sales Quota, Office Location, Sales Person) plus `Country` now emitted with `column_id` on a real model table. Bonus: the same fixed map feeds formula scoping too — `TableauSetControlUseCases.twbx` (Set Control/Sets/Hex Map) and a plain single-table workbook (`Weighted Usage.twb`) each had a formula silently referencing `[Extract::col]` (a table never emitted — `ts tml lint` doesn't parse formula bodies, so this shipped undetected); now correctly scoped to the real table (`Orders::col` / `WEIGHTED_USAGE::col`). No regression: Ads Commercial Dashboard's Table/Model TML byte-identical, both lint clean
- fix: bump ts-cli to v0.85.0 — three small backlog closeouts (BL-060, BL-093, BL-131). **BL-060 (nested-if-in-comparison):** `validate_pre_import()` now mirrors the shipped BL-062 bare-else check with a warning-only regex (`[<>=!]=?\s*if\b` against the already-stripped expression) — a comparison operator binding directly before `if` (e.g. `sum(X) < if(Y) then Z else W`) fails ThoughtSpot import (needs `(if ... then ... else ...)` parens); live-confirmed on Ads Commercial Dashboard's `Dimensions: TrafficLight` formula (silent before, now flagged). **BL-093 (Tableau parameters embedded in Custom SQL):** new `substitute_sql_view_parameters()` (`ts_cli/tableau/params.py`) scans each SQL View's `sql_query` for `<[Parameters].[Name]>` tokens — a token naming a parsed parameter gets that parameter's default value inlined (SQL becomes valid; a `validation_warnings` note records the value is now static) and a token naming an unknown parameter gets a `NEEDS-REVIEW` warning instead, left in place rather than silently passed through to import. Wired into `build-model`'s `_generate_flow` before SQL View emission; no `<[Parameters]` token survives into emitted SQL when a default exists. **BL-131 (native Tableau Sets nudge):** new `count_native_sets()` (`ts_cli/tableau/twb.py`) counts datasource-scoped `<group>` Set elements (static/Top-N/condition-based — every flavor the skill's Phase-2a/2b/2c step handles), excluding Tableau's internal `crossjoin` combined-field mechanism (auto-named `Action (...)`/`Tooltip (...)` groups for multi-field dashboard actions/tooltips — live-confirmed 469 of these vs. ~120 real Sets across the test corpus) and the Pivot-field `<group>` shape (no `<groupfilter>` child). `build-model` now prints a stderr WARNING and adds `sets_detected` to each datasource's result JSON when count > 0, so an automated/Stage-1 run never silently skips the Phase-2 set→cohort step. Live-confirmed: `TableauSetControlUseCases.twbx` → `sets_detected: 10` + warning; a no-Set workbook → 0, no warning; Ads Commercial Dashboard (14 crossjoin Action/Tooltip groups, 0 real Sets) → `sets_detected: 0`, correctly not warned
- fix: bump ts-cli to v0.84.0 — `ts tableau build-model` multi-table quality follow-ups (junk columns, column/formula drops on collision, db_table). **#A (confirmed):** the `__tableau_internal_object_id__` junk pseudo-column (one per physical table, from Tableau's hyper-extract bookkeeping) leaked into Model TML on the multi-table path — the existing junk filter (`clean_columns`) only ran single-table, so it was skipped for federated multi-table datasources. New table-blind `drop_junk_columns()` (`ts_cli/tableau/reconcile.py`) strips it without clean_columns' single-table table-stamp/dedup (both wrong across multiple tables). Live-confirmed: Ads Commercial Dashboard went from 26 leaked occurrences to 0. **#B (reproduced + fixed, two related bugs):** (1) a physical column present in one datasource but not another sharing the SAME table name (e.g. `TableauSetControlUseCases.twbx`'s three datasources all declare a single-table `Orders` relation, each referencing a different column subset) was silently dropped from Table TML — `build-model` iterates datasources independently and writes one `{slug}.table.tml` per table NAME, so the last datasource processed clobbered the file (Set Control's `Sub-Category` was lost to Hex Map's write, producing an `ts tml lint` XREF finding). Fixed with a `table_registry` shared across every datasource in one `build-model` invocation, accumulating the UNION of referenced columns per table name instead of the last write winning. (2) A calc field whose caption collides with a physical column's internal name (Ads' `Region`, tier `parameter_ref`) is correctly auto-renamed to `"Formula Region"` by the existing name-clash safety net at generation time, but the rename was invisible to callers — `ts tableau verify` had no way to know the recipe and reported it as a silently dropped formula (a false-positive structural ERROR). `verify.py` now reproduces the same `detect_name_clashes` computation the generator uses (same inputs, so the two can never drift) before flagging a drop; the rename is also now surfaced in `build-model`'s own `name_renames` result field. **#C (verified — found a real wrong-output case, fixed):** Table TML `db_table` re-slugs the table's logical `name` instead of preferring the parser's own `db_table` field — benign for a plain table (name already equals the physical name), but WRONG for a Tableau-assigned alias of a table joined twice (Ads' `d_partner1` aliasing `d_partner`): the re-slug emitted `db_table: d_partner1`, a table that does not exist in the warehouse (only `d_partner` does), which would fail import ("table not found"). `build_table_tml` now prefers the parser's `db_table` field (its last dotted-path segment; `db`/`schema` are already separate fields), falling back to the name-slug only when the parser didn't supply one. Arbiters: Ads emits 0 `__tableau_internal_object_id__` (was 26); Set Control `ts tml lint --dir` clean with no hand-editing (was 1 XREF finding); Ads `ts tableau verify --dir` 0 errors (was 1, the false "Region" drop); `d_partner1.table.tml` now emits `db_table: d_partner` (was `d_partner1`)
- feat: update ts-convert-from-tableau to v1.34.0 — token-reduction guidance: exact copy-pasteable CLI invocations (real flags, from each command's own `--help`) baked into every `ts tableau`/`ts tml lint` step (kills the 5-7 `--help` probes a benchmark run made per session); Step 5a/5b now state explicitly that `build-model` runs ONCE for the whole workbook (not once per datasource — it already emits every used datasource's Model + Table TML in one call); Step 6 now runs `ts tml lint --dir` and the new `ts tableau verify --dir` once each over the whole output directory instead of once per model. Procedure-only — the per-datasource "strict separation" output semantics are unchanged, and a live regression on `TableauSetControlUseCases.twbx` confirms byte-identical Model/Table TML from the new one-call flow vs. the old per-datasource loop. Prereq ts-cli v0.83.0
- perf: bump ts-cli to v0.83.0 — add `ts tableau verify --dir` (aggregates every full Model TML in a `build-model` output directory into one call + one combined exit code, reusing the existing per-model `verify_conversion` — no new logic, just loops it), and rework the `ts-convert-from-tableau` SKILL.md's Step 5b framing so an agent runs `build-model` **once** for the whole workbook (it already emits every used datasource's model + table TML in a single call) instead of once per datasource. Also bakes a canonical copy-pasteable invocation (real flags, from each command's own `--help`) into every `ts tableau …`/`ts tml lint` step, eliminating the 5-7 `--help` probes a benchmark run made per session. Token-reduction only — no conversion-output change: confirmed byte-identical Model/Table TML from a single `build-model` call vs. the old per-datasource loop on a real 3-datasource workbook (`TableauSetControlUseCases`). Arbiter: that workbook now needs 1 build-model + 1 lint + 1 verify (was 3+3+3)
- fix: bump ts-cli to v0.82.0 — `ts tableau build-model` multi-table/federated follow-ups (BL follow-up #2/#3/#4 from the re-benchmark). **#2 (confirmed):** Tableau's caption-collision disambiguation suffix (` (table_name)`, e.g. `LineItemId (agg_booked_monthly)`) no longer leaks into Table TML `db_column_name` — it now comes from the metadata-record's `remote-name` (the real warehouse column), while the display `name` keeps Tableau's own disambiguated caption. Fixes join XREF failures on collision columns and stamps each column's owning `table` from the same metadata (required once db_column_name is de-duplicated, else two tables' same-named columns collide into one bare `column_id`). **#4 (confirmed):** the hyper `Extract` cache wrapper relation (`table` schema-scoped to `[Extract]`, alongside every live-source table) is no longer parsed as a physical table — was emitting a spurious duplicate Table TML per table (Ads Commercial Dashboard: 25 → 13 tables) that overwrote a shared table run-to-run when multiple datasources shared one (Set Control: Orders+Extract → Orders only). **#3 (live-verified, not code-broken today):** live `VALIDATE_ONLY` on se-thoughtspot/APJ_TAB confirms ThoughtSpot rejects a bare (non-`TABLE::col`) `column_id` even on a single-table model (error 14547) while the qualified form validates; audited every `len(tables)==1` branch in the GENERATE flow and found today's single-table path already qualifies correctly (pinned by a new regression test) — no build-model code change needed for the single-table case itself. Added new invariant I12 + a scoped `ts tml lint` check (bare `column_id` on a single-table model) as the permanent regression guard the brief asked for. Regression arbiters: `ts tml lint --dir` clean on Ads (was 2 XREF findings) and Set Control; live `VALIDATE_ONLY` smoke on a real APJ_TAB table
- fix: bump ts-cli to v0.81.0 — wire the documented `REGEXP_EXTRACT`/`REGEXP_MATCH`/`REGEXP_REPLACE`/`FINDNTH` pass-through mappings into `ts tableau build-model` (stop dropping 12 resolvable formulas — was in `_UNMAPPED_FUNCTIONS` despite `tableau-formula-translation.md` documenting the exact `sql_*_op` templates since v0.28.1); re-map `REPLACE` off the invalid bare `replace(...)` native call (live-confirmed error 14516) onto the same pass-through form; fix an LOD keyword-whitespace bug where a grand-total `{FIXED: agg}`/`{INCLUDE: agg}`/`{EXCLUDE: agg}` (no space before the colon) silently emitted invalid `{ FIXED }` TML. Live-verified on se-thoughtspot/APJ_TAB (`VALIDATE_ONLY`, no persistent objects): all 5 formula types return OK; the pre-fix bare `replace(...)` negative control live-fails with 14516, confirming the bug. Regression on the Elevate Tableau Skills workbook: 38/54 → 50/54 formulas now translate (12 previously-dropped REGEXP/FINDNTH formulas; the remaining 4 skips are unrelated table-calc functions). `tableau-formula-translation.md`'s self-contradictory "passed through untranslated" line corrected; `references/coverage-matrix.md` moves these from Unmapped to Mapped
- fix: bump ts-cli to v0.80.0 — Databricks converter emits DATE_TIME (was invalid DATETIME) for timestamp columns (BL-X4)
- docs: trim `ts-convert-from-tableau` SKILL.md to v1.31.0 — add a "Context budget — never Read big tool-output files" rule (naming the real `--out`/`--output` JSON and generated-TML paths), and extract reference-heavy detail from Steps 5, 10, A4, 7, 12, and 3's field-mapping section into six new `references/*.md` files (linked back from each step's spine). SKILL.md goes from 4,402 to ~2,900 lines; no step logic, prompts, or commands changed.
- perf: bump ts-cli to v0.79.0 — batch `ts tables create` imports (50 per call), individual JDBC retry (BL-073 item 14.3)
- fix: `ts tableau build-model` no longer emits raw Tableau table-calc/window functions (`WINDOW_*`, `LOOKUP`, `INDEX`, `FIRST`, `LAST`, `PREVIOUS_VALUE`) verbatim into ThoughtSpot formulas — live-confirmed hard-fail on import (error 14516, "Search did not find '<FUNC> ( ... )'"). These are now rejected at translate time with a table-calc-specific reason and land in the skipped/omitted count instead; `classify.py`'s audit tiers corrected to match (`window_ambiguous` added, `moving` retired as dead/wrong). `SIZE()` — the one row-offset function with a context-free translation — is now genuinely translated to `sql_int_aggregate_op("COUNT(*) OVER ()")` rather than omitted. `references/coverage-matrix.md` (ts-convert-from-tableau) updated to move these out of Mapped into Unmapped (U10/U11)
- chore: bump ts-cli to v0.78.0 — table-calc translate-time honesty fix (see above)
- docs: add connection-select shared reference — extract N/F/L + E/C prompt from conversion skills (BL-122 item 11.3)
- feat: ts tableau build-model emits Table TML
- chore: bump ts-cli to v0.77.0
- perf: bump ts-cli to v0.76.0 — batch model TML export + fix AI-instructions false positives in audit (BL-073 items 14.1/14.4)
- fix: bump ts-cli to v0.75.0 — `connections add-tables` now includes `authenticationType` in the update payload (BL-095)
- chore: bump ts-cli to v0.74.0 — add `median()` mapping for Databricks formula translation (BL-123 item 13.7)

## 2026-07-22
- feat: update `ts-object-model-spotql-query` to v1.5.0 — add Snowflake SV / Databricks MV backing rules (BL-118)
- refactor: scope CoCo Snowsight to conversion pipeline only — remove recipe skills (business-days, hms-display)
- chore: bump ts-cli to v0.73.0 — retire vestigial phase1+ file emission (BL-125), bare-else detection (BL-062)
- chore: bump ts-cli to v0.72.0 — `io_helpers` module, `profile_ops` path override, bare-except narrowing (BL-121)
- docs: extract import error table + post-import verification to shared `ts-tml-import-gate.md` §4/§5 — closes BL-063
- refactor: rewire `ts-convert-to-snowflake-sv` Steps 7-9 onto `ts snowflake build-sv` — relationship building, column classification, DDL assembly now deterministic CLI for model format (BL-063 phase 1b)
- refactor: rewire `ts-convert-from-snowflake-sv` onto deterministic CLI commands — `ts snowflake parse-sv`, `translate-formulas`, `build-model` replace 8 inline Python blocks (BL-063 phase 1a)
- feat: add `ts-convert-from-powerbi` skill + `ts powerbi` command group (parse / build-model / build-liveboard) — Power BI (`.pbip`: TMDL semantic model + PBIR report) → ThoughtSpot converter; DAX translated to `[formula_<name>]` id-refs (topo-sorted + dangling-ref cascade), joins with real relationship cardinality, AVG-vs-SUM from `summarizeBy`, Spotter enabled; time-intelligence (SAMEPERIODLASTYEAR / YoY) flagged NEEDS REVIEW and recovered via the Spotter last-mile. Reuses the shared `dump_tml_yaml` + `tableau.liveboard.build_from_spec` emitter.
- docs: add `agents/shared/mappings/powerbi/powerbi-formula-translation.md` (DAX→TS formula map) + `worked-examples/powerbi/` (SPLY parameter, CALCULATE→group_aggregate, dual-axis combo)
- chore: bump ts-cli to v0.71.0 — `ts powerbi` command group
- fix: raise snowflake-connector-python floor to >=3.13.1 (CVE-2024-49750, CVE-2025-2479x) and setuptools to >=78.2 (path-traversal CVE)
- fix: metadata search page_size 50 → 500, correct `--tag` docs in 2 skills, remove dead Qlik CSV (audit findings 14.1, 5.1, 1.2)
- feat: add quality gates catalog generator (`generate_quality_gates.py`); auto-generates `docs/quality-gates.md` from pre-commit.sh, validate.yml, and validator docstrings

## 2026-07-21
- feat: add `ts-convert-from-qlik` skill + `ts qlik` command group (parse / build-model / build-liveboard) — Qlik Sense → ThoughtSpot converter across four sources (`--mode offline|engine-artifacts|qlik-cloud|engine`; qlik-cloud/engine are live SOURCE-provenance paths behind the `[qlik]` extra); flags Set Analysis `$`-context, variables, and no-equivalent functions NEEDS REVIEW rather than downgrading
- docs: add `agents/shared/mappings/qlik/qlik-thoughtspot-formula-translation.md` (199-row Qlik→TS formula map) + `schemas/qlik-app-ir.md`; fixed Upper/Lower → `sql_string_op` passthrough (no native TS `upper()`/`lower()`)
- chore: bump ts-cli to v0.69.0 — `ts qlik` command group + live Qlik Cloud/Engine extraction
- feat: `ts snowflake parse-sv` — deterministic Semantic View DDL parser (BL-100 PR1); extracts tables, relationships, dimensions, metrics (semi-additive, window, USING), facts, custom instructions, verified queries, extension JSON
- feat: `ts snowflake translate-formulas` — Snowflake SQL → ThoughtSpot formula translator (BL-100 PR2); function mapping, identifier resolution, column classification, window/LOD/semi-additive/USING handling
- feat: `ts snowflake build-model` — Semantic View → ThoughtSpot Model TML assembly + two-pass import (BL-100 PR3); inline Scenario B joins (equi/range/ASOF), SV synonym→display name, private columns, fact table detection; fixes latent `name` field bug in parse-sv table entries
- feat: `ts snowflake introspect` — INFORMATION_SCHEMA query + Snowflake→ThoughtSpot type mapping + tables-spec/tables-map assembly (BL-100 PR4); codifies Steps 6A–6C of ts-convert-from-snowflake-sv
- feat: `ts snowflake build-sv` — ThoughtSpot Model TML → Snowflake Semantic View DDL assembly (BL-100 PR5); column_id resolution, classification (dim/metric/time_dim), to_snake aliasing, relationship naming, metric topological ordering, CA extension JSON
- chore: bump ts-cli to v0.67.0

## 2026-07-20
- fix: `ts profiles add`/`update` now coerce `true`/`false` `--field` values to JSON booleans — previously `--field verify_ssl=false` was stored as the string `"false"`, which `requests` treats as a CA-bundle path, breaking every call to a self-signed/private cluster
- chore: bump ts-cli to v0.62.1

## 2026-07-18
- feat: ts-convert-from-tableau Step 6 now runs `ts tableau verify` as a migration-fidelity gate (silent-drop + mistranslation detection) — skill v1.29.0
- feat: `ts tableau verify` — source↔output migration-fidelity gate diffing a `ts tableau parse` output against generated Model TML for silent formula/table/join drops + LCS formula-equivalence (reuses `classify.py` tier split + `tml_lint`)
- chore: bump ts-cli to v0.62.0
- feat: `ts tml lint` cross-reference (XREF) check — catches a model referencing a table/column no batch TML generates, when table + model are linted together; also runs as a pre-flight warning in `ts tableau build-model`
- chore: bump ts-cli to v0.61.0
- fix: Tableau formula translator no longer corrupts string/date literals that contain reserved keywords (e.g. `IF [x]="END"`); adds `tableau/literals.py` masking, double-quote to single-quote normalisation, and `#...#` to `to_date()`
- fix: stop flagging valid `add_years()`/`add_weeks()` as non-existent in Tableau formula validation
- chore: bump ts-cli to v0.60.1
- feat: codify ts-convert-to-databricks-mv emission (ts databricks build-mv, deterministic, both runtimes)
- chore: bump ts-cli to v0.55.0

## 2026-07-16
- feat(ts-cli): codify the Tableau **model discovery algorithm** (v0.60.0) — the hard-won reasoning from the FedEx VEDR golden build, turned into deterministic code so it stops being rediscovered session to session. `ts tableau build-model` now: (1) **extracts the logical "noodle" join** from `<object-graph>/<relationships>` (`_extract_noodle_joins` in `twb.py`) — modern Tableau stores joins there, not as physical `<relation join=…>`, so build-model previously emitted an **unjoined** model that ThoughtSpot rejects; (2) infers **join cardinality direction** via a detail-id-count grain heuristic and emits `MANY_TO_ONE` (a wrong guess silently double-counts — the Insurity trap); (3) resolves **formula column refs** to a SQL-View's collision-renamed column name (`BEHAVIOR` → `BEHAVIOR (Custom SQL Query2)`); (4) **de-dupes** case-collision columns, keeping the MEASURE twin (fixes a numeric conditional getting `else ''`); (5) applies an **AVG-vs-SUM** heuristic (`normalized`/`rate`/`score`/`KI value` → AVERAGE, else SUM) instead of hardcoding SUM; and drops the schema-invalid `columns` key from `model_tables` (was import error 13122). Also `ts load databricks`: categorical/range **column pinning** (`values`/`min`/`max`) + **per-table row counts** so synthetic data aligns to the formulas' categorical literals and thresholds, and `INT64`/`INT32`/`FLOAT64` type detection. **Live e2e on ps-internal (AnujSeth org)**: FedEx twb → build-model → synthetic data → phased import (all phases OK) → liveboard; double-count check passes (`SUM(Speeding Events)=684`=ground truth, join did not fan out). Method recorded in memory (`reference-tableau-model-discovery-algorithm`, `reference-fedex-golden-liveboard-method`). +6 unit tests. Deterministic chart inference (KPI/BAR/COLUMN/LINE/table + Top-N set extraction + Measure-Values expansion) added to `build-liveboard`/`dashboards.py`
- feat(ts-cli): codify the Tableau **liveboard** step (v0.59.0, closes ts-convert-from-tableau open item #20) — `ts tableau parse` now emits `dashboards` (per-dashboard visuals with mark + shelf/role/measure-tagged fields, calc-id→caption resolution, date buckets, grid tiles via new `ts_cli/tableau/dashboards.py`), and `ts tableau build-liveboard --input <parse.json> --model-name <m>` consumes it, so **parse→build-liveboard runs with no hand-assembled spec**. This was the root cause of session-to-session dashboard-quality variance: the Tableau liveboard step was agent-driven (bespoke scripts) while PowerBI/Sisense were Python-driven. Live: FedEx VEDR workbook → 18 auto-extracted tiles, lint-clean. Method + gap analysis: `docs/tableau-liveboard-deterministic-method.md`. 5 new unit tests
- fix(ts-cli): `ts load` synthetic-data generator (v0.58.0) recognizes `NUMBER`/`NUMERIC`/`DECIMAL`/`INT`/`BIGINT` numeric types (previously only literal `INTEGER`/`FLOAT`), so numeric columns get real numbers instead of `val_00001` strings — a latent bug that also affected the Snowflake loader for `NUMBER` columns. Adds `ts load databricks --replace` (CREATE OR REPLACE to re-provision a table's schema). Both caught during a live Databricks e2e. 4 new unit tests
- feat(ts-cli): `ts load databricks` (v0.57.0) — provisions tables + synthetic data into a Databricks catalog.schema via the SQL Statement Execution API (Delta column-mapping preserves spaced column names 1:1), completing the "Databricks planned" half of ts-load-source-data (skill v1.1.0). **Live-verified on ps-internal**: created + populated `workspace.sisense_demo.orders_demo` (200 rows). Documents the OAuth/PKCE connection caveat (new table must be UI-selected on the connection before a model binds). 4 new unit tests
- fix(ts-cli): `ts connections add-tables` accepts `db`|`database` and errors cleanly instead of a raw `KeyError` traceback when a key is missing
- fix(ts-cli): `ts connections list` v0.56.0 — no longer defaults `--type` to `SNOWFLAKE` (which silently hid Databricks/BigQuery/etc. connections despite the "list all" docstring); now lists **every** warehouse type by default, `--type` filters. Caught live: the command was reporting only Snowflake connections on a multi-warehouse org
- feat(ts-cli): org support (v0.56.0) — `export TS_ORG=<org_id>` scopes CLI calls to a non-default ThoughtSpot org (passed as `org_id` int to `auth/token/full`; `org_identifier` is silently ignored by that endpoint — verified empirically), with an org-keyed token cache. Enables reaching org-scoped connections/metadata (e.g. an AnujSeth-org Databricks connection). 5 new unit tests
## 2026-07-17
- feat: add ts-convert-from-sisense skill + `ts sisense` command group (parse / build-model / build-liveboard)
- chore: bump ts-cli to v0.68.0

## 2026-07-15
- chore: ts-cli — add a `[snowflake]` optional-dependency extra and a clearer remedy message when `snowflake-connector-python` is missing for `method:python` profiles (covers `pip install 'thoughtspot-cli[snowflake]'` and the isolated `uv tool install ... --with` form). Aggregate advisor F2.
- docs: add ts-object-model-aggregates `references/remediation-plan.md` (phased plan from the first end-to-end aggregate build) and route findings into open-items.md #18.
- chore: bump ts-cli to v0.54.0 — `ts databricks translate-formulas` now rewrites Databricks JSON colon-path access (`col:a.b`, `parse_json(col):a.b`) to a `get_json_object` pass-through, since ThoughtSpot's `sql_*_op` parser rejects the colon syntax and bracket notation fails on Databricks VARIANT (live-verified 2026-07-15).
- docs: record the JSON-path pass-through rule across the formula-translation references (Snowflake→bracket, Databricks→get_json_object, Looker/Tableau cross-refs).
- fix(ts-cli): `ts tableau build-liveboard` v0.55.0 — two fixes a **live ps-internal import** caught that lint did not (round-trip verified against the real "Retail Sales - Classic" model): (1) a bucketed date's output column is renamed (`[Date].monthly` → `Month(Date)`), so chart/axis/table now reference the resolved name, not the raw one; (2) `custom_chart_config` column refs are GUIDs (assigned post-create), so a hand-authored display-name config errors `Invalid GUID string` on fresh import — the emitter now drops a display-name config and lets `ADVANCED_LINE_COLUMN` auto-resolve, replaying only genuine captured (GUID-based) configs. Corrected the shared combo worked example, `thoughtspot-chart-types.md`, and Step 10a accordingly
- feat(ts-cli): add `ts tableau build-liveboard` (v0.54.0) — deterministic Answer + tabbed-Liveboard TML emission from a parsed Tableau dashboard spec (role-aware axis layout, chart-type requirement floor, overrides capture-and-replay for format/client_state_v2/custom_chart_config/viz_style). ThoughtSpot-side logic ported from the verified standalone Power BI converter's generate_tml.py into `ts_cli/tableau/liveboard.py`; 21 unit tests. Codifies SKILL.md Step 10 chart/liveboard prose into tested Python (audit angle 11, agentic→deterministic). Parser-side role extraction is a tracked follow-on (ts-convert-from-tableau open item #20)
- feat(ts-cli): add `ts spotter answer` (v0.53.0) — ask Spotter (ThoughtSpot AI) a natural-language question over a Model and return its search `tokens`/`display_tokens`. Wraps the V2 endpoint `POST /api/rest/2.0/ai/answer/create` (`singleAnswer`, Beta 10.4.0.cl+; spec verified via `get-rest-api-reference`), surfacing structured `FORBIDDEN`/`UNAUTHORIZED`/`SPOTTER_ERROR` statuses. Pure `normalise_answer_response` + 10 unit tests. Live-verification against a Spotter-enabled instance is a tracked open item
- feat: ts-convert-from-tableau v1.28.0 — Spotter last-mile + chart/layout fidelity: new Step 12.6 uses `ts spotter answer` to express still-parked formulas (opt-in, verify-then-adopt, can seed a coverage tile); Step 10a durable dual-axis combo via `custom_chart_config`; Step 9c container-tree layout mapping + aspect-ratio sizing; Step 10b currency/number format, Color-shelf→`slice-with-color`/`trellis-by`, and sort fidelity. Live-verification gaps tracked as open items #17–#19

## 2026-07-14
- note: the aggregate-model advisor (`ts-object-model-aggregates` + `ts aggregate`) merged to `main` as **ts-cli v0.53.0**. The per-entry `v0.46.0`–`v0.49.2` references below were the feature branch's local bump sequence (developed in parallel with main's own v0.46–v0.52 work) — they describe the change history, not the shipped version numbers.
- fix: two RLS hardening fixes in `ts-object-model-aggregates`, from live testing + product-owner clarification (Task 26). **(1) Identical-rule dedup:** product owner clarified STRICT RLS is usually a single rule while NON-STRICT RLS repeats the SAME rule across many base tables (a cluster setting undetectable from our TML data) — `propagate_rls` previously emitted one output rule per base rule with no dedup, so non-strict RLS produced N byte-identical rules on the aggregate. Fixed: after remapping, rules whose REMAPPED expr is identical (whitespace-normalized) now collapse to ONE rule, keeping the first occurrence's name; genuinely different rules, and the Task 22 same-physical-column-name-maps-to-distinct-aggregate-columns collision fix, both still survive untouched (their remapped exprs differ, so the dedup key never matches). **(2) Pass-2 RLS attach now fails CLOSED:** `ts tables create`'s two-pass RLS registration (Task 25) printed a loud stderr error on pass-2 (attach) failure but still exited 0 — the one fail-OPEN link in an otherwise fail-closed pipeline, so a scripted caller saw "success" for a created-but-UNSECURED table. Fixed: the command now exits non-zero whenever any table's RLS attach fails, while still printing the name→guid JSON first (the manual-attach recovery instructions need the guid) — ts-cli v0.49.2 (`ts_cli/aggregate/rls.py`, `ts_cli/commands/tables.py`)
- fix: LIVE RLS testing of `ts-object-model-aggregates` surfaced two real bugs in Task 23's propagation path, both fixed. **Bug A:** `propagate_rls` omitted a `tables` sub-block naming the aggregate table — a known-good ThoughtSpot `rls_rules` block has `tables`/`table_paths`/`rules`, not just the latter two; omitting `tables` failed live import with `OBJECT_NOT_FOUND ... LOGICAL_TABLE`. Fixed: `propagate_rls` now emits all three sub-blocks in that order. **Bug B:** a single `create_new` import of the aggregate table TML that already carries a self-referencing `rls_rules` block also fails live (the self-reference can't resolve before the table exists), independent of Bug A. Fixed: `ts tables create` now registers RLS in two passes automatically whenever a spec carries `rls_rules` — create the table without it, then re-import with `rls_rules` + the created GUID at the document root + `--no-create-new` to attach it; a no-RLS spec is unaffected. `ts-object-model-aggregates` SKILL.md Step 6d documents the two-pass behaviour and Step 6e now confirms the rule actually attached live (`ts tml export` + compare) rather than assuming a non-null GUID means RLS is in effect. Multi-rule/multi-base-table propagation into a single aggregate block was confirmed already correct (Task 22's collision fix), no code change needed there — ts-cli v0.49.1 (`ts_cli/aggregate/rls.py`, `ts_cli/commands/tables.py`)
- feat: wire RLS propagation (Task 22's pure `ts_cli/aggregate/rls.py`) into `ts aggregate recommend`/`generate` — `recommend` surfaces per-candidate `rls: {required, missing}` + `rls_conflict` (plus an `rls_conflicts` stdout summary) from the tables-dir Table TMLs' `rls_rules`; `generate` recomputes the conflict and FAILS CLOSED (before writing any file) if a candidate's grain still omits a required RLS filter column, otherwise remaps the base rule(s) onto the aggregate's own grain columns (`propagate_rls`) and attaches them to the aggregate Table TML before it's written — replacing the old manual "apply RLS in the UI" hard gate. `ts-object-model-aggregates` SKILL.md Step 5 now prompts exclude-vs-force-add per conflicting candidate, Step 6e shows/confirms the auto-propagated rule instead of gating manually, Step 7 adds the RLS leak-test (routed query as an RLS-restricted user) as the live gate, and Step 5d's candidate table now shows measures carried + the proposed aggregate name — ts-cli v0.49.0 (`ts_cli/commands/aggregate.py`, `ts_cli/commands/tables.py`)
- fix: `ts aggregate generate` emitted an aggregate table/model name containing a raw space for multi-word dimensions (e.g. `DM_CATEGORY_AGG_MONTHLY_PRODUCT CATEGORY` for "Product Category") — an unquoted SQL identifier with a space breaks `CREATE TABLE`. `_aggregate_name` now sanitizes the assembled name (and any `--agg-name` override) via `sanitise_name` (shared with `ts load`'s warehouse identifier derivation), collapsing non-`[A-Z0-9]` runs to a single underscore — ts-cli v0.48.1 (`ts_cli/commands/aggregate.py`)
- fix: `ts aggregate generate`/`profile`'s SpotQL-wrapping DDL path (Task 18) emitted invalid SpotQL — live-proven on an aggregate-aware cluster: `SUM(physical_column)` and `start_of_month(date)` both `QUERY_GEN_ERROR` (SpotQL is a semantic layer over display names with no date-bucket UDF). `build_spotql` now references measures by display name and selects raw date columns; `wrap_as_ddl` does the date bucketing + measure re-aggregation itself in an outer `DATE_TRUNC(bucket, ca_N)`/`reagg(ca_N)` SELECT, the exact shape proven live end-to-end (192-row monthly aggregate, total matched the detail exactly). Scoped to single-component SUM/MIN/MAX/COUNT measures; AVG/RATIO raise `UnsupportedMeasureError` (falls back to `sqlgen.build_select`) rather than emit a guess — ts-cli v0.48.0 (`ts_cli/aggregate/spotql_aggregate.py`)
- feat: pivot `ts aggregate generate`/`profile` DDL and profiling SQL to wrap ThoughtSpot-generated SQL (`ts spotql generate-sql`) instead of the hand-rolled join walker, fixing wrong joins on role-playing/ambiguous-path dimensions — ts-cli v0.47.0 (`ts_cli/aggregate/spotql_aggregate.py`; `sqlgen.build_select` kept as a `--no-spotql` fallback)

## 2026-07-13
- feat(ts-cli): bump to v0.52.0 — `ts spotql classify-columns` now detects **semi-additive** measures (outermost op `last_value`/`first_value`) as a new `semiadditive_measure` kind and adds a `wrapper` field (`AGG`/`SUM`/`None`) to every result. Fixes a misclassification: these must be referenced in SpotQL as `SUM(...)` (`AGG()` errors `NON_CONVERTIBLE_FUNCTION`), the inverse of ordinary aggregate-formula measures. Live-verified on nebula-aggregate-aware
- feat: ts-object-model-spotql-query v1.4.0 — semi-additive `SUM`-vs-`AGG` wrapper rule (Step 2/3, spotql-rules.md, limitations.md, patterns.md); corrects earlier docs that wrongly showed `AGG("Inventory Balance")` for a `last_value` snapshot measure
- feat(ts-cli): `ts model promote-formula` CLI command (BL-066, ts-cli v0.51.0) — codifies formula promotion merge as deterministic Python: duplicate detection, reference mapping, column_type inference, TML merge, parameter co-promotion
- feat: ts-object-answer-promote v1.4.0 — Steps 7–10 delegate to `ts model promote-formula` CLI command (BL-066)
- feat: adopt `ts profiles` CLI commands across 4 profile skills (BL-084 PR2) — ts-profile-thoughtspot v1.2.0, ts-profile-snowflake v1.1.0, ts-profile-databricks v1.1.0, ts-profile-tableau v1.1.0. Replaces hand-coded slug derivation, keychain commands, env var naming, and profile JSON I/O with the shared CLI substrate from PR1
- feat(ts-cli): `ts profiles add/update/remove/sync-env` + `list --json` — codifies profile substrate (BL-084 PR1, ts-cli v0.50.0). New `profile_ops` module consolidates slug derivation, env-var naming, keychain command generation, zshenv upsert, and profile JSON CRUD across 4 platforms. Legacy `load_profiles`/`load_tableau_profiles` delegate to the shared module
- docs: shared profile-select-and-authenticate reference for conversion skills
- feat(ts-cli): bump to v0.50.0 — auto-generate `sql_*_op` pass-through formulas for LOWER, UPPER, MINUTE, SECOND, and DATE_FORMAT instead of skipping them as untranslatable. Eliminates the manual export→edit→reimport cycle for these functions. DATE_FORMAT bakes the format literal into the SQL template
- fix(ts-cli): bump to v0.49.0 — CASE WHEN / IF() translation now emits correct ThoughtSpot `if (cond) then val else val` syntax instead of the comma-separated `if (cond, val, val)` form that was rejected by the TML import formula parser. DATE_TRUNC now handles sub-day units (HOUR, MINUTE, SECOND) via `sql_date_time_op` pass-through instead of raising UntranslatableError. Updated worked examples and formula translation doc

## 2026-07-12
- feat(ts-cli): add `ts snowflake exec` (v0.48.0) — run a `.sql` template or inline `-q` query against a Snowflake profile with deterministic `--var name=value` placeholder substitution, reusing the one connector in `load.py` (both `python`/`cli` methods). Codifies the two `ts-recipe-formula-*-snowflake` skills (BL-079, audit 11.2/11.3): UDF DDL now lives in `references/*.sql` templates instead of markdown fences the agent retyped each run, and Steps 1/3/4 no longer inline a `snowflake.connector` connect block that had drifted from `load.py`. New `check_patterns` rule 7 (cloned `snowflake.connector.connect(` in a SKILL.md; `ts-profile-snowflake` allowlisted) keeps the ~30-line clone from re-appearing. Smoke tests deploy via the real command against the single-source templates — **live-verified on se-thoughtspot** (business-days via python/key_pair, hms via cli). `json_safe_value` coerces the `Decimal`/`datetime`/`bytes` the connector returns so a timestamp/decimal result no longer crashes `json.dumps`. 16 new unit tests
- refactor(validate): consolidate the runtime skill-dir list into a shared `tools/validate/_dirs.py` (BL-110, audit 4.4). 14 validators independently hard-coded `('agents/cli','agents/claude','agents/coco-snowsight')` (and its CLI-family / CoCo-alone variants); a dir rename meant ~18 edits and a missed one silently PASSed. `_dirs` exposes `ALL_RUNTIMES`/`CLI_RUNTIMES`, `ALL_RUNTIME_PATHS`/`CLI_RUNTIME_PATHS`, the `CLI`/`CLAUDE`/`COCO` scalars, and a `runtime_globs()` helper; a `test_dirs.py` guard fails if any listed runtime dir is missing on disk. Verified output-neutral across all ~28 validators (only cosmetic diffs: `check_runtime_coverage --verbose` column order → canonical cli-first, and `check_smoke_tests`' self-referential ALLOWLIST line numbers). 9 new tests
- fix(ts-cli): bump to v0.47.1 — `_stdin_has_piped_content()` no longer hangs forever on an idle open non-TTY stdin (BL-097). A background/script shell whose stdin is an open-but-empty pipe made the unconditional `sys.stdin.read()` block waiting on an EOF that never came (Task 5 workaround was `< /dev/null`). Now `select()`-guarded: read only when the fd reports readable within a zero timeout (data → content, EOF/`< /dev/null` → empty, regular-file redirect → readable); an idle open pipe reports not-ready and returns False without reading. Falls back to the prior blocking read where `select` can't poll the handle (e.g. Windows non-socket). 5 new tests incl. a real-pipe hang guard that fails against the old code

## 2026-07-11
- fix(validate): `check_audit_freshness` — drop ts-cli version bumps as a full-audit *activity* trigger. The internal CLI version isn't audit surface (audits examine skills/mappings/currency/security), bumps are too frequent to signal anything, and the real churn they ride along with is already caught by the commit-count trigger. Activity now = new skill / new runtime / 2+ new shared refs / 40+ commits. With this + the SHA baseline, the checker is silent again after the 2026-07-11 wave
- fix(validate): `check_audit_freshness` — measure full-audit *activity* from the report's commit SHA (`git log <report-sha>..HEAD`), not its calendar date. The date baseline swept in same-day-as-the-report work (the audit's own routing wave) as "substantial work since the audit", and double-counted each ts-cli bump (it edits 2 version files). Now counts per-commit and only work strictly after the report. New pure `_parse_activity`/`_latest_full_audit_commit` + 6 tests
- fix(validate): `check_audit_freshness` — a full audit now satisfies the external-sweep cadence (it runs all angles, external included). Previously the external nudge measured only from `*-external.md`, so it kept firing "~13d overdue" the day after a full audit until a separate external-only sweep ran
- fix(codification): migrate shared `ts-tml-import-gate.md` off the superseded stdin JSON-array wrapper to `ts tml lint --file`/`--dir` + `ts tml import --file`/`--dir` (audit 5.1 remainder, BL-117); extend `check_patterns` Check 6 to scan `agents/shared/**/*.md` so the shared doc can't regress (2 new tests). Post-merge: `./scripts/stage-sync.sh` (shared file → CoCo stage). No skill version bumps — the referencing SKILL.md files are unchanged
- feat(ts-cli): bump to v0.47.0 — `ts metadata search --connection <name>` (`-c`), a client-side case-insensitive filter on `metadata_header.dataSourceName` for connection-scoped table discovery (BL-111 part a; pure `filter_by_connection` helper + 11 tests). Deconflicts with the concurrent v0.46.0 CI PR — merge that first
- chore(cleanup): retire `agents/claude/references/direct-api-auth.md` (curl + `/tmp/ts_token.txt` fallback now prohibited by `ts-cli.md`/`security.md`) + its two dead reference rows in ts-convert-from-snowflake-sv (→1.16.1) / ts-convert-to-snowflake-sv (→1.3.2); drop the now-stale note in `check_orphan_references.py` (BL-109)
- fix(hooks): `pre-push.sh` now smoke-tests only dirs with a `SKILL.md` — a shared dir matching the skill path pattern (`agents/claude/references/`) or a fully-deleted skill dir no longer trips a false "no smoke test found" failure (surfaced by BL-109's deletion)
- chore(ci): bump ts-cli to v0.46.0 — lift the CPython cap to `>=3.10,<3.15` (BL-106) + add a `pytest-matrix` CI job running the unit/validator suite on 3.10/3.11/3.13/3.14 (BL-107); 3.14 entry verifies the lifted cap. Validators/linters stay single-version (3.12) in `validate`
- chore(security): bump ts-cli to v0.45.2 — `requests>=2.33.0` floor (closes GHSA-gc5v-m9x4-r6x2 / transitive urllib3 PYSEC-2026-141/-142; BL-105); SHA-pin GitHub Actions `checkout` (v4.3.1) + `setup-python` (v5.6.0) in validate.yml (BL-108)
- feat(validate): 2026-07-11 audit backlog+validators batch — new `check_orphan_references` (kills the dead-reference-file class; deleted 2 obsolete Mode C specs); extend `check_references` to `references/*.md` + `docs/`; `check_smoke_tests` ALLOWLIST BL-reference gate; `check_patterns` Check 6 (blocks the stdin `ts tml import` wrapper); fix `check_open_items` WONTFIX-convention false-positives; weekly scheduled `pip-audit` CVE sweep (audit 1.1/1.4/5.1/6.3/16.3)
- chore(skills): migrate ts-convert-from-looker (1.0.1) + ts-object-model-coach (2.3.2) to `ts tml import --file`/`--dir` (audit 5.1); file BL-102–BL-117 for the remaining backlog-bucket findings
- docs(mappings): 2026-07-11 audit mapping-update batch — Databricks MV `parameters:` GA (18.2+) + tiered runtime; Snowflake SQL-query logical tables (`base_table.definition:`), free-text AI instructions, `is_enum`/`sample_values`; thoughtspot-* + ts-to-snowflake-rules currency anchors bumped to 2026-07 (audit 13.1/13.4–13.7/13.10/13.12)
- chore(skills): bump ts-convert-from-snowflake-sv 1.16.0, ts-convert-to-snowflake-sv 1.3.1, ts-convert-to-databricks-mv 1.1.1, ts-convert-from-databricks-mv 1.8.2
- feat: add ts aggregate command group (aggregate-model advisor engine) — ts-cli v0.46.0
- chore: bump ts-cli to v0.45.1 — fix `resolve_profile` crash on the wrapped `{"profiles":[...]}` file format (audit 4.1) + consolidate triplicated profile parsing on `load_profiles()` (audit 4.2)
- fix: rewire `smoke_ts_dependency_manager.py` onto the real `ts dependency backup/apply-change/rollback` surface with a destructive-leg opt-in gate (audit 6.1)
- feat: add ts-convert-from-looker skill (community contribution, PR #201)
- docs: formula function-composition rules + TML import behaviours across shared references (thoughtspot-formula-patterns, ts-snowflake-formula-translation, ts-from-snowflake-rules) + ts-convert-from-snowflake-sv v1.15.0 — SE-cluster-validated batch

## 2026-07-10
- docs: shared ts-tml-import-gate.md extracted from ts-convert-from-databricks-mv (1.8.1) + ts-convert-from-snowflake-sv (1.14.1) — pre-import lint gate + import policy now one reference (BL-063 PR5)
- docs: `ts-convert-from-snowflake-sv` v1.14.0 — cumulative window metrics corrected to `moving_sum(group_aggregate(...))`, new `COUNT_IF` mapping, limitations L6 (BOOL-in-`if` parentheses) and L7 (two-pass formula import), verified on SE cluster
- feat: add `ts databricks build-model` — deterministic ThoughtSpot Model (+ Table) TML assembly from `parse-mv`/`translate-formulas` JSON, with a TML invariant/lint gate (files always written for inspection) and an optional `ts tml import --policy PARTIAL` of the model TML (BL-063 PR4 Task 5, ts-cli v0.44.0)
- feat: `ts-convert-from-databricks-mv` Steps 5/6/9/9.5/10/11 rewired onto the deterministic 3-command pipeline (`ts databricks parse-mv` / `translate-formulas` / `build-model`) instead of inline LLM parsing/translation/TML assembly; `tables.json` moves to a v2 shape (BL-063 PR4, `ts-convert-from-databricks-mv` v1.8.0)
- fix: ts-cli live e2e fixes found and closed during BL-063 PR4's end-to-end gate against se-thoughtspot — flat import-response GUID shape in `extract_imported_guid`; connection-scoped GUID resolution + `BOOLEAN`→`BOOL` data-type normalization in `ts tables create` (previously could resolve to a same-named table on the wrong connection, or reject a valid `BOOLEAN` column); in-band `ERROR`-status import responses now surfaced as a non-empty `import_error` by `ts databricks build-model` instead of being swallowed. The `ts tables create` fixes affect all callers of that command, not just this skill (BL-063 PR4)
- feat: Genie Code `ts-convert-from-databricks-mv` v1.1.0 — rewired onto the vendored `databricks_mv_lib` notebook (`%run ../../notebooks/databricks_mv_lib`, deploy-time concatenation of `parse_metric_view`/`translate_metric_view`/`build_table_tml`/`build_model_tml_dbx`/`validate_tml_invariants`/`lint_tml`/`dump_tml_yaml`/`extract_imported_guid` from `tools/ts-cli/`), replacing the hand-rolled "identical to the CLI skill" steps with the same deterministic pipeline the CLI skill runs via `ts databricks ...` (BL-063 PR5 Task 8)
- feat: BL-063 PR5 — add `agents/databricks/build_mv_lib.py` (invoked by `deploy.sh`), the generator script behind the vendored `databricks_mv_lib` notebook; relocate `dump_tml_yaml`/`extract_imported_guid` into new `ts_cli/tml_common.py` and `resolve_name_collisions`/`add_formula_prefix`/`expr_is_aggregated`/`fix_double_aggregation` into new `ts_cli/formula_common.py` — both platform-neutral and Genie-vendorable, with backward-compat re-export shims left in `tableau/yaml_out.py` and `tableau/naming.py`; close out BL-099 (flat import-response GUID parsing now shared via `tml_common.extract_imported_guid` at `tml.py`/`dependency.py`/`tables.py`; duplicate display-title guard extended across all `columns[]` in `mv_build_model.py`; duplicate-identifier guard added to `parse-mv` in `mv_parse.py`) (ts-cli v0.45.0)

## 2026-07-09
- feat: ts databricks translate-formulas — deterministic MV formula translation (BL-063 PR3, ts-cli v0.43.0)
- feat: add `ts databricks parse-mv` — deterministic Metric View YAML parser (ts-cli v0.42.0, BL-063 PR2; BL-098 density-check warning on trailing/leading window measures)
- docs: Databricks Metric View window-semantics deep-analysis (BL-063 PR1) — live-verified trailing/leading/cumulative/all/semi-additive range behavior against a Databricks fixture + ThoughtSpot number-match on both platforms. Corrected 2 window mappings: trailing/leading anchor args (`moving_sum([m], N, 0, [date])` actually reproduces the *inclusive* form, not the default/exclusive one — now `moving_sum([m], N, -1, [date])` / `moving_sum([m], -1, N, [date])`) and the `range: current` + `offset` mechanism (row-relative `moving_sum([m], N, -N, [date])` LAG idiom, not a wall-clock `sum_if(diff_months/quarters/years(...), today())`). Documented the `materialization:` block for the first time. Updated `databricks-metric-view.md`, both `ts-databricks-*` mapping files, `ts-convert-from-databricks-mv/SKILL.md` (v1.6.0 — resolves previously-PENDING `leading`/`all`), and `ts-convert-to-databricks-mv/SKILL.md` (v1.0.3). Closes BL-064 items 10 (window range types undocumented) and 13 (`materialization:` undocumented); confirms item 11 (phantom `entities:`/`db_connection:` syntax) already resolved and item 12 (`safe_divide` comment) partially deferred. Files BL-095 (`connections add-tables` missing `authenticationType` + instance `updateConnectionV2` 500s), BL-096 (SpotQL `generate-sql`/`fetch-data` return empty-body 500s, worked around via `searchdata`), and BL-097 (`ts tml import` hangs on an open non-TTY stdin). No ts-cli code changes — sets up BL-063 PR2 (`ts databricks parse-mv`/`translate-formulas`). See `docs/audit/2026-07-08-dbx-window-claim-matrix.md`.
- docs: Databricks dimension/metric semantic deep-dive (BL-063 PR1.5) — live-verified LOD dimension × filter interaction, cross-measure ratio × query grain, global `filter:` × window ordering, and semi-additive × date-range filter behavior against a Databricks fixture + ThoughtSpot number-match on both platforms. Two divergences: (1) trailing/leading window frame semantics (E1, and the frame half of C1's split verdict) — Databricks is date-interval framed, ThoughtSpot `moving_sum` is row-positional, so PR1's C1/C3 CONFIRMED verdicts were density-conditional (dense-fixture only) and now carry a sparse-data caveat everywhere trailing/leading appears; (2) LOD-dimension filter-kind asymmetry (A2) — a Databricks MV's own global `filter:` is filter-aware for a partition-window LOD dimension, an ad hoc query-time `WHERE` on an unfiltered MV is not, a DBX-internal asymmetry with no ThoughtSpot analogue (ThoughtSpot is filter-aware under both filter kinds). Three confirmations: cross-measure ratio inlining is grain-safe at every grain tested (B1); global-filter × window ordering is filter-before-window on both platforms (C1 ordering half); semi-additive `last`/`first_value` collapses to the in-filtered-range observation on both platforms (D1). 10 doc files corrected/confirmed across `databricks-metric-view.md`, both `ts-*-databricks-rules.md` mapping files, `ts-databricks-formula-translation.md`, `ts-databricks-properties.md`, `thoughtspot-formula-patterns.md`, both Databricks worked examples, and both `ts-convert-*-databricks-mv/SKILL.md` files (skill version bumps consolidated with the A3 entry below — one row per shipped change per versioning.md). Filed BL-098 for the E1/C1-frame divergence's follow-up action items (PR2 density-check warning, PR3 sparse-data-risk annotation). No ts-cli code changes — closes the PR 1.5 gate on BL-063 PR 3 (`ts databricks translate-formulas`). See `docs/audit/2026-07-09-dbx-semantic-claim-matrix.md`.
- docs: Databricks A3 follow-up (user-suggested, BL-063 PR1.5) — corrects the A1/A2 "no ThoughtSpot analogue" conclusion for DBX's MV-filter-aware + query-time-`WHERE`-blind composite LOD behavior. Live-verified that `group_aggregate(sum(x), {dim}, {})` is blind to a search-level/query-time filter but still respects a model-level `filters:` block — exactly DBX's composite — so `{}` + a model-level filter mirroring the MV's `filter:` reproduces both halves in one ThoughtSpot construct. `query_filters()` remains the default LOD mapping; `{}` is the new refinement for reproducing DBX's query-time-blind reading specifically. Also live-tested the documented subtraction form `query_filters() - {col}`: import-accepted but does not exclude a filter pinned on a derived boolean formula built from the subtracted column. 9 doc files updated: `databricks-metric-view.md`, both `ts-*-databricks-rules.md`, `ts-databricks-formula-translation.md`, `ts-databricks-properties.md`, `thoughtspot-formula-patterns.md`, `ts-to-databricks.md` worked example, and both `ts-convert-*-databricks-mv/SKILL.md`. MINOR-bumped `ts-convert-to-databricks-mv` to v1.1.0 and `ts-convert-from-databricks-mv` to v1.7.0 — one consolidated changelog row per skill covering both the PR1.5 corrections/caveats and the A3 reproduction-path capability (the MINOR justification), since both shipped in the same PR. No ts-cli code changes; no new backlog item (resolved refinement, not an open divergence). See `docs/audit/2026-07-09-dbx-semantic-claim-matrix.md` (A3).

## 2026-07-08
- feat: ts-cli v0.40.0 — `ThoughtSpotClient.request()` retries transient gateway faults (502/503/504) + connection/timeout errors with exponential backoff, then fails cleanly (no `JSONDecodeError` traceback on a 504 HTML page). Closes BL-089 1c (export resilience) → BL-089 fully done
- feat: ts-cli v0.38.0 — `ts tableau parse`/`build-model` accept a published-datasource `.tds`/`.tdsx` (physical tables/joins/columns/calcs), so multi-query datasources build multi-table models via GENERATE mode without hand-assembly (BL-089 M8)
- docs: update ts-convert-from-tableau to v1.27.0 — `.tds`/`.tdsx` parse path + Step 3.5 correction (field API returns columns not joins)
- feat: add ts dependency backup / mutate / rollback (ts-cli v0.39.0, BL-083 PR1 substrate — codifies ts-dependency-manager backup/rollback + pure REMOVE/REPOINT transforms)
- feat: add `ts dependency apply-change` (ts-cli v0.41.0, BL-083 PR2 — codifies ts-dependency-manager Step 9 destructive orchestrator: drift → delete → dependent-fix → source → set-delete; corrects the source-first ordering bug to source-LAST per error 14544; ts-dependency-manager skill v1.4.0). Live-verified on se-thoughtspot (open-item #23 green end-to-end + rollback); the live run found+fixed two mutation/rollback bugs (open-items #24 aliased-column strip, #25 root-first rollback order)

## 2026-07-06
- feat(tableau): `build-model` now converts Custom SQL relations (`<relation type='text'>`) into **SQL View TML** and references them by name in the model — previously Custom-SQL-only datasources were dropped. Includes physical/SQL-View column dedup and formula resolvability against SQL View columns. Verified end-to-end live (parse → emit → import → query). Bumps ts-cli to v0.37.0 (ts-convert-from-tableau v1.26.0)

## 2026-07-05
- docs: update ts-convert-from-tableau to v1.25.0 — multi-query→multi-table model guidance + liveboard parameter rule (BL-090)
- fix(tableau): match IF/END case-insensitively so mixed-case nested IF (outer `END`, inner lowercase `end`) fully converts — closes the last Start/End Date import failures (bumps ts-cli to v0.36.1). Follow-up to #185, which squash-merged before this commit.
- fix(tableau): formula translation — boolean aggregation `MAX/MIN(<cmp>)` → `agg(if <cmp> then 1 else 0)` (M6a), and scope columns inside string-literal-adjacent concat so `'[' + STR([id])` labels resolve (M7); bump ts-cli to v0.36.0
- chore: bump ts-cli to v0.35.0
- fix(tableau): resolve multi-table formula failures deterministically in build-model — table-aware bare-ref qualification, qualified-column + cross-formula-cascade filtering, parameter auto-migration in the --existing-guid merge flow, and cascade-aware import retry (default --max-retries 25→10)

## 2026-07-04
- fix: ts tableau build-model --existing-guid now applies --column-name-map (ts-cli v0.34.0, ts-convert-from-tableau v1.24.0)
- feat: bump ts-cli to v0.33.0 — build-model column-schema reconciliation (sqlproxy suffix/junk/qualify + --reconcile-table)
- fix: bump ts-cli to v0.32.1 — classify-formulas classifies per datasource (was flattening multi-datasource workbooks and deduping shared calc names; live-test finding)
- chore: bump ts-cli to v0.32.0
- feat: ts tableau parse + classify-formulas commands (Tableau audit/migrate convergence)

## 2026-07-03
- chore: bump ts-cli to v0.31.0 (ts spotql classify-columns — BL-087)
- chore: bump ts-cli to v0.30.0 (ts snowflake diff + lint-ddl — BL-063 quick wins)
- chore: bump ts-cli to v0.29.0 (ts tableau build-model --table-name-map)
- docs: full repo audit 2026-07-03 — report + same-day routing (PRs #168-#176, BL-073..BL-087), agentic→deterministic codification review
- feat: add 5 audit-harvest validators — `check_no_inline_requests` (SKILL.md code fences must not instruct `requests`/`urllib` calls, finding 5.2), a `DEPRECATED_V2_ENDPOINTS` denylist in `check_no_v1_endpoints` (bare batch `/template/variables/update-values` path, finding 13.1), `check_pagination_convention` (AST-scoped guard against a hard-capped `record_size` literal with no pagination loop, finding 14.2), `check_slash_command_refs` (every `/ts-<skill>` mention in agents/ docs must resolve to a real skill directory, finding 1.1), and `check_skill_flag_usage` (cross-checks every `ts <group> <command> --<flag>` a SKILL.md documents against the real typer command tree, finding 11.1b). Wired into pre-commit + CI; dated the two `check_smoke_tests` ALLOWLIST exemptions with BL-076 (finding 6.3)
- chore: bump ts-cli to v0.28.0 — per-identifier variables endpoint, full auto-pagination, dependent matched-columns field, dev extra
- chore: bump ts-cli to v0.28.1 — Tableau fail-loud: 13 spatial + USERATTRIBUTE user functions
- fix: ts-cli v0.27.0 — impact-report deep probes (RLS/Monitor/Joins/Spotter AI surface/Column alias rows) no longer report `checked=True, found=0` when the backing TML export raises; a failed probe now flips the affected row(s) to `checked=False` and surfaces a warning in the report (audit 4.1). `ThoughtSpotClient.request()` now clears the cached token and retries once on a 401 instead of treating a rotated/revoked token as valid for up to 23h (audit 4.2). `ts tml import`/`ts tml lint` gain `--file` (repeatable) and `--dir` (non-recursive) raw-TML-file input, alongside the existing stdin JSON-array interface — makes the `ts tml import --file <path>` command already documented in ts-convert-from-snowflake-sv/ts-convert-from-databricks-mv SKILL.md actually work (audit 11.1)
- fix: ts-audit v2.4.0 — 3 report bugs: scorecard GUID-matching, false orphans from pagination + associated export
- chore: bump ts-cli to v0.26.6
- chore: bump ts-cli to v0.26.5 — ACOS/ASIN/ATAN/COT rejected loudly at translate time (BL-072 partial)
- chore: bump ts-cli to v0.26.4 — move TableauClient to ts_cli/tableau/client.py; check_file_size allowlist now empty
- fix: ts-cli v0.26.3 — ts-load-source-data synthetic data: a numeric column (INTEGER/FLOAT) whose name merely contains "name"/"customer" no longer gets random person-name strings (type mismatch); it now falls through to the numeric generator. Adds a regression test
- chore: bump ts-cli to v0.26.2 — BL-069 follow-ups: dead-code removal, filter_unresolvable_formulas annotation fix, module-health baseline re-key
- chore: bump ts-cli to v0.26.1 — decompose build_model_cmd into ts_cli/tableau/build_model.py + flow functions (BL-069 follow-up)
- feat: update ts-object-model-erd to v1.7.0 — subject-area grouping: a **Group by** selector colors tables into subject areas via three selectable strategies (Name prefix; Graph cluster via deterministic Louvain-style modularity — ~20 communities on the 79-table GTM export; Fact neighbourhood via nearest-fact `(dist,factId)` relaxation). Non-destructive shared display — accent stripe per node, dark AA-contrast tint on overview/LOD blocks, and a legend with ghost-only single-group highlight; layout, fact/dim fill, and RLS/focus borders untouched. Also hardens `esc()` to escape quotes (+ fixes an unescaped `findingCard` data-target) and refreshes the Review-notes panel after Clear notes
- feat: update ts-object-model-erd to v1.6.0 — notes UX: save/delete give immediate feedback (shared `commitNotes()` exit, inspector re-renders after save/delete so Delete is reachable without a reload, self-dismissing "Saved ✓"/"Note deleted" confirmation); new **Notes** filter chip highlights every noted table (including at overview/LOD zoom) and keeps a noted join emphasized even without a noted endpoint, correctly enabling/disabling on model load (including Share-HTML baked notes) and never leaving the diagram ghosted after the last note is removed; new **Review notes** panel lists every note with a Table/Join/"not in model" tag (stale keys are delete-only, never clickable), click-to-jump moves the canvas, inline delete
- feat: ts-cli v0.26.0 — 14 Tableau function translations implemented (were silent pass-throughs); unmapped functions and unknown date units now fail loud at translate time; scalar MAX/MIN and IN(...) scan bugs fixed
- docs: ts-convert-from-tableau doc/mapping drift refresh — SKILL.md command fixes, mapping-file alignment with v0.26.0 code, matrix/open-items verification pass, gap documentation + 2 new backlog items

## 2026-07-02
- refactor: split Tableau pipeline into ts_cli/tableau/ package (BL-069); bump ts-cli to v0.25.4
- fix: validator test fixtures no longer corrupt the real repo when pytest runs under pre-commit hook env (GIT_* scrub)
- feat: update ts-object-model-erd to v1.5.0 — overview legibility (semantic zoom): node/edge strokes (incl. the edge hit-path) now use `vector-effect:non-scaling-stroke` so borders stay crisp at any zoom; below 0.5× zoom, tables render as bold color-coded overview blocks (solid saturated kind fill + state-aware border + centered title) instead of full column cards, restoring full detail above the threshold; no layout/position change
- feat: update ts-object-model-erd to v1.4.0 — large-model navigation: scroll/trackpad pans (pinch/⌘-scroll zooms), pan-drag no longer resets focus, arrow/`+`/`-`/`0` keyboard nav, an always-on collapsible minimap with click/drag-to-navigate, a shared `MIN_K=0.12` fit floor (fit/wheel/zoom-out), fit-to-focus, and search-zooms-to-readable. Also fixes a latent crash when a join references a table with no Table TML and no `model_tables` entry
- fix: update ts-object-model-erd to v1.3.0 — `build_erd.py` ingests a `ts tml export` JSON dump directly (routes model/table by TML content, not filename) and fails loud when no model is present, instead of silently rendering an empty diagram; SKILL.md Step 3→5 rewritten as a clean pipe. RLS model corrected: highlight only the table a rule is defined on (secured) and any table its expression references (in RLS path) — removed the incorrect join-propagation ("RLS inherited via joins", "RLS edge", propagating rule cards, join-ancestor subgraph). Column inspector: column groups are now flat headed sections instead of nested `<details>`, which left column rows hidden in some browsers
- feat: update ts-object-model-erd to v1.2.0 — `--ai-analysis` flag injects an agent-synthesized business-context corpus (domain/objectives/audience/questions/AI-instructions) into the ERD; column inspector surfaces per-column AI context + synonyms; RLS legend parity (secured tables show red border + lock by default); double-click/compare focus hides out-of-scope tables; parser handles the nested `rls_rules` TML shape
- feat: add `check_module_health` validator — a cyclomatic-complexity ratchet (radon) that blocks new/worsening god-functions against a committed baseline; wired into pre-commit (staged) and CI (full). Adds an automated component to repo-audit Angle 4 (Tools quality)
- docs: repo-audit rubric now names the code-health sweep tooling — `vulture` (dead code, Angle 1), `jscpd` (code duplication, Angle 11), and `agentlinter` as an optional local-only instruction-hygiene advisory (Angle 2); these run during the sweep, not as per-PR gates
- chore: remove dead code — `agents/cli/ts-audit/analyzer.py` + `report.py` and their orphaned tests (4,722 lines), superseded by the codified engine in `tools/ts-cli/ts_cli/audit/*` (the audit skill runs via `ts audit`, not these files; one test already imported a since-deleted module). BL-069 raised to high priority; BL-070 marked partially done
- chore: remove dead import (`render_json`) in ts-cli metadata command (vulture)
- fix: audit cluster heatmap no longer rolls unattributable findings onto the first model in multi-model audits (was inflating that model's severity across angles)
- fix: ERD fact/dimension classifier — an outgoing join alone no longer marks a table a fact; only real (visible) measures do, with measureless pass-through tables as bridges
- fix: ERD viewer no longer errors when clicking a flagged fan-out join with no attached finding
- chore: bump ts-cli to v0.25.3
- refactor: ERD generation consolidated to a single definition in `agents/shared/erd` (parser + assembler moved out of the ts-object-model-erd skill); ts-audit ERD embed now consumes it instead of a duplicate parser
- fix: ERD dimension/fact classifier — hidden and non-measure formula columns (e.g. RLS/parameter helper formulas) no longer promote a dimension to a fact
- feat: ERD layered layout clusters joined tables via Sugiyama median crossing-reduction
- chore: bump ts-cli to v0.25.2
- feat: ERD — AI domain summary, multi-select filter chips, RLS red colour scheme, grouped controls bar, join-tree double-click
- chore: bump ts-cli to v0.25.1

## 2026-07-01
- feat: wire ts-object-model-erd into ts-audit report — ERD button opens interactive diagram per model
- feat: ts-cli performance — `requests.Session` pooling, BFS frontier batching, Liveboard export batching
- fix: bump `requests>=2.32.4` (CVE-2024-35195, CVE-2023-32681), raise Python floor to >=3.10
- docs: update Muze charting status to phase 1 GA, Snowflake Advanced constructs YAML coverage
- chore: bump ts-cli to v0.25.0
- feat: add ts-object-model-erd skill
- feat: add shared ERD renderer module (`agents/shared/erd/`) — data-driven CSS + JS + render.py ported from validated mockup
- feat: update ts-audit to v2.2.0 — report UI fixes (accessible heatmap, clickable breadcrumbs/KPIs, check metadata, AI analysis panel)
- chore: bump ts-cli to v0.24.0
- feat: add `ts audit report` command — unified HTML report
- chore: bump ts-cli to v0.23.0
- chore: delete superseded `efficiency_report.py`
- feat: add `ts audit run` command — codifies all 51 audit checks (A1-A5, D1-D12, H1-H10, P1-P18, S1-S10) as deterministic Python
- fix(audit): include report_template.html in wheel package-data

## 2026-06-28
- feat: add Migration Pace (Fast/Complete) to ts-convert-from-tableau — Fast parks failed formulas; Complete enters bounded fix cycle; Step 12.5 resume prompt for post-report fixes
- feat: add `--max-retries` flag and enriched `formulas_dropped_on_import` dict to `build-model` command
- feat: wire `ts tableau build-model` into SKILL.md Step 7 Phase 2 — replaces inline Python formula assembly (root cause of 1,389-tool-call migration)
- feat: add dual-join table alias detection to model_builder — same physical table with different aliases (e.g. `d_partner` / `d_partner1`) now preserved
- feat: add `validate_pre_import()` + `add_formula_prefix()` integration to `build-model` command (both flows)
- feat: add `fix_bare_refs()` post-pass — table-qualifies bare `[Column]` refs and prefixes formula cross-refs in `build-model --existing-guid` flow
- feat: add single-table force-remap for sqlproxy columns — 100% remap when model has one table
- feat: add `max(bool)=false` pattern detection to `validate_pre_import()`
- fix: strip Tableau double-quote wrapping from parameter defaults and member values
- fix: orphaned END/CASE keyword strip after failed `convert_if_then`/`convert_case_when` parsing
- fix: extend `_DATE_INDICATORS` regex to catch date column names (prevents `else ''` on date expressions)
- fix: `--max-retries` default 10→25 for complex workbooks with cascading formula dependencies
- docs: update SKILL.md — auth via `ts profiles list`, model TML schema read, GUID capture, duplicate detection, datasource name matching
- chore: bump ts-cli to v0.21.0
- chore: add `check_skill_cli_usage.py` regression validator — prevents drift back to inline Python TML assembly

## 2026-06-27
- feat: add `ts tableau build-model` command (ts-cli 0.18.0) — deterministic TWB-to-model-TML pipeline: parses TWB XML, resolves all internal refs (Calculation_ and copy-style), translates formulas, resolves name collisions, applies formula_ prefix, fixes double aggregation, splits into phased import files by dependency level. New `model_builder.py` module with pure functions for all 8 model-level transforms
- chore: bump ts-cli to v0.18.0
- feat: update `ts-convert-from-tableau` — add scope 4 (Models only) and scope 5 (Tables only) migration modes with per-step scope annotations
- chore: bump ts-cli to v0.17.0 — 9 new pre-transforms (ifnull stripping, sum_if conversion, date arithmetic, scalar MAX/MIN, comment stripping, CSQ alias resolution, no-keyword LOD, if/then/else validation, operator spacing); 156 new tests
- feat: update `ts-convert-from-tableau` — orphan calc detection (Step 3g), Phase 1.5 base model checkpoint, excluded formulas + review flags report sections (BL-053), blend risk classification (BL-045), formula complexity + realistic coverage estimate (BL-047), targeted retry with context cache (BL-049), compound connection prompt (BL-051)
- feat: add `ts-load-source-data` skill v1.0.0 — load CSV data into Snowflake (or generate synthetic data from schema definitions) for ThoughtSpot to connect to; adds `ts-load-*` naming family to skill-naming.md and check_skill_naming.py
- feat: add `ts tableau translate-formulas` CLI command (ts-cli 0.16.0) — deterministic 14-step Tableau → ThoughtSpot formula translation pipeline with dependency DAG, cross-reference resolution, column scoping, parameter conflict detection. Pure-function engine in `tableau_translate.py`
- chore: bump ts-cli to v0.16.0
- feat: update `ts-convert-from-tableau` — add Step 3.6 (join confirmation for published datasources), CLI formula translation reference in Step 5b, two-phase model import in Step 7 (base model first, then formulas with iterative error recovery), cross-reference depth reporting in audit mode (Step A3/A4)

## 2026-06-26
- feat: add `ts load` CLI command group — schema inference, synthetic data generation, Snowflake loading
- feat: add `ts tableau download` command (ts-cli 0.15.0) — download published datasource content (TDSX) from Tableau Server/Cloud, extract archive, validate CSV files for row integrity (column count consistency, corrupt lines with proper quoted-field handling). Prerequisite for BL-010 (`ts-load-source-data`)
- chore: bump ts-cli to v0.15.0
- chore: bump ts-cli to v0.14.0 — register `ts tableau` command group (signin, datasources, datasource); add `--tableau` flag to `ts profiles list`
- feat: add `ts-profile-tableau` skill v1.0.0 — Tableau Server/Cloud credential setup (password + PAT auth), profile management (add/list/test/remove), stored in `~/.claude/tableau-profiles.json`
- feat: add `ts tableau` CLI commands (ts-cli 0.14.0) — `signin` (PAT + password auth), `datasources` (list/search with auto-pagination), `datasource` (detail + `--fields` for VizQL read-metadata). TableauClient with 401 retry and retryable-error backoff
- feat: add Step 3.5 to `ts-convert-from-tableau` — auto-resolve published datasources (sqlproxy) via Tableau REST API; graceful degradation when no Tableau profile configured

## 2026-06-25
- feat: update `ts-object-model-spotql-query` to v1.2.0 — add `references/architecture.md`, the "Why SpotQL" value-prop & architecture reference vs raw DB SQL: ThoughtSpot never executes SpotQL (compiles it to deterministic warehouse SQL via the same QueryGen as Liveboards/Answers/Search/Spotter; the LLM's SQL is intent, never run); semantic-layer guarantees (RLS/CLS, model filters, join defs, governed metrics/LOD/semi-additive, custom calendars, multi-fact chasm/fan-trap resolution); architecture advantages (determinism/traceability, cross-product consistency, physical-layer abstraction + dialect portability, single point of change, governed scale); the hybrid Token-based-Answers (primary) + SpotQL (expressibility fallback) flow with a verification layer unified across both transformers (co-existence + parity). Mermaid diagram + ASCII fallback. SKILL.md capability bullet + References row; README skills table deep-links it as the "Why SpotQL" starting point.
- feat: update `ts-object-model-spotql-query` to v1.1.0 — add `references/integration.md` (raw SpotQL API for non-CLI consumers); Step 6 emits paste-ready request bodies; Step 2 TML parsing hardened (`properties.column_type`, `formulas[]` via `formula_id`, deterministic raw-vs-aggregate-formula classification); capability summary added; Step 1 accepts Model GUID/URL with search as fallback. Record `connection_type` / callosum endpoint finding in open-items.md.
- feat: add `ts-object-model-spotql-query` skill v1.0.0 — query a ThoughtSpot Model with SpotQL: write Semantic SQL (grounded in bundled rules/UDF/pattern references lifted from agent-expressibility-eval), validate it to warehouse SQL via `ts spotql generate-sql`, execute via `fetch-data`, and review the results. The single-question primitive; accuracy / regression / feature / known-limitation testing are documented as compositions over it. Verified live on champ-staging.
- chore: bump ts-cli to v0.13.0 — add `ts spotql generate-sql` / `ts spotql fetch-data` (run SpotQL Semantic SQL against a Model; JSON output, structured query errors); add `raise_for_status` opt-out to the HTTP client so callers can surface 4xx query-error bodies
- refactor: rename `ts-dependency-audit` → `ts-audit` — the skill is a read-only health assessment, not a dependency-graph operation; add new `ts-audit` naming family (#8) to `skill-naming.md` and `check_skill_naming.py`

## 2026-06-18
- feat: add `ts-audit` skill v1.0.0 (originally `ts-dependency-audit`) — cluster-wide ThoughtSpot environment audit across five angles (42 checks): AI Readiness (description/synonym coverage, Spotter readiness score), Data Modeling (complexity, joins, duplicates, grain, overlap classification, zero-column tables), Human Readiness (names, hidden columns, orphans, formula promotion, stale objects), Performance (SQL Views, scalar formulas, progressive joins/filters, date constraints, column sprawl), Security (PII detection, indexing without RLS, CLS gaps, credentials). Interactive HTML report with cluster heatmap, per-model scorecards, and by-check drill-down. Usage analysis (BI Server) planned for Phase 2.

## 2026-06-17
- feat: ts-cli 0.11.0 → 0.12.0 — add `ts connections create` (Snowflake **key-pair** auth, no tables): `POST /api/rest/2.0/connection/create` with `authenticationType=KEY_PAIR`, the private key read from `--private-key-path` into the `private_key` config attribute (never logged), `validate=false`. The three `ts-convert-from-*` skills now offer **creating a new connection** at the connection step instead of only selecting an existing one (snowflake-sv full create path; tableau create path when the source is Snowflake; databricks-mv gets the explicit "stop & instruct" fallback — native PAT/OAuth create backlogged as BL-036).
- feat: codify the pre-import TML gate (audit angle 11). `ts tml lint` now also checks **I8** (duplicate `column_id` — a hard import rejection); the three `ts-convert-from-*` skills replace their copy-pasted hand-written grep gate with a `ts tml lint` call (snowflake-sv 1.11.2, databricks-mv 1.5.2, tableau 1.14.2). New `check_no_inline_tml_gate.py` guards the migration (fails if a CLI convert skill re-adds a hand-rolled `grep … aggregation:` gate), wired into pre-commit + CI — mirroring `check_no_v1_endpoints`. ts-cli 0.10.1 → 0.11.0.
- feat: first full audit sweep via the `repo-audit` runner (44 findings: 5 high) + remediations. Quick-win fixes: currency-anchor validator extended to `agents/shared/schemas/` (presence now CI-gated, staleness stays a soft nudge) with anchors added to all 16 schema files; fixed 8 broken slash-command references (`/ts-profile-setup`→`/ts-profile-thoughtspot`, `/snowflake-profile-setup`→`/ts-profile-snowflake`); corrected ts-cli README `--create-new` default (was documented backwards) + `--policy` values; corrected provably-wrong product-currency claims in `snowflake-schema.md` (`facts[]`/`sample_values`/filter-labels now YAML-expressible) and `databricks-metric-view.md` (Metric Views GA 2026-04-02, Preview-channel obsolete; v0.1 legacy). Converter emit changes deferred to BL-031/BL-032 pending live verification. Filed BL-030…BL-035. Reports: `docs/audit/2026-06-17-{external,full}.md` (inaugural manual ledger preserved as `-inaugural-full.md`).
- feat: establish a repeatable repo-audit framework (`.claude/rules/repo-audit.md`). Codifies the two-bucket rule (every finding → a validator or a dated backlog item), an internal/external angle taxonomy (the 12 internal angles are per-PR validators; external angles **13 product-currency**, **14 performance**, **16 dependency-currency** run on a weekly specialist sweep; **15 fidelity** parked). Adds `check_audit_freshness.py` (soft nudge when an external sweep or full audit is due by time **or** by accumulated work — never auto-runs), `check_mapping_currency.py` (per-PR soft nudge on stale platform-mapping *currency anchors*), currency anchors on all 10 `agents/shared/mappings/` files, and the inaugural audit ledger `docs/audit/2026-06-17-full.md`. Both nudges wired into `pre-commit.sh`. Includes the on-demand `repo-audit` Workflow runner (`.claude/workflows/repo-audit.js`, `scope: external | full`) — per-platform specialist + performance + dependency finders → synthesis → a report saved under `docs/audit/`. Execution is nudge-driven and on-demand, **no scheduled cron**. Designed config-driven for reuse in other repos.
- feat: harden three validators (final repo-audit Low items). Add `check_no_v1_endpoints.py` — an AST-based guard that fails CI/pre-commit if any non-test, non-docstring Python literal calls a `/tspublic/v1/` endpoint (wired into both `scripts/pre-commit.sh` and `validate.yml`); docstring mentions and test assertions are exempt. Tighten `check_secrets.py` placeholder detection so short markers (`test`/`my`/`xxx`/`fake`) only match as whole `\W`-delimited tokens, not arbitrary substrings (a secret merely *containing* those letters is no longer waved through). Make `check_coverage_matrix.py` reject dateless `BACKLOG` exemptions — each must now carry a target date or `#NNN`/`BL-NNN` ref; added BL-029 for the three skills still owing a coverage matrix.
- feat: ts-cli v0.10.0 — add `ts tml lint`, a connection-free pre-import linter for the model invariants `VALIDATE_ONLY` accepts silently (guid placement, I1 unpaired formula, I2 aggregation-in-formula, I4 model_table id≠name, I5 COUNT_DISTINCT on a physical column); exits 1 on findings so it composes with `&&`. Also adds central HTTP-error handling in `client.request()` — non-2xx now prints one secret-free diagnostic line (preferring the TS error body's `debug`/`error`/`message`) and exits 1 instead of dumping a traceback. (Codification Phase 1 from the repo quality audit.)
- fix(databricks): correct the Genie SETUP.md shared-path — shared refs live under `.assistant/skills/shared/` (matching `deploy.sh` and the skills' `../shared/` references), not `.assistant/shared/` (the documented manual install was broken). Add a `databricks` column to `generate_parity.py`/`PARITY.md` so the Genie runtime is visible in the matrix, and document `agents/databricks/` as a deliberately-separate runtime in `runtime-coverage.md`. (From the repo quality audit.)
- fix: ts-convert-from-tableau v1.14.1 — string **parameters** must be `CHAR` not `VARCHAR` (ThoughtSpot rejects VARCHAR list params); add a MEASURE-vs-ATTRIBUTE classification rule (formula is a measure if it transitively references a measure formula; numeric physical columns default to MEASURE; qualify bare unbracketed column refs). From the live Catalog Health Workbook migration.
- docs: add BL-027 (explicit table→ThoughtSpot binding instead of search-and-guess) and BL-028 (Audit mode for the visualization layer) to the backlog — both from the Catalog Health migration.
- docs: add `agents/shared/schemas/thoughtspot-chart-types.md` — verified `answer.chart.type` enum (44 values) + analytical-intent → chart-type mapping + full **Muze charting library (`ADVANCED_*`)** spec (custom_chart_config shelf model, early-access, verified live on se-thoughtspot); cited by ts-convert-from-tableau
- feat: ts-convert-from-tableau v1.14.0 — add a Legacy-vs-Muze charting-library prompt (Step 10-charts); on Muze, emit `ADVANCED_*` + `custom_chart_config` (Tableau Color → slice-with-color, small multiples → trellis-by) for cartesian/pivot intents with Legacy fallback for the rest
- docs: add BL-026 + design `docs/designs/ts-object-liveboard-builder.md` (build-the-best-liveboard skill)

## 2026-06-16
- chore: migrate the last v1 API (`/tspublic/v1/connection/fetchConnection`, removed/404 on newer builds) to v2 `connection/search`; ts-cli v0.9.0 (`_fetch_connection_v2`/`_adapt_v2_databases`) + databricks `ts_client.py`; no v1 endpoints remain in the repo
- feat: ts-convert-from-tableau v1.11.0 — reorder so the source-table question (exist/create/search) comes before any connection selection or search; add connection-scoped vs instance-wide search (`--name` pattern, `metadata_header.dataSourceName` filter)
- feat: add the same connection-scoped vs instance-wide table search to ts-convert-from-snowflake-sv (v1.10.0) and ts-convert-from-databricks-mv (v1.4.0)
- feat: add a how-to-identify-the-connection prompt (name it / filter by partial string / list all) to the connection-selection step of all three CLI from-* conversion skills — ts-convert-from-tableau (v1.12.0), ts-convert-from-snowflake-sv (v1.11.0), ts-convert-from-databricks-mv (v1.5.0)
- feat: ts-convert-from-tableau v1.13.0 — add a migration-scope choice (Models+Liveboards / Tables+Models only / Liveboards-only with an existing-model picker: GUID/name/filter/list-all, models found via `--subtype WORKSHEET` + `worksheetVersion == "V2"`); fix the model `obj_id` reuse bug (a fresh model's requested obj_id is reassigned by ThoughtSpot — read the real one back before referencing it in liveboard viz/cohort bindings); add an efficiency section (batch independent prompts, single-pass parse, one model export for obj_id + param UUIDs + resolved names)

## 2026-06-14
- feat: BL-024 — row-offset table calcs (INDEX/LOOKUP/FIRST/LAST/SIZE) translate via native TS window functions (`moving_sum`, `first_value`, `last_value`, `rank`) instead of SQL pass-through (which fails for DATE/numeric ORDER BY columns); ts-convert-from-tableau v1.10.0; live-verified 2026-06-15
- chore: retire Cursor runtime — delete `agents/cursor/` (14 .mdc rules, SETUP.md, install scripts), remove all Cursor references from validators, pre-commit hook, rules, README, backlog, and auditor; closes BL-017
- docs: add data-blend-to-model shared reference — two-datasource Tableau blend → single ThoughtSpot model with LEFT_OUTER join worked example

## 2026-06-13
- docs(BL-018): map four previously-unmapped SV features to ThoughtSpot — range joins (GAP-08), filter labels (GAP-10), view-backed sources (GAP-04), verified queries → NLS Feedback TML (GAP-05); update `ts-from-snowflake-rules.md` DDL format + mapping sections, `sv-to-ts-gap-analysis.md`, `thoughtspot-model-tml.md` (remove `is_mandatory`, correct `apply_on_tables` semantics), and `backlog.md` (BL-019/BL-020 for Databricks/Tableau parity)
- feat(from-snowflake-sv): v1.9.0 — identifier resolution engine: facts parsing (BL-003b), metric→fact resolution (BL-003c), double aggregation via group_aggregate (BL-003), window metrics referencing metrics (GAP-13), joinless SV handling (GAP-03/BL-004)
- fix(from-snowflake-sv): fact references use `[formula_<id>]` syntax, not display name — live-verified; add `if()` parenthesization fix; add identifier resolution worked example
- docs: add `ts-from-snowflake-identifier-resolution.md` worked example — facts, metric-on-fact, double aggregation, verified end-to-end
- fix(coco): mirror parity — port PT1 pass-through policy and error-010256 rule to CoCo skills; reverse-port name-normalisation rule to CLI; fix count_distinct example (audit C8/F2/F11)
- fix(snowflake-sv): Phase 1 audit — fail-loud SV DDL parsing (C5), LEFT_OUTER join default (F5), authority fix (F4), discovery SQL fix (F8), Mode C comparison fix (F7), INFORMATION_SCHEMA case fix (F12)

## 2026-06-12
- feat(from-tableau): v1.8.0 — static sets → column sets (BL-009 Phase 2a); detect+log Top-N/set-ops/set-actions as deferred; refresh thoughtspot-sets-tml.md with column-set/query-set vocabulary
- fix(mappings): trig unit bug — SIN/COS/TAN now convert radians→degrees (Tableau trig is radians, ThoughtSpot is degrees); UPPER/LOWER fixed to sql_string_op pass-through (no native in TS 26.6.0); REGEXP_MATCH fixed to sql_bool_op (returns boolean, not 1/0)
- feat(convert-from): PT1 cross-skill pass-through policy — scalar pass-throughs reliable; aggregate pass-throughs (sql_*_aggregate_op) must be flagged for review; policy in ts-model-conversion-invariants.md, applied across Tableau/SV/Databricks formula-translation files
- feat(from-tableau): v1.7.0 — Phase-1 Tableau function mappings: DATEPARSE, EXP, trig, STARTSWITH/ENDSWITH, PI/RADIANS/DEGREES composites, PROPER/ASCII/CHAR/REGEXP/FINDNTH pass-through, WINDOW_*/RUNNING_COUNT table-calc notes (BL-009 Phase 1)
- feat(validate): `check_tml.py` now enforces I4 (join id==name exact case) and I5 (no `aggregation: COUNT_DISTINCT` on physical columns); added `tools/validate/tests/test_check_tml.py` (BL-001)
- feat(convert-from): add an inline pre-import validation gate (I1/I2/I4/I5) to ts-convert-from-tableau, -snowflake-sv, and -databricks-mv before model TML import (BL-001)
- fix(mappings): Snowflake `BOOLEAN` maps to `BOOL` for ThoughtSpot — `ts tables create` rejects `BOOLEAN` on Snowflake connections (BL-006)
- chore(mirrors): mirror conversion invariants into the tableau + databricks-mv cursor rules, completing BL-012 parity

## 2026-06-11
- docs: add `ts-model-conversion-invariants.md` shared reference — canonical hard-rule checklist (I1–I7 + EXC1 + N1) for all Model-producing conversion skills; cross-linked from `thoughtspot-model-tml.md`
- feat: add `conversion-consistency-auditor` subagent — semantic auditor for I1–I7 and N1 across the five conversion skills plus their cursor + coco-snowsight mirrors (Mirror parity section); I3 is advisory (WARN); run before merging any conversion-skill PR
- fix(agents): update `consistency-checker` to current layout — scans `agents/cli/`, `agents/claude/`, and `agents/coco-snowsight/` (keeps `agents/claude/`-only skills like `ts-profile-snowflake` in coverage)
- feat(from-snowflake-sv): v1.5.0 — drop TEST_SV_ prefix, I5 explicit note, open-items.md
- feat(from-databricks-mv): v1.1.0 — preserve Spotter setting on in-place update, drop TEST_MV_ prefix
- fix(from-tableau): v1.5.39 — add I1–I6 hard rules to Step 5b, I7 formula-reference gate
- fix(mirrors): mirror the conversion invariants into the snowflake-sv cursor rule (v1.3.1) and coco-snowsight skill (v1.3.1) — I1–I6 + N1 callout and I7 gate, matching the CLI skill
- fix(validators): complete the `agents/claude/`→`agents/cli/` + `agents/coco/`→`agents/coco-snowsight/` rename left incomplete by PR #18. The validators (`check_references`, `check_skill_versions`, `check_skill_naming`, `check_yaml`, `check_patterns`, `check_smoke_tests`, `check_consistency`, `check_open_items`, `suggest_*`) globbed the old paths and were silently checking only 1 of 20 skills; they now scan `agents/cli/`, `agents/claude/`, and `agents/coco-snowsight/`. `check_references` now skips `{template}` placeholder link targets.
- docs(layout): fix stale `agents/claude/` / `agents/coco/` references across `.claude/rules/*`, `CLAUDE.md`, `agents/PARITY.md`, `agents/cursor/*`, `agents/coco-snowsight/CLAUDE.md`, `tools/` docs, and two cursor rule mirrors to the canonical `agents/cli/` + `agents/coco-snowsight/` layout (`agents/claude/` retained for the Claude-only `ts-profile-snowflake` annex)

## 2026-06-10
- feat: `ts-convert-from-tableau` v1.2.0 → v1.5.37 — major dashboard→liveboard migration upgrade, hardened against 6 real workbook migrations on a live ThoughtSpot. Adds Step 4.5 (confirm tables exist before searching; connection required, no placeholders), Step 5.5 (Spotter on every model), Step 7/7.5 (formula-review checkpoint + model confirmation), Step 9d (orphan-worksheet prompt), full liveboard generation (obj_id binding, complete chart blocks with resolved names, note tiles, KPI-per-measure, parameter header chips, Migration Summary tab, curated style themes), Step 11.5 (formula-coverage answers — every formula gets a testable answer), and Step 12 (written `MIGRATION_REPORT.md` with outcomes table, hyperlinks, and a full formula-mapping status table)
- docs: expand Tableau shared reference library — `tableau-formula-translation.md` (rank direction arg; `cumulative_*`/`moving_*` are query-time only and take the shelf attribute as sort arg; `concat()` not `+`; dynamic year-comparison; drop redundant pass-through formulas), `tableau-tml-rules.md` (in-place re-import requires top-level `guid`/`obj_id`), and schema docs (`thoughtspot-answer-tml.md` PERCENTAGE format; `thoughtspot-liveboard-tml.md` TABLE_MODE tiles omit the chart block)

## 2026-06-09
- feat: add `ts-convert-from-tableau` skill — convert Tableau workbooks (.twb/.twbx) into ThoughtSpot table + model TMLs with optional dashboard-to-liveboard migration; available in CLI, Cortex Code CLI, and Cursor
- docs: add Tableau shared reference library — formula translation (`tableau-formula-translation.md`) and TML generation rules (`tableau-tml-rules.md`) in `agents/shared/mappings/tableau/`
- chore: bump ts-cli to v0.8.0 — adds `--include-obj-id`, `--include-obj-id-ref`, `--no-guid` flags to `ts tml export` for export_options support

## 2026-06-01
- feat: add `ts metadata report` command — dependency walk + TML probes + risk classifier + formatters
- feat: rewrite ts-dependency-manager Steps 4/5 to delegate to `ts metadata report` CLI

## 2026-05-22
- feat: add `ts-convert-to-databricks-mv` and `ts-convert-from-databricks-mv` skills — convert between ThoughtSpot Models and Databricks Metric Views (v0.1 single-source and v1.1 multi-source); available in CLI, Cortex Code CLI, and Cursor
- feat: add `ts-profile-databricks` skill — manage Databricks connection profiles with Service Principal (OAuth M2M), PAT, or existing CLI profile auth; available in CLI, Cortex Code CLI, and Cursor
- docs: add Databricks Metric View shared reference library — MV YAML schema (`databricks-metric-view.md`), bidirectional mapping rules, formula translation reference, and property coverage matrix in `agents/shared/mappings/ts-databricks/`

## 2026-05-13
- feat: add `ts-recipe-formula-hms-display-snowflake` (v1.0.0) — deploys four Snowflake scalar UDFs (`format_seconds_to_hms`, `format_seconds_to_dhms`, `format_minutes_to_hm`, `format_minutes_to_dhm`) for formatting integer durations as `HH:MM:SS` / `DD:HH:MM:SS` / `HH:MM` / `DD:HH:MM` strings; shows ThoughtSpot `sql_string_op` formula syntax and TML pattern; available in CLI, Cortex Code CLI, Snowsight Workspaces, and Cursor
- refactor: introduce `ts-recipe-*` naming family for analytical capability skills — rename `ts-setup-snowflake-udfs-business-days` → `ts-recipe-formula-business-days-snowflake` (v2.0.0 MAJOR); update validator, skill-naming doc, smoke tests, README, and all SETUP.md files
- feat: split README "Setup" section into "Connection Profiles" (`ts-profile-*`) and "Recipes" (`ts-recipe-*`) for clearer category separation

## 2026-05-12
- feat: add `ts-setup-snowflake-udfs-business-days` (v1.0.0) — deploys three Snowflake scalar UDFs for weekday-only date arithmetic (`get_business_days_clamped`, `get_business_minutes_clamped`, `get_business_duration_str`) and shows ThoughtSpot `sql_int_op` / `sql_string_op` formula syntax; available in CLI, Cortex Code CLI, Snowsight Workspaces, and Cursor

## 2026-05-11
- chore: bump ts-cli to v0.6.0
- fix: `ts tml import` default changed from `--create-new` to `--no-create-new` — prevents silent duplicate creation when importing TML with an existing GUID; updated help text and docstring with explicit warning about the `--create-new` + existing-GUID pitfall
- fix: `ts tml export --type FEEDBACK` now exits immediately with a clear error explaining that feedback TML must be exported via the feedback object's own GUID (the API returns HTTP 400 for model GUID + type=FEEDBACK); directs user to `ts metadata dependents` to locate feedback GUIDs
- feat: `ts profiles list --snowflake` — lists Snowflake profiles from `~/.claude/snowflake-profiles.json` (name, method, account, warehouse); previously there was no CLI path to list Snowflake profiles
- fix: `ts-object-model-coach` Step 2b — replaced broken `--type FEEDBACK` export with the correct two-step approach: find feedback object GUIDs via `ts metadata dependents`, then export each GUID directly; handles zero-feedback models gracefully (proceeds with empty list)
- chore: bump ts-cli to v0.5.0 — adds `--type` flag to `ts tml export` for FEEDBACK TML export
- fix: migrate all direct urllib API calls in `ts-object-model-coach` to ts CLI (`ts tml export --type FEEDBACK`, `ts metadata dependents --raw`, `ts metadata dependents`); Cursor mirror updated to match (v1.2.0)

## 2026-05-05
- feat: add Mode C (update existing) to `ts-convert-from-snowflake-sv` (v1.4.0) and `ts-convert-to-snowflake-sv` (v1.2.0) — diff-and-confirm workflow for applying a changed SV/Model to an existing counterpart; per-column KEEP/UPDATE/MERGE decisions; `ai_context` and Data Model Instructions never touched; coaching handoff to `/ts-object-model-coach` after import. Mirrored in Cursor `.mdc` (v1.3.0 / v1.2.0) and CoCo runtimes (v1.3.0 / v1.2.0).
- feat: Mode B (split/merge) is now first-class in both conversion skill mode menus — previously a sub-flow triggered by domain detection, now an explicit choice at session start.
- docs: rename "reverse-engineer" → "convert" in all descriptions for `ts-convert-from-snowflake-sv` across Claude, Cursor, CoCo, README, and SETUP.md files.
- test: add `--mode-c` flag to `smoke_ts_from_snowflake.py` (verifies `--no-create-new` updates in-place, not duplicate) and `smoke_ts_to_snowflake.py` (verifies `CREATE OR REPLACE` on an existing SV).

## 2026-04-28
- feat: `ts-convert-from-snowflake-sv` adds Step 9.5 — confirm Spotter (AI search) enablement before import (default Y). Preserves the existing setting on in-place updates rather than silently overwriting. Mirrored in Cursor `.mdc` and CoCo runtimes.
- feat: SV ↔ TS metadata mapping — `with synonyms=(...)` now maps to display name + `properties.synonyms` (top-level `synonyms:` is silently dropped on TS import); per-dimension/metric `comment='...'` → column `description`; per-table `comment='...'` in the SV `tables(...)` block → TS Table TML `table.description`. Affects `ts-convert-from-snowflake-sv` and `ts-convert-to-snowflake-sv` across Claude/Cursor/CoCo runtimes.
- feat: semi-additive direction handling — `non additive by (col desc nulls last)` now translates to `first_value(...)` (previously documented as untranslatable); `asc nulls last` continues to translate to `last_value(...)`.
- fix: `count_distinct(...)` is rejected by the TS formula parser — `unique count` (with a space, not underscore) is the only valid form. Updated all references in `thoughtspot-formula-patterns.md`, `ts-snowflake-formula-translation.md`, and reverse-flow rules.
- fix: `+` does not concatenate strings in TS formulas — must use `concat(...)`. Documented in `thoughtspot-formula-patterns.md`.
- docs: add `ts-from-snowflake-dunder.md` worked example exercising multi-value synonyms, descriptions, table comments, semi-additive (closing/opening), `unique count`, and `concat()`. BIRD example unchanged.
- chore: add skill-naming convention rule + validator (`.claude/rules/skill-naming.md`, `tools/validate/check_skill_naming.py`) — six families documented (`ts-object-*`, `ts-profile-*`, `ts-convert-*`, `ts-dependency-*`, `ts-variable-*`, `ts-setup-*`); pre-commit fails if a skill name doesn't match a family or sit on the (currently empty) allowlist
- feat: rename `ts-coach-model` → `ts-object-model-coach` (skill bumped to v2.0.0) — aligns with the `ts-object-{type}-{verb}` family pattern; slash command, smoke-test filename, and cache directory all change
- chore: add runtime-coverage convention rule + validator (`.claude/rules/runtime-coverage.md`, `tools/validate/check_runtime_coverage.py`) — Cursor mirrors Claude; CoCo divergences documented in `EXPECTED_DIVERGENCES` map with per-entry justification; pre-commit fails on undocumented gaps
- feat: add Cursor mirrors for `ts-dependency-manager` and `ts-object-model-coach` (closes the previously-undetected gap where Claude had skills with no Cursor `.mdc`); both marked "Untested in Cursor" pending validation by a Cursor user

## 2026-04-27
- feat: add `ts-dependency-manager` skill (v1.0.0) — Audit / Remove / Repoint modes for safely changing columns across Models, Views, Answers, Liveboards, and Sets, with alias-aware dep walk, STOP-condition handling, TML backup, post-import verification, drift detection, and rollback
- feat: add `ts metadata dependents <guid>` to ts-cli (v0.4.0) — wraps v2 metadata/search with `include_dependent_objects=true`; default flat output (one row per dep) or `--raw` for the full v2 response
- feat: add `ts-variable-timezone` skill — manage `ts_user_timezone` variable at org and user level (Beta in 26.5, EA in 26.6)

## 2026-04-24
- feat: add skill versioning — every SKILL.md now has a `## Changelog` section with semver tracking
- feat: add interactive changelog prompt to pre-commit hook (`suggest_skill_version.py`)
- chore: move `semantic-layer-compare` skill to `semantic-layer-research` repo

## 2026-04-23
- feat: add cross-platform credential support (Windows/Linux) and Cursor agent runtime
- feat: add multi-domain split and multi-SV merge to conversion skills
- feat: redesign `ts-convert-to-snowflake-sv` to use native DDL creation (DROP YAML output)
- refactor: rename project from `thoughtspot-skills` to `thoughtspot-agent-skills`
- chore: add branching protocol rule (session-start check)

## 2026-04-22
- feat(ts-object-answer-promote): add parameter promotion — promote Answer parameters to Model alongside formulas (Abhijit Das)

## 2026-04-21
- refactor: add `ts-` brand prefix to all skill names
- refactor: rename `ts-object-model-promote` → `ts-object-answer-promote`
- refactor: restructure profile setup skills — profile before platform
