# Mirror sync debt

Acknowledged gaps between canonical CLI/Claude skills and their runtime mirrors.
`check_mirror_sync.py` fails on any UNACKNOWLEDGED gap. Closing a row = syncing the
mirror and bumping its `synced-from` marker.

| Mirror | At (marker) | CLI now | Gap | Owner decision |
|---|---|---|---|---|
| agents/coco-snowsight/ts-convert-to-snowflake-sv/SKILL.md | v1.2.2 | v1.3.1 | SV-to v1.2.3 (minor checklist addition) + v1.2.4 (credential-refresh pointer) + v1.3.0 (Step 9.5C/Step 11 delegate to `ts snowflake diff`/`lint-ddl`, BL-063) + v1.3.1 (2026-07-11 mapping batch: document `sql_view` → `base_table.definition:` D-Direct auto-map option, emission deferred to BL-031, audit 13.4) | won't sync — v1.3.0/v1.3.1 depend on the `ts` CLI (`ts snowflake diff`/`lint-ddl`) or are docs-only pending BL-031 emission work, which CoCo cannot invoke/benefit from yet; sync scheduled for the earlier checklist-only gap. The underlying shared-reference content (snowflake-schema.md, ts-snowflake-properties.md) reaches CoCo via stage-sync independent of this marker gap. |
| agents/coco-snowsight/ts-convert-from-snowflake-sv/SKILL.md | v1.9.0 | v1.16.0 | CLI-only: connection-scoped table search (v1.10.0) + N/F/L connection-identification prompt (v1.11.0/v1.11.1) + Step C3 delegated to `ts snowflake diff` (v1.13.0, BL-063) — all depend on `ts connections list` / `ts snowflake diff` and a local shell; plus content-only additions v1.14.0/v1.14.1 (cumulative window + import-gate fixes), v1.15.0 (formula function-composition rules, SE-cluster-validated), and v1.16.0 (2026-07-11 mapping batch: SQL-query logical tables `base_table.definition:` → SQL View TML, `is_enum`/`sample_values` dimension clauses, free-text `ai_sql_generation`/`ai_question_categorization` instructions, audit 13.5/13.6/13.7) | won't sync — CoCo runs in Snowsight (stored procs, no `ts` CLI); no CoCo equivalent applies for the CLI-only versions. The content-only versions' shared-reference source (thoughtspot-formula-patterns.md, ts-snowflake-formula-translation.md, ts-from-snowflake-rules.md, snowflake-schema.md) reaches CoCo via stage-sync independent of this marker gap. |
