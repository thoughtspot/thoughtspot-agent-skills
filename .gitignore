# Credential and profile files
*.env
.env.*
*-profiles.json
*_profiles.json

# Private keys and certificates
*.pem
*.p8
*.key
*.pfx
*.p12

# Tokens and secrets
*token*.txt
*secret*.txt
*credentials*.json
*credentials*.yaml

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
dist/
build/

# macOS
.DS_Store

# Editor
*.swp
*.swo
.idea/
.vscode/

# Temp / test output files
/tmp/
*.tmp

# Local test data
.data/

# AI tool local settings — ignore personal config, track shared project config
# (.claude/agents/, .claude/rules/, .claude/settings.json are committed; settings.local.json is not)
.cursor/
.claude/settings.local.json
.gemini/

# Deployment state (local only — tracks last Snowflake stage sync)
.snowflake-deploy-sha

# Per-machine smoke test configuration (model names, connection details, etc.)
tools/smoke-tests/smoke-config.local.json

# Skills pending testing — not yet published to remote
agents/claude/ts-convert-to-databricks-mv/
agents/claude/ts-object-model-builder/
tools/smoke-tests/smoke_ts_model_builder.py

# Databricks shared content — not yet published to remote
agents/shared/worked-examples/unity-catalog/